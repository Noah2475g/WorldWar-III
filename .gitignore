node_modules/
dist/
# Der Bau MIT Mehrspielereinstieg (T-M39-04): dieselbe Quelle, die Bauflagge gesetzt.
# Er ist die Gegenprobe zu "kein WebSocket im ausgelieferten Buendel" und wird wie jeder
# andere Bau nicht eingecheckt - die MESSUNG steht in docs/reports/packaging-netfree.json.
dist-mp/
coverage/
*.tsbuildinfo
.DS_Store
Thumbs.db

# Tauri build output (M11)
apps/desktop/src-tauri/target/
# Vom Bau erzeugte ACL-Schemata (T-M16-03). Sie entstehen bei jedem tauri build neu
# aus capabilities/ und tauri.conf.json; eingecheckt waeren sie eine zweite Wahrheit
# neben der Konfiguration, die sie erzeugt.
apps/desktop/src-tauri/gen/

# Local runtime artefacts
*.log
.vite/
.turbo/

# Raw research downloads: large, re-fetchable, not part of the product
docs/research/_raw/
