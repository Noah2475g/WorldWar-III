# PROGRESS

Eine Zeile je abgeschlossener Aufgabe: `T-ID · Datum · Kurzbeschreibung · Tests · verify`

## Meilenstein M0 — Fundament ✅

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M0-01 | 2026-09-02 | Monorepo mit pnpm, TypeScript (strict), Vitest, ESLint 9 — fünf Pakete plus Headless-App | grün |
| T-M0-02 | 2026-09-02 | Skriptfläche nach D15; `verify.mjs` überspringt Abdeckungsschwellen für leere Pakete (behebt den Bootstrap-Blocker) | grün |
| T-M0-03 | 2026-09-02 | Sieben Guards mit dauerhaften Verstoß-Fixtures; vier prüfen die echte ESLint-Konfiguration | grün |
| T-M0-04 | 2026-09-02 | Anforderungs-Abgleich als reine Funktion; liest den `scope`-Block, verlangt echte Zusicherungen | grün |
| T-M0-05 | 2026-09-02 | Plan-Konsistenztest — fand acht Abweichungen zwischen `03-TASKS.md` und `tasks.yaml` | grün |

## Meilenstein M1 — Simulationskern ✅

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M1-01 | 2026-09-02 | Festkomma-Arithmetik, Rundung halb vom Nullpunkt weg, Überlauf wirft, negative Null normalisiert | grün |
| T-M1-02 | 2026-09-02 | xoshiro128\*\*-Zufallsgenerator als reine Daten; Golden-Vektor für Seed 42 eingefroren | grün |
| T-M1-03 | 2026-09-02 | Zustands-Hash über kanonische Projektion; Ereignisprotokoll ausgeschlossen | grün |
| T-M1-04 | 2026-09-02 | Vollständiges Zustandsmodell inkl. Trefferpunkte-Pool, KI-Gedächtnis, Aufklärung, Produktionsreste | grün |
| T-M1-05 | 2026-09-02 | Tick-Pipeline mit 12 Phasen + Tagesabrechnung nach der Buchhaltung | grün |
| T-M1-06 | 2026-09-02 | Determinismus über 500 Ticks; Golden-Master erzeugt und gegengeprüft | grün |
| T-M1-07 | 2026-09-02 | Kommando-Registry: `check` und `apply` teilen eine Regel; unbekannte Kommandos werden abgelehnt | grün |
| T-M1-08 | 2026-09-02 | 25 Ereignisarten, Vollständigkeit zur Übersetzungszeit geprüft; Alarme steuern das Vorspulen | grün |
| T-M1-09 | 2026-09-02 | Kern-Uhr: `runTicks`, `fastForward` mit sechs Zielarten, Alarm-Unterbrechung, harte Obergrenze | grün |

## Meilenstein M2 — Karte, Graph, Wege ✅

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M2-01 | 2026-09-02 | Kartenvalidator mit zehn Fehlerklassen inkl. `EDGE_ASYMMETRY`; Erreichbarkeit über Land **und** See | grün |
| T-M2-02 | 2026-09-02 | Testkarte „Kleine Welt": 12 Provinzen, 3 Nationen, alle Geländetypen, zwei Inseln, Fluss und Meerenge | grün |
| T-M2-03 | 2026-09-02 | Wegfindung mit deterministischem Gleichstand; Kostenfunktion kommt vom Aufrufer | grün |
| T-M2-04 | 2026-09-02 | Szenario-Lader: Spielsituationen als YAML statt als Testcode | grün |

## Meilenstein M3 — Wirtschaft, Bau, Rekrutierung ✅

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M3-01 | 2026-09-02 | Regelwerk als Datendateien: 7 Ressourcen, 7 Gebäude, 10 Einheiten, KI-Gewichte — mit strenger Prüfung beim Laden | grün |
| T-M3-02 | 2026-09-02 | Produktion mit belegter Moralformel (0,20 + 0,80 × Moral) und verlustfreiem Restwertübertrag | grün |
| T-M3-03 | 2026-09-03 | Unterhalt, Mangelzustände statt negativer Bestände, Lagergrenzen mit Tagesmeldung | grün |
| T-M3-04 | 2026-09-03 | Bauaufträge mit Sofortzahlung, Bauplatzgrenze, Abbruch mit halber Erstattung, Verfall bei Eigentümerwechsel | grün |
| T-M3-05 | 2026-09-03 | Rekrutierung mit Gebäudevoraussetzung; frische Einheiten starten mit moralabhängiger Stärke | grün |
| T-M3-06 | 2026-09-03 | Markt mit tickfestem Preis für alle Spieler, Preisbewegung durch Nachfrage, tägliche Rückkehr zum Grundwert | grün |

**Bemerkenswert in M3:**
- Der Fairness-Test des Marktes deckte auf, dass ein tickweise wanderender Preis den
  ersten Spieler in der Zugreihenfolge strukturell bevorzugt hätte — genau der Vorteil,
  den das Original für Geld verkauft. Der Preis ist jetzt für den ganzen Tick fixiert.
- Die belegte Moralwirkung auf frische Rekruten trifft im Trefferpunkte-Pool-Modell eine
  Designentscheidung, die schriftlich festgehalten ist (`DECISIONS.md`, 2026-09-03).
- Mehrere Tests waren zunächst falsch, weil sie die Produktion desselben Ticks
  mitgerechnet haben. Sie vergleichen jetzt gegen einen Leerlauf-Tick.

**Bemerkenswert unterwegs:**
- Der Festkomma-Linter hat dreimal angeschlagen — jedes Mal bei echter Ganzzahl-Rechnung,
  die nun eine begründete Ausnahme trägt. Kein Fehlalarm, aber auch kein Selbstläufer.
- Der Golden-Master wurde einmal absichtlich neu erzeugt (neues Spielerfeld) und einmal
  zur Probe gebrochen — er schlägt bei einer Ein-Punkt-Änderung an.
- Ein echter Fehler, den kein Test fand: `requirements-coverage.mjs` tat gar nichts, weil
  sein Einstiegspunkt-Vergleich unter Windows nie zutraf. Jetzt per Kindprozess getestet.

## Meilenstein M4 — Armeen, Bewegung, Kampf ✅

*Nachgetragen am 2026-09-03 aus `tasks.yaml` und der Commit-Historie — die Einträge
M4 bis M8 fehlten, obwohl die Aufgaben erledigt waren.*

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M4-01 | 2026-09-03 | Armeen bilden, teilen, zusammenlegen — Trefferpunkte bleiben dabei exakt erhalten | grün |
| T-M4-02 | 2026-09-03 | Bewegung: angezeigte Ankunft = tatsächliche Ankunft, kein Durchmarsch durch besetzte Provinzen, Bahn- und Geländefaktoren belegt | grün |
| T-M4-03 | 2026-09-03 | Kampfauflösung mit Stapel-Deckel, Mindestschaden gegen Endlosschlachten, symmetrisch bei Seitentausch, Dreiparteienkampf löst sich auf | grün |
| T-M4-04 | 2026-09-03 | Bombardement ohne Gegenschaden; Rückzug mit Verlust, doppelter Aufmarschverzögerung und Sperrfrist | grün |
| T-M4-05 | 2026-09-03 | Auffrischung nur im eigenen Gebiet und nicht im Kampf; bei Materialmangel halbiert | grün |
| T-M4-05b | 2026-09-03 | Seetransport: Kapazität, belegte Ein- und Ausschiffungszeiten, Verwundbarkeit auf See, Landungsmalus | grün |
| T-M4-05c | 2026-09-03 | Luftstreitkräfte als Fernwaffe — Wirkung nur im Umkreis des Flugplatzes, keine Eroberung, Bodenzeit nach Einsatz | grün |
| T-M4-06 | 2026-09-03 | **Durchstich:** kopflose Minimalpartie über 500 Ticks mit Bauen, Rekrutieren, Marschieren, Kampf, Eroberung; Endzustand als Golden-Master | grün |

## Meilenstein M5 — Moral, Eroberung, Sieg ✅

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M5-01 | 2026-09-03 | Moral als Ziel-/Istwert mit Drift von einem Siebtel je Tag (nicht je Tick); jeder Term einzeln geprüft | grün |
| T-M5-02 | 2026-09-03 | Eigentümerwechsel nur ohne Verteidiger; Aufstandsrisiko (33 − Moral) × 3 % je Tag; Garnison unterdrückt | grün |
| T-M5-03 | 2026-09-03 | Hauptstadt, Punkte, Sieg und Ausscheiden — Prüfung nur im Tageswechsel; Hauptstadtverlegung mit Kosten und Sperrfrist | grün |
| T-M5-04 | 2026-09-03 | Ereignisprotokoll als Ringpuffer, Filter nach Art und Spieler, Verdichtung zu Tagesbündeln bei hohem Tempo | grün |
| T-M5-05 | 2026-09-03 | Hot-Seat mit zwei menschlichen Spielern — ohne Sonderpfad im Kern, Hash reproduzierbar | grün |
| T-M5-06 | 2026-09-03 | Kennzahlenbericht über 200 Spieltage; der Test schlägt fehl, sobald eine Ressource unbegrenzt wächst | grün |

## Meilenstein M6 — Diplomatie und Nebel des Krieges ✅

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M6-01 | 2026-09-03 | Diplomatische Zustände: Kriegserklärung erst nach Vorlaufzeit, Ansehensverlust beim Überfall, Waffenstillstand läuft ab | grün |
| T-M6-02 | 2026-09-03 | Öffentliche Sicht — keine unerlaubten Felder, Rückgabetyp ist nicht `GameState` | grün |
| T-M6-03 | 2026-09-03 | Aufklärungswissen: letzter bekannter Stand mit Zeitstempel, überlebt Speichern und Laden | grün |

## Meilenstein M7 — Der Computergegner ✅

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M7-00 | 2026-09-03 | KI-Gedächtnis liegt im Spielzustand — nach Speichern und Laden identische Entscheidungen über 50 Ticks | grün |
| T-M7-01 | 2026-09-03 | KI-Grundgerüst und Erklärbarkeit: jeder KI-Befehl besteht `canApply`, die Ausgabe nennt Ziel, Nutzen und Alternative | grün |
| T-M7-02 | 2026-09-03 | Wirtschafts- und Bau-KI — reagiert auf Mangel, verschuldet sich nie, priorisiert Nahrung | grün |
| T-M7-02b | 2026-09-03 | Bedrohungskarte und Kräftevergleich; jeder Nutzenterm liefert 0…1000 | grün |
| T-M7-03 | 2026-09-03 | Militär-KI: verteidigt bei Bedrohung, wählt schwache wertvolle Ziele, bricht aussichtslose Angriffe ab | grün |
| T-M7-04 | 2026-09-03 | Diplomatie-KI — nimmt vorteilhaften Frieden an, kein Zweifrontenkrieg auf Stufe leicht | grün |
| T-M7-05 | 2026-09-03 | Schwierigkeitsgrade und Rechenbudget: KI ≤ 30 % der Tickzeit; Turnier über 50 Partien, schwer schlägt leicht in ≥ 70 % | grün |

## Meilenstein M8 — Speichern, Wiederholung, Dauerlauf ✅

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M8-00 | 2026-09-03 | ~~Speicher-Schnittstelle für Tauri, Node und Arbeitsspeicher — dieselbe Vertragstestreihe gegen alle drei~~ **Am 2026-09-06 auf `todo` zurückgestuft:** von den vier zugesagten Dateien existiert nur `StoragePort.ts`, `MemoryStorage` ist die einzige Umsetzung, und die „Vertragstestreihe gegen alle drei" ist ein einzelnes `it()`, das `MemoryStorage` gegen sich selbst prüft. **Abgelöst durch T-M14-08** (dauerhafter Browserspeicher); der Datei-Port folgt in M16 | ⚠ zurückgestuft |
| T-M8-01 | 2026-09-03 | Speichern und Laden mit Hash-Gleichheit; unbekannte Version wird abgelehnt; Kommandolog in eigener `.replay`-Datei | grün |
| T-M8-02 | 2026-09-03 | Kopfloser Läufer ausgebaut: Wiederholung aus Seed und Kommandolog reproduziert den Endzustand | grün |
| T-M8-03 | 2026-09-03 | Langlauf und Rechenbudget: Tick-Median < 0,5 ms, p99 < 2 ms, 1000 Spieltage ohne Fehler | grün |

## Meilenstein M10 — Oberfläche (läuft)

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M10-02 | 2026-09-03 | Simulations-Host im Worker: Tempo-Rastpunkte bis 100, Rückstand auf 2 Ticks gedeckelt, Vorspulen in Häppchen mit wirksamem Abbruch, nur die Spielersicht verlässt den Worker | grün |

**Stand 2026-09-03:** M0 bis M8 abgeschlossen, M10 begonnen (53 von 83 Aufgaben).
525 Tests grün, Kern-Abdeckung 95,9 %, `pnpm verify` vollständig grün.
Anforderungs-Tor: 57 von 74 Anforderungen durch Tests belegt.

**Bemerkenswert in M10:**
- `pnpm verify` war auf `main` rot: ein Typfehler aus M8 (`migrate.ts` setzte `hash` auf
  `undefined`, was `exactOptionalPropertyTypes` verbietet) hinter einer `as`-Zusicherung.
  Der Commit „lint cleanup after M8" hat offenbar nur den Linter laufen lassen. Behoben,
  und die Migrationskette ist jetzt getestet — vorher war sie es nicht, weil die
  Migrationstabelle leer ist und die Schleife nie lief.
- Der Abbruchknopf des Vorspulens wäre eine Attrappe geworden: JavaScript ist einfädig,
  und ein Vorspulen in einem Zug erreicht die Abbruchnachricht nie. Deshalb läuft es
  jetzt in Häppchen über die Ereignisschleife (`DECISIONS.md`, 2026-09-03).
- Der erste Datenschutztest war grün aus dem falschen Grund und dann rot aus dem
  falschen Grund: beide Nationen starten gleich, also hat eine fremde Provinz denselben
  Moral*wert* wie eine eigene. Der Test prüft jetzt die Struktur, nicht die Zahl.

**T-M10-01 vorgelegt (2026-09-03):** Drei Design-Richtungen als Mockup unter
`docs/design/ui-mockup.html`, Tokens und Kontrastwerte in `docs/design/tokens.md`.
Artifact: https://claude.ai/code/artifact/14e02471-d4ab-4267-833d-b6d98f8616e7 —
wartet auf Noahs Freigabe. Bis dahin bleiben T-M10-01b bis T-M11-04 gesperrt.

## Meilenstein M9 — Weltkarte (läuft)

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M10-01 | 2026-09-03 | **Freigabe erteilt:** Richtung A „Lagekarte" — drei Richtungen vorgelegt, Noah hat gewählt | — |
| T-M9-01 | 2026-09-03 | Geodaten von Natural Earth (1:50 Mio, gemeinfrei) geladen; umkehrbare Projektion, geodätische Entfernung getrennt von der Bilddarstellung, Vereinfachung auf Winkelgraden; Herkunft und Lizenz in `docs/ASSETS.md`, gegen das Quellenregister im Code geprüft | grün |
| T-M10-01b | 2026-09-03 | Freigegebene Optik als `tokens.ts` mit Kontrasttest (12 benannte Paare plus vollständige Kreuzprüfung Text × Grund); Lint-Regel verbietet Farbliterale außerhalb der Tokendatei, mit Verstoß-Fixture | grün |
| T-M9-00 | 2026-09-03 | Zuschnitt der Weltkarte auf Grundlage der 1:10-Mio-Daten: **237 Provinzen** aus 258 Staaten und 4596 Verwaltungseinheiten, 93 Gebiete mit Begründung ausgeschlossen, keine Einheit unverbucht; 91 kuratierte Seewege; **24 Startnationen, jede ein eigener Staat** mit mindestens drei Provinzen | grün |
| T-M9-02a | 2026-09-03 | Provinzgeometrien über eine gemeinsame Topologie verschmolzen (1279 Teile → 237 Provinzen), danach vereinfacht: 602 842 Stützpunkte auf 62 195 (−90 %). Gesamtfläche 133,9 Mio km² gegen erwartete 135 Mio ohne Antarktis. Sichtprüfung der gezeichneten Karte bestanden | grün |
| T-M9-02b | 2026-09-03 | Nachbarschaft aus geteilten Bögen: 445 Landgrenzen, symmetrisch, mit geodätischer Entfernung; Lesotho als einzige Enklave erkannt, 21 Inseln ohne Landnachbarn. Vereinfachung auf topologie-erhaltend umgestellt — die vorherige Ring-für-Ring-Variante hatte Nachbargrenzen auseinandergezogen | grün |
| T-M9-02c | 2026-09-03 | Seewege und `world.json`: 184 Küstenprovinzen bestimmt (nachdem 21 Landlöcher geschlossen wurden), 210 Seewege — 91 kuratiert für die Engstellen, 119 abgeleitet, keine über Land, keine Küstenprovinz ohne Anbindung. Datumsgrenze in Punkt-in-Fläche und Interpolation behandelt. **Die Weltkarte besteht den Kartenvalidator des Kerns fehlerfrei** | grün |
| T-M9-03 | 2026-09-03 | Anreicherung: Gelände aus der Lage, Bevölkerung aus Landeszahlen mit Dichtefaktoren, Vorkommen deterministisch aus der Provinzkennung. **Startwerte aller 24 Nationen innerhalb 14 % vom Median** (Grenze 15 %). Kartenbericht in `docs/reports/map.md` | grün |
| T-M9-04 | 2026-09-03 | Kernregeln auf der echten Weltkarte: **Tick-Median 2,76 ms, p99 5,1 ms** bei 237 Provinzen, 658 Kanten und 12 KI-Spielern (Budget 8 / 40 ms); 1000 Spieltage fehlerfrei, Ereignisprotokoll bleibt beschränkt. Messwerte in `docs/reports/worldmap-bench.json` | grün |

**Stand 2026-09-03:** M0 bis M9 abgeschlossen (62 von 83 Aufgaben), M10 begonnen.
691 Tests grün (plus 3 im Langlauf), Kern-Abdeckung 95,9 %, `pnpm verify` vollständig grün.
Die Weltkarte ist fertig: 237 Provinzen, 448 Landgrenzen, 210 Seewege, 24 spielbare
Nationen, Startwerte innerhalb 14 % vom Median.

## Meilenstein M10 — Oberfläche ✅

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M10-03a | 2026-09-03 | Kartenansicht: Trefferprüfung rein geometrisch (und damit prüfbar), Ausschnitt bleibt auf der Karte, Zoom auf den Zeiger, Ebenenreihenfolge nach D11 als Daten statt als Aufrufreihenfolge | grün |
| T-M10-03b | 2026-09-03 | Vier Kartenmodi; Unbekanntes wird als unbekannt gefärbt statt als Null. Bildratenbudget auf der echten Weltkarte gehalten — Zeitmessung in der langsamen Suite, weil sie neben 30 Testdateien die Maschinenlast misst | grün |
| T-M10-04 | 2026-09-03 | Kopfleiste mit Rohstoffen, Bilanz, Uhr, Tempo-Rastpunkten, Vorspulen und Kartenmodus; alle Zahlen über eine einzige Festkomma-Umrechnung. **Am 2026-09-06 auf `todo` zurückgestuft:** das Vorspulen-Menü ist unvollständig — der Knopf springt um genau einen Spieltag, `fastForwarding` ist ein Literal `false`, kein Ziel aus R-TIME-03 ist wählbar. **Geschlossen von T-M15-06** | ⚠ zurückgestuft |
| T-M10-05 | 2026-09-03 | Provinz- und Armeepanel; nicht ausführbare Aktionen ausgegraut **mit Grund**, Tooltip nennt Kosten und Dauer, Ankunft als Zeitpunkt statt als Countdown | grün |
| T-M10-06 | 2026-09-03 | Ereignisleiste: jede der 25 Ereignisarten als deutscher Satz mit Namen statt Kennungen; Klick springt zur Provinz | grün |
| T-M10-07a | 2026-09-03 | Partie erstellen — alle Angaben landen im Anfangszustand, gleicher Seed erzeugt einen bitgleichen Zustand, KI-Bonus offen ausgewiesen (0 %) | grün |
| T-M10-07b | 2026-09-03 | Speichern, Laden, Autospeichern; beschädigter oder fremder Stand wird **in Worten** abgelehnt | grün |
| T-M10-08 | 2026-09-03 | Für jeden der 16 Kommandofehler ein deutscher Satz, der sagt, was fehlt — „Es fehlen 400 Eisen", nicht „nicht genug Rohstoffe" | grün |
| T-M10-09 | 2026-09-03 | Einstellungen für Autospeichern, Ton, Tempogrenze, Schriftgröße und Debug; unsinnige Werte fallen auf die Vorgabe zurück, statt den Start zu verhindern. **Am 2026-09-06 auf `todo` zurückgestuft:** die Zusage „Einstellungen überleben den Neustart" war nie einlösbar — `parseSettings` hat außerhalb seines eigenen Tests keinen Aufrufer, und einen dauerhaften Speicher gab es nicht. **Geschlossen von T-M14-08**, das die Einstellungen im neuen Speicher mitnimmt | ⚠ zurückgestuft |
| T-M10-10 | 2026-09-03 | Debug-Ansicht mit Tick, Hash, KI-Zielen und Kommandolog; im Normalbetrieb unsichtbar | grün |
| T-M10-11 | 2026-09-03 | Kartenauswahl im Startdialog, mit Provinzzahl je Karte | grün |
| T-M10-12 | 2026-09-03 | Bedienung ohne Maus: jede Aktion über Tastatur, Fokusring sichtbar, Schriftgröße einstellbar, Dialoge fangen den Fokus und geben ihn zurück | grün |
| T-M11-04 | 2026-09-03 | Lokalisierung: ein Katalog, keine Anzeigetexte im Code; ein Test verlangt einen Satz für jeden Fehlercode, jede Ereignisart und jede Ressource | grün |

## Meilenstein M11 — Politur und Verpackung

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M11-01 | 2026-09-03 | 13 Icons als eingebettetes SVG in der Formensprache militärischer Lagekarten; nehmen die Textfarbe an, skalieren mit der Schriftgröße, für Vorleseprogramme unsichtbar solange sie Zierde sind | grün |
| T-M11-02 | 2026-09-03 | Erzeugte Klänge statt Aufnahmen (nichts geladen, nichts lizenziert); Ton abschaltbar, und oberhalb von 10 Spielstunden/s schweigen Töne und Animationen — sonst wären die Meldungen eine Störsirene | grün |
| T-M11-03 | 2026-09-03 | Tauri-Verpackung für Windows, Linux und macOS. **Kein Netzzugriff möglich:** keine http-, shell- oder updater-Berechtigung, `connect-src 'none'` in der Inhaltsrichtlinie — ein Guard prüft beides | grün |
| T-M11-04 | 2026-09-03 | (siehe M10) | grün |

## Meilenstein M12 — Balancing, Anleitung, Abnahme

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M12-00 | 2026-09-03 | Parameterlauf: jede beobachtete Konstante um ±25 % bewegt und ausgespielt. **4 von 14 Konstanten sind tragend, alle vier drehen sich um Moral und Ausdehnung**; `battleRate`, `minDamage` und `defenceCap` verschieben den Ausgang um 0,0 %. Der erste Lauf meldete, dass keine Zahl etwas ausmacht — zwischen den USA und Russland passiert in vierzig Tagen nichts. Ein Durchgang zählt jetzt Eroberungen und weigert sich, einen Befund zu melden, wenn keine stattfand | grün |
| T-M12-01 | 2026-09-03 | Alle 60 Konstanten in `BALANCING.md` mit Status, Herkunft und gemessenem Ausschlag; ein Test lässt jede neue Zahl ohne Eintrag durchfallen | grün |
| T-M12-02 | 2026-09-03 | Spielanleitung und Playtest-Vorlage; jede Frage trägt die Anforderungs-ID, die sie prüft | grün |
| T-M12-02b | 2026-09-03 | Einstiegshilfe für die erste Partie: fünf Schritte, die keine Eingabe blockieren, nur beim ersten Mal erscheinen und abschaltbar bleiben | grün |
| T-M12-03 | 2026-09-03 | **Abnahmelauf: 6 von 6 maschinellen Kriterien grün** (`pnpm acceptance`, `docs/reports/acceptance.md`). Offen bleibt AK-7, der Playtest durch Noah | grün |
| T-M12-04 | 2026-09-03 | **Der Abbruchknopf bekommt sein Wort zurück.** Auf dem Bildschirm stand unter „Im Bau" die Zeichenfolge `[province.cancelBuild]` — der Knopf wirkte, nur sein Name fehlte, weil `actions.ts` im falschen Namensraum nachschlug. Belegt am Code: `actions.ts` ruft heute `t('actions.cancelBuild', …)`; die Wächter `prose-in-code` und `text-keys` binden es. **Zeile am 2026-09-11 nachgetragen** — die Aufgabe stand seit dem 2026-09-03 auf `done`, hatte hier aber keinen Eintrag (Fund der Abschlussprüfung) | grün |
| T-M12-05 | 2026-09-03 | **Der Startdialog sagt, was Gewinnen heißt.** Punkte und Eroberung waren wählbar und nirgends erklärt, während das Feld darüber einen erklärenden Satz trug. Der Hinweis nennt jetzt die Schwelle aus `newGame.ts` und wechselt mit der Auswahl (`victoryPointsHint` / `victoryConquestHint`); `Dialogs.test.tsx` rendert beide Fassungen über den Helfer `zeige(victory)`. **Zeile am 2026-09-11 nachgetragen** | grün |
| T-M12-06 | 2026-09-03 | **Die zweite Partie beginnt wirklich.** Der Endedialog bot „Neue Partie" an, und der Klick führte ins Leere: er schloss den Dialog, öffnete keinen Startdialog und ließ den Spieler in der beendeten Partie stehen — nur Neuladen half. `setDialog('new')` hatte einen Zustandswert ohne Renderzweig. **Der lehrreichere Teil steht im Test selbst:** `Standings.test.tsx` rendert den Siegdialog dreimal und jedes Mal ohne `onNewGame`, und weil die Eigenschaft optional ist, lag der Knopf in keinem Testlauf im DOM; keiner der über dreißig App-Tests erreichte je eine entschiedene Partie. Geprüft wurde, *dass* ein Handler gerufen wird — nie, *was* danach auf dem Bildschirm steht. Der Test geht jetzt den Weg des Spielers: entschiedene Partie laden, klicken, hinsehen. **Zeile am 2026-09-11 nachgetragen** | grün |
| T-M12-08 | 2026-09-03 | **Die Kartenwahl wirkt.** Ein Blindschalter: „Kleine Welt (12)" gewählt, gestartet wurde die Weltkarte — und `docs/PLAYTEST.md` behauptete, der Befund sei geschlossen. `App.tsx` nimmt heute `mapById(options.mapId)` beim Start und `mapById(result.state.mapId)` beim Laden; `App.test.tsx` prüft es an den Provinznamen der beiden Karten. **Zeile am 2026-09-11 nachgetragen** | grün |

### Was der Abnahmelauf ans Licht gebracht hat

Drei Befunde, die ohne den Lauf niemand gesehen hätte:

1. **Sechs Anforderungen waren nie geprüft** — und zwei davon nicht, weil ein Test
   fehlte, sondern weil die Sache fehlte. `balance={{}}` stand in der Kopfleiste: die
   Wirtschaftsbilanz war eine leere Eigenschaft. Gebäude wurden auf der Karte nie
   gezeichnet. Beides ist jetzt gebaut, mit Test.
2. **Das Prüfskript sah `*.test.tsx` nicht an** — also genau die Dateien, in denen die
   Oberflächen-Anforderungen belegt werden.
3. **Die langsame Suite scheiterte an sich selbst.** Ein Test, der 467 Sekunden am Stück
   rechnet, beantwortet dem Testläufer nichts mehr; der hält den Arbeitsprozess für
   hängengeblieben und lässt den Lauf scheitern, obwohl jede Zusicherung darin
   durchgeht. Die Langläufe geben die Ereignisschleife jetzt regelmäßig frei. Dazu läuft
   die Suite seriell: ein Bildratenbudget neben einer vierminütigen Simulation misst die
   Maschinenlast, nicht den Code — genau daran war das Tickbudget mit 8,3 ms gegen 8 ms
   gescheitert und lief allein mit 2,2 ms durch.

**Stand 2026-09-03:** M0 bis M12 abgeschlossen (**82 von 83 Aufgaben**). 900 Tests grün
plus die langsame Suite, Kern-Abdeckung 95,9 %, Gesamtabdeckung 90,9 %, **alle 74
V1-Anforderungen durch einen Test belegt**. Offen ist allein T-M12-03 als Tor: der
Playtest durch Noah nach `docs/PLAYTEST.md` — ob das Spiel Spaß macht, findet kein
Skript heraus.

## Vor T-M12-03 — Rauchtest der Oberfläche (2026-09-03, Abend)

Bevor Noah die 45 Minuten investiert, einmal selbst durch die Playtest-Liste, soweit
eine Maschine das kann (Startdialog, Karte, Modi, Vorspulen, Speichern, Tastatur).
Sieben Befunde, sechs behoben — Einzelheiten in `PROBLEME.md`:

| Befund | Anforderung | Status |
|---|---|---|
| KI-Befehle wurden je Tick eines Vorspulens wiederholt angewandt: 1 × „Bau begonnen", 23 × „Befehl abgelehnt" | R-AI-01 | behoben (`advance.ts`, dieselbe Schleife wie der kopflose Läufer) |
| Protokoll zeigte Bauten und Ablehnungen fremder Mächte | R-DIP-04 | behoben (`eventsFor`) |
| Rohtexte im Protokoll: `{{reason}}`, `barracks`, `p2`, Marsch „nach" dem Startort, Handel und Kriegserklärung mit falschen Platzhaltern | R-UI-07 | behoben, Katalog um Gebäude, Einheiten und Ablehnungsgründe ergänzt, Vollständigkeit getestet |
| Wirtschaftsübersicht: zwei von fünf Spalten hinter einem Rollbalken | R-ECON-06 | behoben (Seitenleiste 380px) |
| „1 Tage" im Kosten-Tooltip | R-UI-05 | behoben |
| Escape im Startdialog ließ einen leeren Bildschirm ohne Rückweg | R-UI-03 | behoben |
| **Die Weltkarte ist wirtschaftlich vom Regelwerk abgekoppelt** — Vorkommen und Bevölkerung um Faktor 600–1000 über der Skala, auf der die Regeln abgestimmt wurden; eine Kaserne kostet vier Spielminuten Einkommen, Italien hat kein Material | R-ECON-01, R-MAP-04 | **offen — Entscheidung Noah**, blockiert die Aussagekraft des Playtests |

**Stand 2026-09-03 (Abend):** 82 von 83 Aufgaben. 915 Tests grün, Kern-Abdeckung
95,9 %, Gesamtabdeckung 90,9 %, `pnpm verify` vollständig grün. Der Playtest sollte
erst nach der Entscheidung zur Wirtschaftsskala stattfinden — vorher misst er die
Wirtschaft nicht.

## Nachtrag zu T-M9-03 — Die Weltkarte auf der Skala der Regeln (2026-09-03, Nacht)

Noahs Entscheidung zum siebten Befund: der Weg mit den wenigsten Spielproblemen. Das ist
die Karte auf die Regelskala zu bringen, nicht die Regeln auf die Karte
(`DECISIONS.md`, 2026-09-03).

| Schritt | Ergebnis | verify |
|---|---|---|
| Generator ohne doppelte Festkomma-Umrechnung | `world.json` neu gebaut; `world-shapes.json` bitgleich; Startwerte Median 41.262, größte Abweichung 13 % | grün |
| Bevölkerung auf die Referenz des Kerns (ein Fünftel) | Median 443.000 je Provinz statt 2,4 Mio — der Bevölkerungsfaktor hing zuvor bei jeder Provinz am Deckel, Geld war zwanzigfach zu reichlich | grün |
| Mindestvorkommen Holz und Erz je Startnation (`ensureStartingBasics`) | Italien kann bauen; jede der 24 Mächte produziert Nahrung, Material, Erz und Geld ab Tag 1 | grün |
| Skalentest `economy-scale.test.ts` | Kaserne kostet jede Macht 0,5–30 Tage Einkommen, die typische Macht liegt binnen Faktor 4 an der Referenzkarte; Bevölkerung auf der Referenz | grün |
| Bevölkerungsanzeige | zeigte die Testkarte als „900" statt „900 Tsd" — Einheit des Kerns sind Festkomma-Tausender | grün |

Gemessen: Deutschland Kaserne 2,4 Tage Material / 2,3 Tage Geld (vorher 4 Spielminuten),
Italien 3,5 / 3,1 (vorher nie), Referenz Nordland 8,8 / 4,1. Der Parameterlauf ist auf
der neuen Karte wiederholt — Ergebnis in `BALANCING.md` und `docs/reports/balance-sweep.md`.

## Nachtrag zu T-M10-05/06 — Die Befehle erreichen die Oberfläche (2026-09-03, Nacht)

Beim zweiten Gang durch die Playtest-Liste: Die Provinzleiste bot nur „Kaserne bauen",
die Armeeleiste nichts. Alles andere war im Kern fertig und nirgends anklickbar
(`PROBLEME.md`). Angebunden, mit Tests:

| Schritt | Ergebnis | verify |
|---|---|---|
| `game/actions.ts` | Jeder Befehl als Daten: 7 Gebäude, 10 Einheiten, Hauptstadt, 7 Armeebefehle, 8 diplomatische Handlungen, Markt — Grund aus `canApply`, Kosten und Dauer im Tooltip | grün |
| `game/rejections.ts` | Ablehnungen in Worten: Fehlbetrag gerechnet („Es fehlt an Rohstoffen: 333 Material."), Gebäude bei Namen, Sperren in Tagen | grün |
| Zielwahl | Marsch/Beschuss: Klick auf die Karte oder Liste im Panel, Ankunftszeit vor der Bestätigung, Escape bricht ab | grün |
| Diplomatie (D), Markt (H), Provinzliste | Aus Kopfleiste und Tastatur; Gegenwert vor dem Tausch; Provinzwahl ohne Maus | grün |
| Ende-zu-Ende | Bauen → Ausheben → Armee wählen → Marsch mit Ankunft im Protokoll; Kriegserklärung mit Wirkungstag; Tausch; Abbruch | grün |

## M13 — Eine Oberfläche, die man ansieht

| Aufgabe | Datum | Was | Prüfung |
|---|---|---|---|
| T-M13-01 | 2026-09-04 | Symbole erreichen die Oberfläche: Satz auf die ausgelieferten Regeln gebracht (`shipyard`, `motorized`, `heavy_tank`, `rocket_artillery`, `destroyer`, `transport` hatten keines; `cavalry`, `submarine`, `mine` gab es gar nicht), sieben Rohstoffsymbole ergänzt, `IconRow` als Bauteil, Vorkommen und Gebäude als Symbolzeile statt als Satz, Symbol auf jedem Bau- und Aushebeknopf, Kopfleiste mit Rohstoffzeichen | 963 Tests grün, `pnpm verify` grün, im laufenden Spiel gegengeprüft |
| T-M13-02 | 2026-09-04 | Ton und Einstiegshilfe eingeschaltet: `cueForEvents` wählt je Tick den dringlichsten Ton (Krieg vor Eroberung vor Kampf vor Mangel vor Fertigstellung), Ton hängt am eigenen Protokollanteil, Toneinstellung wirkt; `Tutorial.tsx` zeigt die fünf Schritte aus M12 neben dem Spiel, ohne Eingaben abzufangen, und merkt sich das Abschalten | 977 Tests grün, `pnpm verify` grün, im laufenden Spiel gegengeprüft |
| T-M13-03 | 2026-09-04 | Automatisches Speichern verdrahtet: Kern (Rotation, Zwei-Uhren-Regel) und Einstellung waren seit M8/M10 da, aber unverbunden — die Einstellung war Zierde. Uhr beim Partiestart und beim Laden gesetzt, sonst greift die Echtzeitbedingung sofort; Schreibsperre gegen doppelte Läufe | 980 Tests grün, `pnpm verify` grün |
| T-M13-04 | 2026-09-04 | Erreichbarkeits-Guard: verfolgt die Importkette ab `main.tsx` und meldet jedes Modul, das die laufende Anwendung nicht erreicht. Drei begründete Ausnahmen (Paketeinstieg, Worker-Hülle mit Maschine). Beim Bau selbst gefunden: eine zeilengebundene Regex übersah mehrzeilige Importlisten und erklärte `game/actions.ts` für tot | 987 Tests grün, `pnpm verify` grün |
| T-M13-05 | 2026-09-04 | Sicht erweitert um Bauschlange, Aushebeschlange, Moralziel und sichtbare Kämpfe — jeweils nur für eigene Provinzen bzw. gesehene Orte und nur, wenn die Sicht mit Regeln angefordert wird (die KI zahlt nichts für Anzeigen, die sie nicht liest). Truppenstärke je Provinz bewusst nicht im Kern: `view.armies` ist bereits gefiltert, die Summe ist Darstellung | 994 Tests grün, `pnpm verify` grün |
| T-M13-06 | 2026-09-04 | `Meter` als Bauteil: `role="meter"` mit Zahlen und Textfassung, Füllung aus Token statt Literal, Trendpfeil erst ab einem halben Prozent der Skala. Moral erscheint als Balken mit Pfeil statt als Prozentzahl. Zwei Befunde beim Bau: ein Standardmaßstab für `trendOf` wäre für die Festkomma- und die Prozentskala unterschiedlich falsch gewesen (jetzt Pflichtangabe); die leere Balkenspur hatte 1,27:1 gegen das Panel und bekam einen Umriss | 1008 Tests grün, `pnpm verify` grün |
| T-M13-07 | 2026-09-04 | Fortschritt sichtbar: Bauvorhaben und Aushebungen als Balken mit Restzeit statt als bloße Anzahl, Marsch als Balken, Siegziel als Anteilsbalken in der Kopfleiste. Korrektur an T-M13-05: der Startzeitpunkt gehört doch in die Sicht — ein Balken braucht beide Enden; dazu `departureTick` je eigener Armee und die Punktschwelle in `victory` | 1021 Tests grün, `pnpm verify` grün |
| T-M13-08 | 2026-09-04 | Die Karte beschriftet sich und erklärt ihre Farben: `labels.ts` als reine Funktion mit übergebener Textbreite (jsdom kann keine Schrift messen), Namen erst unterhalb der Zoomschwelle, keiner breiter als seine Provinz, keine Überlappung; heller Saum für Lesbarkeit auf jeder Füllung. `Legend` zeigt endlich, was `legendFor` seit M10 wusste. Schwelle nach der Sichtprüfung von 1,2 auf 2,0 angehoben — bei Spielstart (1,6) war die Karte sonst namenlos | 1030 Tests grün, Beschriftungsbudget gemessen, `pnpm verify` grün |
| T-M13-09 | 2026-09-04 | Karte trägt Hauptstadt (Stern über der Provinz), laufende Kämpfe (Ring mit Säbeln, aus der Sicht statt aus einer Fahne, die nie jemand setzte), Armeekasten mit dem Zeichen der stärksten Gattung, Marschweg der gewählten Armee. Karte und Panels zeichnen dieselben Pfade — `ICON_PATHS` über `Path2D` | 1037 Tests grün, `pnpm verify` grün |
| T-M13-10 | 2026-09-04 | Kartenmodus „Truppenstärke" ersetzt „Bedrohung": die alte Größe hat nie jemand berechnet, der Modus färbte alle 237 Provinzen gleich grau. Stärke wird als reine Funktion über die (bereits nach Sichtbarkeit gefilterten) Armeen der Sicht summiert; Test hält fest, dass **jeder** angebotene Modus aus geführten Daten färbt | 1041 Tests grün, `pnpm verify` grün |
| T-M13-11 | 2026-09-04 | Jedes Ding erklärt sich, wo es steht: `Explain` als Fragezeichen (Tastatur und Zeiger, kein `title` — das wäre für die Tastatur unsichtbar), Erklärungen für 7 Gebäude, 10 Einheiten, 7 Rohstoffe, 4 Kartenmodi, 5 Geländearten, 6 Beziehungszustände. Tests halten Vollständigkeit gegen die Regeldateien, die Zwei-Satz-Grenze und das Verbot fest, Zahlen aus den Regeln in Texten zu wiederholen | 1050 Tests grün, `pnpm verify` grün |
| T-M13-12 | 2026-09-04 | Lageübersicht (Taste `L` und Knopf): jede bekannte Macht mit Punktebalken gegen den Führenden, Verhältnis und **gesehener** Truppenstärke — was hinter dem Nebel steht, steht nicht in der Tabelle. Abschlussfenster, wenn die Partie entschieden ist: es sagt den Ausgang, fasst in drei Zahlen zusammen, hält die Uhr an und lässt sich schließen | 1060 Tests grün, `pnpm verify` grün |
| T-M13-13 | 2026-09-04 | Meldungen und Protokollfilter: `alertsFor` leitet Kampf im eigenen Gebiet, verlorene Hauptstadt, Mangel und Aufstandsgefahr aus der **Lage** ab statt aus dem Protokoll — dieselbe Lage ergibt dieselbe Kennung, also nicht in jedem Tick eine neue Meldung. Klick führt zum Ort. Das Ereignisprotokoll bekommt die Filter, die R-GAME-06 seit M5 verlangt (alles/Kämpfe/Aufbau/Verträge). Beim Bau gefunden: der Filterknopf „Diplomatie" wäre vom Kopfleistenknopf nicht zu unterscheiden gewesen — eigene Wörter | 1073 Tests grün, `pnpm verify` grün |
| T-M13-14 | 2026-09-04 | Kopfleiste: je Rohstoff Symbol, Bestand, Tagesbilanz und — nur bei schrumpfendem Vorrat — die Reichweite in Tagen; unter drei Tagen als Mangel gekennzeichnet. Eine Reichweite bei wachsendem Vorrat wäre Rauschen, das sich als Auskunft ausgibt | 1077 Tests grün, `pnpm verify` grün |
| T-M13-15 | 2026-09-04 | Provinzpanel aufgeräumt und **gemessen**: dasselbe Panel derselben Provinz kam von 864 auf 587 sichtbare Textzeichen (−32 %). Jeder Absagegrund erscheint höchstens einmal je Gruppe (statt viermal „Dafür fehlt das Gebäude: Fabrik."), die Gebäudeüberschrift entfällt, wenn nichts steht. Kein Verlust an Auskunft: die nicht gezeigten Gründe bleiben im Tooltip und in der Textfassung. Beim Messen gefunden: `textContent` zählt auch Textfassungen mit und hätte den Fortschritt als −20 statt −277 Zeichen ausgewiesen | 1080 Tests grün, `pnpm verify` grün |
| T-M13-16 | 2026-09-04 | Bewegung, sparsam: `motion.ts` mit dem Puls als reiner Funktion der Zeit (Kosinus statt Sägezahn — ein Ring, der zurückspringt, zuckt statt zu atmen). Der Kampfring atmet, eine neue Meldung blendet einmal auf, ein fertiger Bau leuchtet einmal. Die Animationsschleife läuft nur, solange irgendwo gekämpft wird, und hört bei `prefers-reduced-motion` und ab zehn Spielstunden je Sekunde ganz auf | 1086 Tests grün, `pnpm verify` grün |
| T-M13-17 | 2026-09-04 | Sichtprüfung im laufenden Programm und Dokumente nachgezogen: `docs/reports/ui-expansion.md` (Kennzahlen vorher/nachher), Anleitung um Anzeigen, Lageübersicht, Erklärungen, Filter und den neuen Kartenmodus erweitert, 19 Playtest-Fragen zu den neuen Anforderungen, README und PROBLEME nachgeführt. **Vier Befunde, die nur am Bildschirm auftraten** (namenlose Karte beim Start, Erklärzeichen unter statt neben den Knöpfen, doppelter Name in der Lagetabelle, unsichtbare Balkenspur) — alle behoben | 1089 Tests grün, `pnpm verify` grün, `pnpm test:slow` grün (Tickbudget gehalten), 82 von 82 Anforderungen testbelegt |

## M14 — Der Stand wird wieder wahr

| Aufgabe | Datum | Was | Prüfung |
|---|---|---|---|
| T-M14-01 | 2026-09-06 | Meilensteinachse im `scope`-Block: neues Fach `later` (ID → „Meilenstein — Begründung"), V1-Pflicht ist `ids − v2_only − later`, eine ID ohne Fach bleibt V1 — die sichere Richtung des Fehlers. Das Tor meldet jetzt **eine** Zeile `V1 offen: N` statt zweier Fälle, dazu je Meilenstein „x von y belegt (Fortschritt, kein Tor)"; ein `later`-Eintrag ohne Begründung oder mit einem Meilenstein, den `tasks.yaml` nicht deklariert, bricht mit Exit 1. `acceptance.mjs` liest diese eine Zeile, AK-2 ist darauf präzisiert. R-NEWS-04 (Weltgeschehen) als Ersatz der Zeitung aufgenommen und T-M15-09 zugeordnet; die Verweise in 2.15 auf die neue Achse gezogen (das früher als „M15" Verschobene ist M18, Tauri ist M16) | **Das Tor ist wieder grün: 101 Anforderungen, 82 V1-pflichtig, `V1 offen: 0`, Exit 0** (vorher 82/100, Exit 1). 1098 Tests grün, Kern 96,1 %, gesamt 93,5 %, `pnpm verify` grün. Beim Lauf gefunden und behoben: ein Ende-zu-Ende-Test riss unter der Abdeckungsmessung sein Zeitlimit (`PROBLEME.md`) |
| T-M14-02 | 2026-09-06 | Der Plan-Wächter liest endlich `files:` und `tests:`. Neu `test/plan-paths.ts` mit `missingPaths` und `reopenedWithoutReason` als **reine Funktionen** — eine Prüfung, die nur die echte Datei liest, wird grün, sobald jemand die Pfade repariert, und beweist danach nichts mehr; jetzt ist auch der Fehlerfall selbst geprüft (toter Pfad, Datei statt Verzeichnis, offene Aufgabe ohne Begründung). **73 Pfadangaben nachgezogen** — durchweg Umbenennungen, die beim Bauen geschahen und nie zurückgeschrieben wurden (`load.ts`→`migrate.ts`, `de.json`→`de.ts`, sechs UI-Bauteile→`Panels.tsx`/`Dialogs.tsx`, drei E2E-Pfade auf das, was tatsächlich belegt). **Zwei Pfade bewusst gestrichen statt umgebogen** (`combat-conservation.test.ts`, `bombard.test.ts`): die Tests wurden nie geschrieben, T-M14-06 und T-M14-07 schreiben sie — ein toter Pfad in einer erledigten Aufgabe wäre hier die eigentliche Lüge. Drei Aufgaben tragen jetzt ein Feld `reopened` mit der Aufgabe, die sie schließt; **T-M10-09 hat dabei seinen Besitzer bekommen** (T-M14-08 nimmt die Einstellungen in den neuen Speicher mit) | **0 tote Pfade (vorher 70)**, 18 Wächtertests grün, `pnpm verify` grün, `git status` sauber |
| T-M14-02b | 2026-09-06 | Das Tor zählt je Akzeptanzkriterium: `describe('R-XX-nn/AK1 …')` belegt AK1, und eine Anforderung gilt erst als belegt, wenn **jedes** ihrer Kriterien einen Test hat. Ein Kriteriumsblock belegt zugleich die ID auf der alten Ebene — der Umstieg ist ein Übergang, kein Bruch. **Übergangsliste** im `scope`-Block: 26 Anforderungen bleiben vorerst auf Namensebene gebucht (die übrigen 56 V1-IDs führen gar keine Kriterien); ein Test friert sie bei 26 ein, sie darf nur schrumpfen. Dazu die **Gegenrichtung**, auf der der Nachtrag 2.15 durchrutschte: jede Anforderung eines *geplanten* Meilensteins braucht eine Aufgabe, jede noch zu bauende zusätzlich einen Entwurfstext — selbstpflegend, weil „geplant" heißt „hat Aufgaben", also greift die Regel bei M17 von selbst, sobald ihn jemand plant | 42 Wächtertests grün; beim Bau gefunden: R-NEWS-04 hatte keinen Entwurf (jetzt D19.8) und R-NEWS-01…03 keine Aufgabe, die sie streicht (jetzt T-M15-09); `pnpm verify` grün |
| T-M14-03 | 2026-09-06 | Sechs Zusagen zurückgenommen statt als Aufgabe weitergeschleppt (Noahs Entscheidung 1), jede vorher am Code geprüft: **Gebäudeunterhalt** (`BuildingRule` kennt kein `upkeep`, die Phase sammelt nur über `armyOrder`), **Einheitenmoral** (ein Verband ist `{unitKey, hpTotal}`), **Garnison gegen Aufstand und Aufständischen-Armee** (`settleMorale` liest `draft.armies` nirgends; `Army.owner: PlayerId` lässt eine besitzerlose Armee nicht zu), **E2E-Stufe** (kein `e2e/`-Verzeichnis, Playwright null Treffer im Lockfile), **Zeitsieg als Auswahl** (`newGame.ts` setzt in beiden Zweigen `dayLimit: null`), **Fluss- und Meerengenmalus** (`crossingFactor` war toter Code — einziger Treffer im Repo war seine eigene Definition, jetzt gelöscht). Dazu **C-02 präzisiert**: die V1 wird im Browser mit IndexedDB abgenommen, Tauri wird M16 mit eigenem AK-8. Neu `test/withdrawals.test.ts` — es prüft die *aktive* Zusage, nicht das Wort, sonst zwänge es dazu, die Rücknahme unbegründet zu lassen | 10 Rücknahmetests grün; der Zeitsieg-Zweig wird jetzt von genau einem Test ausgeführt (vorher von keinem); die **acht kleineren Befunde ohne Aufgabe** stehen mit Meilenstein in PROBLEME.md; `pnpm verify` grün, Kern 96,6 % |
| T-M14-04 | 2026-09-06 | **Eine Spielschleife für alle.** `advanceTicks` in `packages/ai/src/loop.ts` — dem einzigen Paket, das Kern und KI kennen darf; Desktop, Parameterlauf, Turnier und Aufzeichnung rufen jetzt dieselbe. Vorher waren es vier Fassungen, und die des Parameterlaufs fragte die KI einmal je **Spieltag**: weil `shouldThinkThisTick` die Denkzeit über `tick % aiCount` verteilt und `tick` dort stets ein Vielfaches von 24 war, **dachte bei sechs Mächten nur die erste** — jede Balancezahl beschrieb eine Welt, in der fünf von sechs stillstehen. Die Schleife gibt die Ereignisse **aller** Ticks zurück (Voraussetzung für T-M14-05, dessen Ringpuffer-Zählung 3 statt 101 meldete) und die angewandten Befehle je Tick, womit auch die Aufzeichnung ohne eigene Schleife auskommt. Neuer Wächter `test/guards/single-loop.test.ts`: wer `runAi` und `runTicks` in derselben Datei ruft, schreibt eine zweite — mit einer Zusicherung, dass die durchsuchte Menge nicht leer ist (die Lehre aus N1) | 6 Schleifentests grün, darunter **R-AI-07/AK1** (Speichern-Laden-Weiterspielen mit KI), das Befund N2 zufolge nie geprüft wurde; `pnpm verify` grün |
| T-M14-05 | 2026-09-06 | **Die Messgeräte messen die Partie, nicht ihren Schwanz.** (a) Eroberungen kommen aus dem Ereignisstrom des Laufs statt aus `state.eventLog` — einem Ringpuffer von 500 Einträgen, der bei ~12.300 Ereignissen je Partie die letzten Spieltage abdeckte; der Bericht meldete 3 statt 49, und die eingebaute Warnung „keine Eroberung heißt, der Lauf hat nichts gemessen" konnte nie auslösen. (b) `economy` hieß „Total resources produced" und summierte **Endbestände** — jetzt `stockpile`, benannt nach dem, was es misst. (c) **Die Rauschgrenze wird erstmals gemessen:** Startzahlen von 2 auf 12, ein Grundlauf ohne Regeländerung ermittelt die Streuung der Zielgröße, und der Bericht nennt sie samt der Schwelle, ab der ein Ausschlag etwas heißt — bisher lag der größte gemeldete Ausschlag *unter* der eigenen Rauschgrenze. (d) BALANCING.md behauptete „ohne dass eine Stufe je alle Partien gewinnt", während der zitierte Bericht 100 % meldet — richtiggestellt, die Obergrenze folgt in T-M15-05 (sie wäre heute sofort rot und blockierte die Abnahme) | 10 Werkzeugtests grün; **neuer Befund dokumentiert:** das Tickbudget aus R-ARCH-06/AK1 ist auf der Weltkarte um Faktor 4,9 gerissen (2,463 ms gegen 0,5 ms gefordert) und wurde von einem 16-fach großzügigen Budget verdeckt — Entscheidung für Noah in PROBLEME.md; `pnpm verify` grün |
| T-M14-06 | 2026-09-06 | **Der Stapel-Deckel wirkt als Grenzbeitrag, nicht als Faktor auf die ganze Armee.** Neu `effectiveUnits` — das Integral über die belegte Kurve: `n` bis 20, dann `n − (n−20)²/60`, Plateau bei 35 ab 50. `sideAttackValue` skaliert die Armee damit einmal (eine Division statt zweier gerundeter Schritte, sonst wäre dieselbe Armee mit 60 Einheiten um eine Festkomma-Einheit schwächer als mit 50). Vorher gemessen: 20 → 1501, 25 → 1563 (Höhepunkt), 30 → 1502, 40 → 1000, 49 → 121, **ab 50 → exakt 0** bei unverändertem eigenem Verlust — jede Einheit jenseits der 25. machte eine Armee schwächer, und die KI legt Verbände zusammen. **Der alte Test hielt genau das fest** („lässt 60 Einheiten nicht mehr ausrichten als 50" — grün, weil beide null ausrichteten); er ist umgedreht und um eine Monotonie-Zusicherung über die ganze Kurve ergänzt | 19 Kampftests grün; **der Golden-Master blieb gültig, weil beide Fassungen bis 20 Einheiten identisch sind**; D6.5 und BALANCING.md nennen Grenz- und Gesamtbeitrag jetzt getrennt; `pnpm verify` grün |
| T-M14-07 | 2026-09-06 | **Drei Kampfregeln, die nicht taten, was der Entwurf sagt.** (a) **Die Festung gehört ihrem Eigentümer** — sie schützte jede Seite in der Provinz, den stürmenden Angreifer eingeschlossen, und weil der Bonus gedeckelt ist, verschluckte sie dabei sogar den Eingrabungsvorteil des Verteidigers; das Gelände bleibt geteilt, ein Berg gehört niemandem. Der bestehende Test war grün, weil die Festung in einer **herrenlosen** Provinz stand. (b) **Die Rückzugssperre wirkt im Nahkampf** — `cannotAttackUntil` wurde gesetzt und nur vom Beschuss gelesen; eine Armee zog sich zurück und schlug im selben Tick wieder zu. Wer unter Sperre steht, greift nicht an, wird aber getroffen. (c) **Beschuss trifft nur Kriegsgegner** — gefiltert wurde auf „fremde Armee", also traf es Verbündete und Neutrale, die zufällig in der Zielprovinz standen; die Prüfung davor sieht nur, wem die *Provinz* gehört. (d) **Keine leeren Armee-Hüllen mehr** nach Beschuss (Befund 53) — eine Hülle ohne Einheiten hält ihren Besitzer am Leben, woran T-M14-14 gehangen hätte. Neu `bombard.test.ts` — die Datei stand seit M4 als Beleg im Plan und existierte nicht | 40 Kampftests grün; **Befund 52 (Beschuss in Phase 1 statt 8) bewusst nicht hier**: der Umbau ändert die Reihenfolge und damit jeden Golden-Master, ohne heutigen Gewinn — er gehört zu T-M15-07, wo die Feuerautomatik ihn braucht (begründet in PROBLEME.md) |
| T-M14-08 | 2026-09-06 | **Ein Speicher, der das Schließen des Fensters überlebt** — der schwerste Blocker. `IndexedDbStorage` (nicht `localStorage`: ein Weltkarten-Stand ist über ein Megabyte, und synchron hielte er die Oberfläche an), `createStorage` als *eine* prüfbare Stelle für die Wahl, in `App.tsx` verdrahtet. Fällt die Anwendung mangels IndexedDB zurück, **erfährt es der Spieler** als Meldung — der stille Rückfall auf den Arbeitsspeicher war der Zustand, den diese Aufgabe behebt. `storagePortContract` läuft gegen **beide** Umsetzungen und prüft, was nur ein echter Speicher hat (Namen mit Umlauten, 200-kB-Inhalte, zweimal derselbe Name). Dazu **T-M10-09 geschlossen**: `parseSettings` hatte seit M10 keinen Aufrufer — Ton, Schriftgröße und Tempogrenze setzten sich bei jedem Start zurück; jetzt geladen und zurückgeschrieben, mit injizierbarem Speicher statt Umgebungsabhängigkeit | 21 Speichertests grün; neuer Wächter `persistence-contract` (jede `implements StoragePort`-Klasse braucht den Vertrag, und die Menge darf nicht leer sein); **`main.tsx` ist nicht mehr von der Abdeckung ausgenommen** (Befund N5 — ausgerechnet die Datei, die entscheidet, was ausgeliefert wird); `pnpm verify` grün |
| T-M14-09 | 2026-09-06 | **Das Spiel bekommt seine Schrift.** `git ls-files` fand null Asset-Dateien, es gab kein einziges `@font-face` — ausgeliefert wurde der Rückfall auf `system-ui`, während `tokens.ts` IBM Plex verlangt und `ASSETS.md` behauptete, die Schriften seien eingebettet. **Drei Wächter waren grün, weil sie eine leere Menge prüften.** Noahs Entscheidung: einbetten. Vier Schnitte (Sans 400/600, Sans Condensed 600, Mono 400) plus OFL 1.1 von github.com/IBM/plex, Stand `v6.4.0`, über neues `scripts/fetch-fonts.mjs` bezogen — das Skript nennt Quelle, Lizenz und je Datei eine SHA-256-Summe und prüft mit `--check` die eingecheckten Dateien nach, ohne etwas zu laden. `app.css` bindet sie mit **relativer** `url()` ein; eine `https:`-Quelle wäre ein Netzaufruf zwischen dem Spieler und seinem eigenen Spiel (R-FREE-04). `OFL.txt` steht in `.gitattributes` auf `-text`, sonst hätte die Zeilenendennormalisierung beim nächsten Auschecken die Prüfsumme zerstört | **208,1 kB für vier Dateien** (Grenze 400 kB). Der Wächter zählt jetzt die getrackten Dateien, verlangt je Familie aus `TYPE` ein `@font-face` auf eine davon — und **beweist an einer leeren Dateiliste, dass er fallen kann**, dieselbe Zwei-Richtungs-Probe wie die künstliche Waise in `ui-reachability`. Das Design-Tor vergleicht ab jetzt auch die Schriftfamilien von Mockup und Code; 16 Tests grün |
| T-M14-10 | 2026-09-06 | **Was passiert, wenn es schiefgeht.** (a) **ErrorBoundary** — im ganzen Baum gab es keine; ein Renderfehler ergab eine weiße Fläche ohne Hinweis, und für den Playtest hieße das, ein Absturz hinterlässt keinerlei Information. Jetzt eine Meldung mit dem Fehlertext zum Weitergeben, dem Weg zurück und der Zusicherung, dass nichts gesendet wird (Z3). (b) **Die eigene Niederlage** — der Abschlussdialog hing an `victory.winner`, den der Kern erst bei genau *einer* lebenden Macht setzt; schied der Mensch als einer von acht aus, tickte das Spiel weiter, ohne ein Wort. `PublicView.self` führt jetzt `alive`, und der Dialog sagt es. (c) **Die zweite Partie** — der Startdialog öffnete sich nur beim Programmstart; jetzt führt der Abschlussdialog zurück | 4 Fehlergrenzen-Tests, 13 Sichttests grün; `pnpm verify` grün |
| T-M14-11 | 2026-09-06 | **Die KI findet ihr Ziel** — Ursache D, und die Aufgabe mit der größten Wirkung je Zeile. (a) Neu `landReachable`: `hopDistance` zählt Seewege mit, was als Maß für „wie weit ist das weg" richtig und als Antwort auf „kann ich dort hinmarschieren" falsch ist — die KI benutzte es für beides, und **3046 von 4464 Marschbefehlen endeten mit `NO_PATH`**, in jedem Tick neu gefasst. `rateProvinces` filtert jetzt darauf. (b) Neu `opponentsNear`: die Gegner kommen per Breitensuche über Landgrenzen statt aus der Kartenreihenfolge. In der ausgelieferten Voreinstellung waren das bisher Russland, China und Indien — **keiner mit Landweg zum Spieler, 0 Kriegserklärungen in 1000 Spieltagen**; fast jede Messung „es passiert nichts" im Audit hängt daran | **Gemessen an der Standardpartie (60 Spieltage): Gegner jetzt Kanada, Mexiko, Brasilien, Argentinien; 6 Kriegserklärungen statt 0; abgelehnte KI-Befehle 6,3 % statt 57 %.** 36 KI-Tests grün, `pnpm verify` grün |
| T-M14-12 | 2026-09-06 | **Die KI benutzt, was sie hat.** (a) **Truppenmischung** — `economy.ts` wählte `tank` oder `infantry`, zwei von zehn Arten; neu `nextUnitFor` mit grobem Zielverhältnis (50/30/20), gemessen am eigenen Bestand. Die **Artillerie** ist dabei Vorbedingung für R-BAT-08: eine Feuerautomatik ohne Fernwaffen wäre gebaut, grün getestet und wirkungslos. (b) **`SET_CAPITAL`** — von keiner Zeile erzeugt; wer die Hauptstadt verlor, hatte für den Rest der Partie keine, und die Entfernungsstrafe traf **jede** Provinz voll. (c) **`MERGE_ARMIES`** — gemessen 127 Armeen bei einer Macht, deren stärkste 2 % der Kraft hielt; jetzt legt sie zusammen, gedeckelt bei zwanzig Einheiten, weil darüber jede weitere weniger beiträgt. (d) **`PublicView.incomingOffers`** — die KI warf `acceptPeace` ins Blaue (99 % abgelehnt), weil die Sicht die Angebote an sie gar nicht führte; zwischen zwei KI-Mächten konnte ein Krieg dadurch strukturell fast nie enden. Der alte Test hielt genau dieses blinde Verhalten fest | 50 KI-Tests grün, `pnpm verify` grün. **Offen aus dieser Aufgabe:** der `tradeCommands`-Auslöser (die KI handelt erst bei eingetretenem Mangel) — siehe PROBLEME.md |
| T-M14-13 | 2026-09-06 | **Was der Kern kann, erreicht der Spieler** — das Muster, das dieses Projekt viermal getroffen hat, mit einem Wächter dagegen. Neu `test/guards/ui-command-coverage.test.ts`: er prüft **jeden im Kern registrierten Kommandotyp** und jede Variante der Haltungen gegen das, was die Oberfläche erzeugt — der bestehende `ui-reachability` prüft nur *Module*, und `game/actions.ts` war erreichbar, während die Hälfte der Befehle darin fehlte. Er fand sofort **`CANCEL_BUILD`** (seit M3 gebaut, unerreichbar, weil die Sicht die `orderId` gar nicht führte) und **`retreat`** (die einzige Kampfhandlung, die die KI befehlen konnte und der Spieler nicht). Beide geschlossen. Dazu der **Kampfbericht** (R-BAT-07, seit M4 im Kern): `losses` ist ein Objekt und fiel aus der Textbildung still heraus, die nur flache Werte übernimmt — der Spieler erfuhr nur, wer das Feld behauptet, nicht was es gekostet hat | **Beim Bau des Wächters fiel auf, dass er selbst zu grob war:** `cancelActions` war geschrieben, exportiert und nirgends aufgerufen. Er prüft jetzt zusätzlich, dass jede Aktionsliste auch benutzt wird. 4 Wächtertests, 17 Ereignistests grün, `pnpm verify` grün |
| T-M14-15 | 2026-09-06 | **Der Playtest-Bogen misst, was schiefgehen kann** (Befund N10). Er war nach dem *Gebauten* geschrieben, nicht nach der Anforderung: Frage 4 fragte nach „Macht, Gegnerzahl, Schwierigkeit, Startzahl", während R-GAME-01 an erster Stelle die **Karte** nennt — und die war ein Blindschalter. Ein Bogen, der abfragt, was ohnehin da ist, kann eine Abnahme nur bestehen. Neu: Fenster schließen und wiederkommen (26a), Einstellungen über den Neustart (26b), Rückzug (25a), Kampfbericht mit beiden Seiten (25b), Bauabbruch (25c), die eigene Niederlage (25d), zweite Partie (25e) — **jede dieser Sachen war im Kern gebaut, getestet und für den Spieler unerreichbar**, und keine fällt in einer einzelnen Sitzung auf. Ein Nachwort hält fest, warum sie dazugekommen sind | **AK-7 ist ab jetzt messbar statt behauptet.** Im Abnahmebericht stand dafür eine feste Zeile „⏳ ausstehend“ — ein Kriterium, das seinen Zustand nie ändern kann, ist keins; der Bericht hätte sie auch am Tag nach dem Playtest unverändert gedruckt. Neu `scripts/playtest-sheet.mjs` mit `docs/reports/playtest-v1.md` (60 Fragen, aus dem Bogen erzeugt, gegebene Antworten bleiben beim Neuerzeugen stehen) und `playtestStatus` als **reiner Funktion**: sie zählt beantwortete Fragen und verlangt zu jedem „nein“ eine Zeile in der Befundtabelle — die Regel, an der Abnahmebögen sonst scheitern. `pnpm acceptance` schreibt daraus die AK-7-Zeile. Der vierte Haltepunkt und die Wächterliste sind in **einer** Änderung eingetragen, und der Wächter prüft zusätzlich, dass `tasks.yaml` und die Haltepunkt-Übersicht dieselben vier nennen. **Offen bleibt Noahs Playtest selbst** — dafür kann kein Skript einspringen, wohl aber sagen, ob er stattgefunden hat |
| T-M14-14 | 2026-09-06 | **AK-1 ist zum ersten Mal belegt.** Das erste und wichtigste Abnahmekriterium prüfte bis heute *kein einziger Test*: die einzigen `winner`-Zusicherungen im Bestand waren ein Zweispieler-Einheitstest und ein Determinismusvergleich, und keine automatisierte Partie lief je auf der Weltkarte bis zu einer Entscheidung. Möglich wurde der Test erst durch T-M14-11 — in der alten Aufstellung gab es 0 Kriegserklärungen in 1000 Tagen, ein Abnahmetest darauf hätte nichts gemessen. Gespielt wird die **ausgelieferte** Voreinstellung, nicht eine eigens gebaute Aufstellung. `pnpm acceptance` führt AK-1 jetzt als eigene Zeile statt in der Sammelzeile AK-1/4/6 | **Gemessen: 8 Mächte (7 KI), entschieden an Spieltag 822 durch Punktesieg, 8 Kriegserklärungen, 2899 Eroberungen, 5899 Gefechte, 188 s Laufzeit** (`docs/reports/fullgame.json`). Beim ersten Lauf trat `Timeout calling onTaskUpdate` auf — dieselbe Klasse, die `sweep.ts` schon dokumentiert hat: der Lauf rechnet jetzt in Tagesabschnitten und gibt die Ereignisschleife frei, mit identischem Ergebnis |

## M15 — Die KI wird ein Gegner

| Aufgabe | Datum | Was | Prüfung |
|---|---|---|---|
| T-M15-01 | 2026-09-06 | **Wer erfährt was.** Drei Fragen teilten sich zwei Felder, und die dritte gab es nicht: `audience` (wer *darf* lesen), `severity` (muss es *vor* der Zeit gesehen werden) und neu `concerns` (wen geht es *an*). Ohne das dritte hielt `firstAlertFor` **jeden öffentlichen Alarm für jeden Spieler** an — eine Eroberung zwischen zwei fremden Mächten stoppte das Vorspulen eines Unbeteiligten, und auf einer Weltkarte mit acht Mächten kam man damit keine drei Spieltage weit. Genau die Betriebsart, für die dieses Spiel existiert. `concerns` ist **Pflichtfeld** an `BaseEvent` mit der Vorgabe `concerns = audience` im `emit()`: privat ist damit ohne Zutun richtig, und ein *öffentlicher* Alarm — der Fall, um den es geht — muss seine Betroffenen nennen. Dazu **`BATTLE_STARTED`**: seit M1 deklariert, in der Alarmliste, Vorspulziel, mit eigenem Klang — und von keiner Zeile erzeugt; „bis zum ersten Gefecht vorspulen" lief stumm bis zur Obergrenze | **Pflichtfeld statt optional war die teurere und einzig ehrliche Wahl:** ein fehlendes Feld hieße still „betrifft niemanden", und die Typprüfung fand prompt **vier von Hand gebaute Ereignisse**, die `emit()` umgehen (darunter `GAME_STARTED` im Produktcode). Entprellung gegen die Kampfliste des Vortricks, weil sie je Tick neu gebaut wird und jedes Mal eine neue `battleId` bekommt — sonst meldete ein Gefecht über drei Ticks dreimal seinen Beginn. **Der Golden-Master blieb gültig:** `eventLog` steht in `HASH_OMIT_KEYS`, und die Vergabe der `battleId` wurde bewusst nicht angefasst. Eigenschaftslauf über 200 Spieltage: 0 Alarme ohne Betroffenen, 0 Verstöße gegen `concerns ⊆ audience`. 1229 Tests grün, `pnpm verify` grün |
| T-M15-02 | 2026-09-06 | **Die Freischaltungsachse.** Der Befund war keine falsche Formel, sondern eine fehlende Achse: **Spieltag 1 unterschied sich von Spieltag 40 durch nichts als den Kontostand** — alles war vom ersten Tick an baubar, es gab nichts, worauf man hinarbeitet. Jedes der 7 Gebäude und jede der 10 Einheiten trägt jetzt einen ersten Spieltag; davor lehnt der Kern mit `NOT_YET_AVAILABLE` ab und **nennt den Tag**, für Mensch und KI gleich (eine Freischaltung, die nur den Menschen bremst, wäre ein KI-Bonus — und R-AI-02 verlangt, dass Boni ausgewiesen werden). Fünf Tage sind belegt (Kaserne 1, Hafen 2, Eisenbahn 5, Fabrik 8, Flugplatz 10), zwölf abgeleitet, **alle siebzehn mit Begründung in BALANCING.md** und dort maschinell geprüft — der Test las bis heute nur `constants.json`, die neuen Zahlen wären also ohne Herkunft geblieben | **Die eigentliche Entscheidung war die Zählung:** `currentDay` ist eins-basiert, der erste Tick liegt auf Tag 1. Null-basiert wäre die Kaserne am ersten Spieltag nicht baubar gewesen, und der Fehler hätte im Playtest wie ein Wirtschaftsproblem ausgesehen. Der Lader lehnt einen fehlenden Tag ab **und** eine Einheit vor ihrem Gebäude (dann schlüge der Auftrag an `MISSING_BUILDING` fehl — falsche Begründung). Sechs bestehende Tests fielen und wurden **nicht weichgespült**: sie prüfen Küste, Hafen, Bauplätze und Geld, also stellen sie jetzt die Uhr hinter den letzten Freischaltungstag. Golden-Master unverändert. 1246 Tests grün, `pnpm verify` grün |
| T-M15-03 | 2026-09-06 | **Die Freischaltung erreicht den Bildschirm.** T-M15-02 hat den Riegel gebaut; ohne diese Aufgabe erführe der Spieler davon nur, dass ein Knopf grau ist — und ein gesperrter Knopf, der nicht sagt *wann*, sieht aus wie ein Fehler im Spiel statt wie eine Regel. Der Tag steht jetzt auf **zwei** Wegen da: im Ablehnungstext und im Tooltip der Sache. Nur der erste wäre eine Zusage über die **Reihenfolge der Prüfungen im Kern** gewesen — stünde die Kasse davor, läse der Spieler „zu wenig Rohstoffe" für etwas, das es noch gar nicht gibt. Nach der Freischaltung fällt der Zusatz weg. Dazu die KI: `nextBuilding` und `nextUnitFor` filtern auf den Tag, gelesen aus `view.tick` statt aus einer zweiten Zeitrechnung | **Der Wächter kann fallen, und das wurde nachgewiesen:** ohne den Filter ergibt der 60-Tage-Lauf mit drei KI-Mächten 2 Ablehnungen `NOT_YET_AVAILABLE`, mit ihm 0 — gezählt aus dem Ereignisstrom, mit einer vorgeschalteten Zusicherung, dass der Lauf überhaupt etwas gemessen hat. `packages/ai/src/economy.test.ts` stand im Plan und existierte nicht; sie ist jetzt angelegt. 1256 Tests grün, `pnpm verify` grün |
| T-M15-04 | 2026-09-06 | **Die erste echte Migration — und die Löcher, die in ihrer Lücke lagen.** `MIGRATIONS` war ein leeres Objekt mit einem auskommentierten Beispiel; die Kette ist **nie gelaufen**. Genau deshalb fiel nicht auf, dass `migrate` nach jedem Schritt den Hash entfernt (richtig — er beschreibt den Zustand *vor* der Umstellung), während `save.ts` nur `if (migrated.hash)` prüfte: **ab dem ersten echten Schritt wäre jeder migrierte Stand ungeprüft durchgelaufen, auch `{}`** (Befund 55). Dazu zwei Versionsnummern, von denen nur eine geführt wurde (Befund 56). Neu: `SCHEMA_VERSION` 2, **ein** Schritt 1 → 2, der alle M15-Felder leer anlegt (Betroffenheit, Verstimmungen, Feuerleitung), `validateState` als eigene Datei, und **drei geprüfte Ladewege** — migriert → Zustandsprüfung, unmigriert → Prüfsumme, keins von beidem → Ablehnung. Gefahren wird der Schritt an einem eingefrorenen echten V1-Stand (`packages/core/test/golden/save-v1.json`, 240 Ticks, Gefecht, Eroberung), nicht an einer Testtabelle | **Zwei Zusagen der Aufgabe waren nicht einlösbar** und stehen mit Ersatz in PROBLEME.md: der Hash *muss* sich ändern, wenn ein Feld dazukommt. An seine Stelle tritt eine schärfere Prüfung — der Unterschied zwischen altem und migriertem Zustand darf **ausschließlich** aus den drei angelegten Feldern bestehen. Der Golden-Master wurde erneuert, aber **erst nach dem Beweis**, dass sich nur die Gestalt geändert hat: derselbe 500-Tick-Lauf liefert nach Abzug von `schemaVersion`, `grievances` und `holdFire` bitgleich die alten Werte. Beim Bau gefunden: die Migration stürzte an fremdem Inhalt mit einem `TypeError` ab, statt ihn abzulehnen. 1274 Tests grün, `pnpm verify` grün |
| T-M15-05 | 2026-09-06 | **Das Verhältnis steuert die KI — erst die Messlatte, dann die Regel.** *Schritt 1:* Das Turnier bekam die Paarung „schwer gegen normal", zählt Kriegserklärungen und Friedensschlüsse, und der Grundlauf wurde festgeschrieben, **bevor** eine Zeile Diplomatie sich änderte. Er sagte mehr als „unentschieden": 25:25 war kein Gleichstand, sondern **die erste Nation gewann alle 50 Partien** — das Turnier maß die Startaufstellung der Testkarte. Gewertet wird jetzt paarweise (Hin- und Rückpartie desselben Seeds), und `winRateA` ist eine Punktquote. *Schritt 2:* `relationship.ts` als reine Funktion über fünf Anteile; `warThreshold` und `trustThreshold` als **eigene** Regelwerte, weil `maxFronts` bisher Frontenzahl, Kriegsschwelle und Risikobereitschaft in einem Wert war. Verstimmungen entstehen bei Überfall und verlorener Provinz, klingen täglich ab; `PublicView` führt endlich `reputation`, die öffentlichen Kriege und `occupiedSince` | **Zwei Befunde beim Messen, beide behoben:** die KI befahl in einer Partie **762 von 807 Mal** einen Marsch dorthin, wo die Armee schon stand (der Angriffszweig filterte die eigene Provinz nicht) — und `COMMAND_REJECTED` trug den Grund nicht, obwohl der Kern ihn kennt, weshalb es sich hinter `INVALID_TARGET` versteckt hatte. **807 → 0.** Dazu: ein Friedensschluss ist im Kern ein *Waffenstillstand*, das Turnier zählte den falschen Zustand und meldete 0. **Gemessen nach der Regel:** „schwer gegen normal" 0,80 statt 25 Unentschieden; **78 Friedensschlüsse in 50 Partien** statt 0 in 150. Die Obergrenze 0,95 steht zwischen *benachbarten* Stufen — „schwer gegen leicht" bei 1,00 zu drücken hieße, „schwer" absichtlich schlechter zu machen (begründet in PROBLEME.md). Golden-Master erneuert: die Regeländerung ist beabsichtigt. 1293 Tests grün |
| T-M15-06 | 2026-09-06 | **Vorspulen erreicht die Oberfläche — und der Wurmfortsatz ist weg.** Der Knopf rief den Kern nicht auf: `App.tsx` und die Taste `F` rechneten `step(ticksPerDay)`, also genau einen Spieltag; `fastForwarding` stand hart auf `false`, wodurch der Abbruchzweig toter Code war; `fastForward` aus dem Kern wurde in `apps/desktop/src` **an keiner Stelle** gerufen. Der einzige Aufrufer war `sim/SimEngine.ts`, den nichts startet. **Ziel Z1 — die frei regelbare Spielgeschwindigkeit, das erste erklärte Produktziel — war halb eingelöst, und R-TIME-02 wurde ausschließlich gegen Code geprüft, den kein Spieler ausführt.** Noahs Entscheidung: Weg (b), löschen. `apps/desktop/src/sim/` ist weg (521 Produktions-, 371 Testzeilen), `game/fastForward.ts` ruft die Schleife des Kerns in Häppchen von 24 Ticks, mit Abbruch und mit dem Grund des Halts in Worten. Zwei neue Haken im Kern: `commandSource` bekommt den **Zustand** statt der Tickzahl — mit einer Tickzahl allein war die KI dort nicht aufrufbar, und genau deshalb hatte sich die Oberfläche eine eigene Schleife gebaut —, und `afterTick` legt das Gedächtnis ab, ohne dass der Kern die KI kennt | **Die Entscheidung stand schriftlich, bevor Code fiel** — mit einem Test, der das erzwingt (`docs.test.ts` verlangt beide Wege, ihre Kosten und den gewählten in DECISIONS.md). Der Plan-Wächter fand sofort, dass **T-M10-02 auf gelöschte Dateien zeigte**; die Aufgabe ist wie T-M8-00 als zurückgenommene Zusage geführt, mit Begründung. **T-M10-04 mitgeschlossen** (seit T-M14-02 zurückgestuft). Neuer Wächter: Tempo, Pause und Vorspulziel dürfen nicht im `GameState` liegen — mit einer Zusicherung, dass die geprüfte Menge nicht leer ist. Zwei Ausnahmen im Erreichbarkeitswächter sind ersatzlos entfallen. 1285 Tests grün, Abdeckung gesamt **94,1 %** (vorher 93,6 — der gelöschte Code war ungenutzt und mitgemessen) |
| T-M15-07 | 2026-09-06 | **Feuerautomatik, Feuerleitung — und der vertagte Umbau.** Eine stehende Fernwaffenarmee schießt jetzt von selbst; „Feuer halten" ist die **Ausnahme**, denn andersherum wäre der Normalfall eine stumme Batterie an der Front gewesen. Dazu Befund 52, den T-M14-07 bewusst vertagt hatte: `BOMBARD` wirkte in Phase 1, der Nahkampf in Phase 8 — eine Armee, die in diesem Tick abmarschierte, wurde noch am alten Ort getroffen. Der Befehl setzt seither nur eine **Absicht**, aufgelöst in der neuen Phase `bombardment` zwischen Bewegung und Nahkampf, mit demselben Code wie die Automatik: dieselbe Kanone, eine Regel. Zielwahl bestimmt (größte sichtbare Stärke, bei Gleichstand kleinere Provinzkennung, über `provinceOrder`/`armyOrder`). Die KI lässt eine Armee mit Reichweite und Ziel stehen | 12 Beschusstests grün, darunter **Wirksamkeit statt Grün** (60 Einheiten richten mehr an als 25 — ohne T-M14-06 wäre er rot, und *das* ist der Grund für die Reihenfolge) und die Spiegelung beim Seitentausch. Golden-Master des Durchstichs erneuert (Phasenreihenfolge geändert — beabsichtigt); `tiny-500` blieb unverändert, weil die Testkarte keine Fernwaffen kennt. **AK3 ist offen und begründet:** im Turnier über 150 Spieltage entsteht **kein einziges** selbsttätiges Beschussereignis. Die Kette ist Glied für Glied nachgemessen — keine Artillerie, weil keine Fabrik, weil **83.081 Holz gegen 667.000 Kosten**. Ein Nebenblocker dabei behoben (`shortages.size === 0` war als Vorsicht gedacht und eine Dauersperre). Zugewiesen an T-M15-08, dem Integrationstor. Die Zusicherung wurde **nicht** grün formuliert: eine Grenze über null Ereignissen sagte nichts. 1299 Tests grün |
| T-M15-09 | 2026-09-06 | **Weltgeschehen: ein Filter statt einer Zeitung.** `WORLD_EVENT_TYPES` als **Positivliste** von acht Arten — keine Ausschlussregel, denn die gäbe bei jeder neuen Ereignisart still die falsche Antwort, und eine Meldung über fremde Vorräte wäre von selbst „Weltgeschehen". Die Auswahl liest `audience` **bewusst nicht**: wer wem den Krieg erklärt, ist öffentlich, und ein Filter, der die Leserschaft prüft, wäre für einen Unbeteiligten regelmäßig leer. R-NEWS-01/02/03 sind damit **ersatzlos aus dem Anforderungsdokument entfernt** — Text, `scope`-Einträge, Bezüge; eine gestrichene Anforderung, die ausformuliert stehen bleibt, wird beim nächsten Lesen wieder zur Zusage | **Der echte Umbau steckte in `App.tsx`, nicht im Knopf:** dort stand `.slice(-40)` **vor** dem Filtern. Bei 500 Ereignissen, von denen nur die drei ältesten Weltgeschehen sind, hätte der Filter nichts gefunden — ein Knopf, der lügt. Jetzt wird gedeutet, dann zugeschnitten. Dazu vier Satzfassungen aus fremder Sicht (`_FOREIGN`): drei Sätze trugen ein stillschweigendes „ich", und der Kampfbericht aus T-M14-13 hätte Dritten die Verluste genannt (R-DIP-04). **Beide Golden-Master byteweise unverändert**, kein Zustandsfeld, keine Migration — nachgewiesen mit `git diff`. Nebenbei aufgefallen: mit dem Löschen des Worker-Hosts in T-M15-06 hatte **R-TIME-02 seinen einzigen Beleg verloren** (er lag in den Tests eben jenes nie gestarteten Codes) — das Tor meldete `V1 offen: 1`, der Beleg steht jetzt bei der Regelung selbst |
| T-M15-08 | 2026-09-06 | **Das Integrationstor — und es hat zugemacht.** Die letzte Aufgabe von M15 baut nichts Neues, sie **misst**, ob die gebauten Mechaniken für die KI überhaupt leben. Auf der ausgelieferten Weltkarte, 200 Spieltage, acht KI-Mächte, alle Zahlen aus dem Ereignisstrom. Der Befund war hart: R-BAT-08 war in zwölf Einzeltests belegt und **im Spiel tot** — 0 Artillerie, 0 selbsttätiger Beschuss. Die Kette ist Glied für Glied nachgemessen, und **jedes Glied hatte einen eigenen Grund:** (1) die KI handelte erst bei eingetretenem Mangel, also nie für etwas, das sie *vorhat*; (2) Landprovinzen kamen vor Städten, und neun Kasernen verbrauchten das Holz, bevor die Stadt an der Reihe war — eine Fabrik kann nur in einer Stadt stehen; (3) ein Kasernen-Riegel übersprang jede Fabrikprovinz ohne Kaserne; (4) **die dringlichste Einheit war unbezahlbar** — der Panzer hat bei 30 % Zielanteil immer den größeren Rückstand als die Artillerie mit 20 %, scheitert aber am Öl, gewann die Wahl und die Artillerie kam nie an die Reihe | **Gemessen: 3.155 Handelsgeschäfte (vorher 0), 44 Fabriken (vorher 0), 11 Artillerie (vorher 0), 114 selbsttätige Beschüsse (vorher 0)**, dazu 0 Ablehnungen wegen Freischaltung, 0 diplomatische Fehlwürfe, 0 Geldmangel. **Erst nach dem vierten Glied fiel ein einziger Schuss — vier grüne Einzeltests hätten über jedes dieser Glieder hinweggesehen**, weil jeder die Lage selbst herstellt, in der die Mechanik greift. Die Handelsänderung riss dabei die Stufen-Spreizung aus T-M15-05 (0,80 → 1,00); `recruitShare` ist neu kalibriert (200/360 → 0,85), und die Antwort ist **bimodal**: 195/335 gibt 0,50, 190/345 gibt 1,00 — es gibt keine glatte Mitte. R-AI-08 ist auf das gekürzt, was M15 baut (Spionage mit R-SPY nach M17). Golden-Master unverändert. 1315 Tests grün, Kern 96,8 % |
| *Nachlauf* | 2026-09-06 | **Der volle Abnahmelauf hat zwei Rückschritte aus M15 aufgedeckt — beide behoben.** (a) **AK-1 war gerissen:** die Standardpartie kam über 1500 Spieltage zu keinem Ausgang, obwohl 2453 Provinzen den Besitzer wechselten; der Punktanteil des Führenden stieg auf 57 % und blieb ab Spieltag 900 stehen. Ursache war **eine Zeile aus T-M15-07**: „eine Fernwaffenarmee bleibt stehen" prüfte `armyRange > 0` und traf damit **jede Armee, die irgendeine Kanone dabeihat** — die KI rekrutiert gemischt, also blieb praktisch die ganze Front stehen. Jetzt bleibt nur stehen, wessen Einheiten sämtlich Fernwaffen sind; dazu die Verlockungsgrenze in der Kriegsentscheidung von 200 auf 450, damit eine erdrückende Übermacht ein tadelloses Verhältnis überstimmen kann. (b) **Das KI-Rechenbudget war rot** (0,517 gegen 0,5) | **AK-1 wieder grün: entschieden an Spieltag 876**, Sieger p6, 11 Kriegserklärungen, 2025 Eroberungen. Beim Rechenbudget zwei echte Verbesserungen (Verhältnis einmal je Macht und Tick statt viermal; `nextBuilding` bekommt die Provinz statt ihrer Kennung — ein quadratischer Durchlauf je Tick fiel weg), aber **0,498 gegen eine Grenze von 0,500 ist kein Ergebnis, das man stehen lässt**: die KI ist in M15 um **73 %** teurer geworden (0,0468 → 0,0808 ms), und das Profil zeigt, dass **97 % der „KI-Zeit" der Bau der öffentlichen Sicht ist**, nicht die Entscheidung. Als offener Befund an M16 zugewiesen; die Grenze wurde **nicht** angehoben. **Die Lehre ist dieselbe wie beim Integrationstor, nur umgekehrt:** dort kam eine Mechanik nie vor, hier kam sie überall vor — beide Male war jeder Einzeltest grün, und erst der Lauf über die ganze Partie hat es gesagt |

## M16 — Verpackung als Programm

| Aufgabe | Datum | Was | Prüfung |
|---|---|---|---|
| *Planung* | 2026-09-06 | **M16 bekommt einen Plan — er hatte keinen.** Der Meilenstein war seit dem 2026-09-05 deklariert und trug **null Aufgaben und null Anforderungen**: Prosa in `03-TASKS.md`, ein in C-02 zugesagtes **AK-8, nach dem kein Skript suchte** (Abschnitt 3 kannte AK-1…AK-7, `acceptance.mjs` prüfte AK-1…AK-5 und AK-7), und vier Befunde ohne Besitzer. Neu: sieben Aufgaben (T-M16-01…07), drei Anforderungen (**R-PKG-01** das Programm läuft, **R-PKG-02** Spielstände im Dateisystem, **R-UI-15** bedienbar ohne Maus), Entwurfskapitel **D20** und ein eigener Abschnitt 3.1 für AK-8 — **ausdrücklich außerhalb der V1-Abnahme**, weil ein AK-8 in der V1-Tabelle die Abnahme an einen Bau kettete, der hinter ihr liegt: der Fehler des Nachtrags 2.15 in neuer Gestalt | Plan-Wächter 21/21 grün, `test/requirements.test.ts` 22/22 grün. `pnpm coverage:requirements` meldet weiterhin **`V1 offen: 0`, Exit 0** — die drei neuen IDs stehen im `scope`-Block unter `later: M16` und zählen nicht gegen die V1. Das Tor führt M16 jetzt mit „0 von 3 belegt (Fortschritt, kein Tor)". Reihenfolge der Aufgaben begründet: T-M16-02 (KI-Rechenbudget) hängt **nicht** am Bau und steht deshalb vor ihm |
| *Befund* | 2026-09-06 | **R-AI-04 wird auf der falschen Karte gemessen** — nachgetragen in `PROBLEME.md`. Die Reparatur vom selben Tag hat R-ARCH-06/AK1 auf die ausgelieferte Weltkarte gezogen und **R-AI-04 elf Zeilen darunter stehen lassen**: die Anforderung sagt „bei 8 KI-Spielern", gemessen wird auf **zwölf Provinzen mit drei Mächten**. Der Dateikopf von `tick.bench.slow.test.ts` beschreibt genau diesen Fehler — „a twentieth of the real one … certify nothing about the requirement" — und der `describe` darunter begeht ihn weiter. `worldmap.bench.slow.test.ts` ruft `runAi` in beiden Prüfungen auf und **misst es nie** | Statisch belegt, ohne Lauf: der zweite Agent hatte die langsame Suite in Arbeit, und ein Bench neben einem Bench misst die Maschine. **Folge für die Bewertung des Vortagsbefundes:** der gemeldete Wert **0,498 gegen 0,500 sagt über R-AI-04 nichts** — ob die Anforderung unter ihren eigenen Bedingungen gehalten wird, ist bis heute **ungemessen**, was etwas anderes ist als „knapp gehalten". Zugewiesen an **T-M16-02**, dort zuerst messen, dann reparieren |
| *Vorarbeit* | 2026-09-06 | **Wo die Zeit von `publicView` liegt, statisch gelesen.** Drei Beobachtungen tragen den Entwurf D20.3: (1) `visibleProvinces` läuft **zweimal je Macht und Tick** — einmal in `updateIntel`, einmal in `publicView`, über denselben Zustand im selben Tick; (2) die **Geografie** jeder `VisibleProvince` — `id`, `name`, `kind`, `terrain`, `coastal`, `neighbors`, `seaLinks` — ändert sich in einer Partie nie und wird trotzdem je Macht und Tick neu abgeschrieben, auf der Weltkarte 237 Provinzen × 8 Mächte × jeden Tick; (3) der Anteil hat Zähler **und** Nenner, deshalb führt `ai-bench.json` künftig beide Zahlen einzeln | Keine Zeile Produktionscode geändert — die Reihenfolge in T-M16-02 ist ausdrücklich *erst messen, dann reparieren*, sonst ist hinterher unbekannt, was die Reparatur bewirkt hat |
| T-M16-01 | 2026-09-06 | **AK-8 bekommt einen Ort — und die Regel dahinter einen Wächter.** Neu `scripts/acceptance-criteria.mjs`: die Kriterien als **Daten** mit `scope` (`V1` oder Meilenstein), gelesen von Abnahmelauf **und** Wächter. `unhomedCriteria` prüft **drei** Richtungen — ein Kriterium, das die Anforderungen führen und keine Liste kennt; eine Liste, die eines führt, das die Anforderungen nicht kennen; und `nurErwaehnt`, der Zustand, in dem AK-8 wirklich war: **in C-02 zugesagt, in keiner Tabelle definiert**. Wer die Erwähnung als Definition zählt, findet den Fehler nie — die Zusage stand ja da. Dazu `criteriaOf`, weil der Lauf Zeilen wie `AK-4/6` führt: wer sie als **eine** Kennung liest, findet AK-6 nie. Der Ausgang des Laufs hängt seither an `v1Failures`, nicht mehr an allen Ergebnissen | **Am Stand von vorher nachgewiesen, dass der Wächter beißt:** gegen `01-REQUIREMENTS.md` aus `eae9f4d` meldet er AK-8 zweifach (`ohneKriterium` **und** `nurErwaehnt`), gegen den heutigen Stand nichts. Vier der acht neuen Tests laufen an **erfundenen** Dokumenten, damit die Prüfung nicht bedeutungslos wird, sobald die echten Kennungen stimmen — die Lehre aus T-M14-02, wo drei Wächter über einer leeren Menge grün waren. Zwei Tests zeigen die Trennung als Funktion statt als Behauptung: ein gerissenes AK-8 lässt den Ausgang der V1 unberührt, **und dieselbe Zeile fängt ihn, sobald `scope` auf `V1` steht**. `test/requirements.test.ts` 30/30, Plan-Wächter 21/21, `pnpm coverage:requirements` weiterhin `V1 offen: 0`, ESLint sauber. ⚠ `pnpm verify` steht noch aus: ein zweiter Agent hatte die langsame Suite in Arbeit, und der Tick-Bench liegt bei 0,498 gegen 0,500 — ein Lauf daneben misst die Maschine |
| T-M16-03 | 2026-09-06 | **Das Spiel ist zum ersten Mal ein Programm.** C-02 nennt Tauri seit dem ersten Tag und war die einzige Rahmenbedingung, die **nie ausgeführt** wurde. Jetzt: `@tauri-apps/cli` im Lockfile (vorher **null** Treffer), `Cargo.lock` im Baum, und ein selbst gezeichnetes Symbol — es gab im ganzen Projekt **keine einzige Bilddatei**. `scripts/build-icon.mjs` zeichnet es aus den Farben der Oberfläche (Leinen, Tusche, eine zinnoberrote Einheitenmarke mit dem Andreaskreuz) und rendert **jede Größe einzeln statt zu skalieren**. Der Wächter `packaging` bekam einen zweiten Teil, der nur an Dingen hängt, die ein Bau hinterlässt oder ohne die er abbricht — der alte verglich `tauri.conf.json` gegen `capabilities/local-only.json`, zwei Dateien derselben Hand, und blieb grün, während nie ein Bau lief | **Gemessen: `TAURI_EXIT=0`.** `worldwar.exe` 7,86 MB, dazu **zwei Installationspakete** — `WorldWar_0.1.0_x64_en-US.msi` (2,94 MB) und ein NSIS-Setup (2,20 MB). **Drei Abbrüche waren nötig, um dorthin zu kommen, und keiner stand in der Vorlage:** (1) `icons/icon.png` fehlte; (2) `tauri-build` verlangt auf Windows zusätzlich `icons/icon.ico` für die Ressourcendatei; (3) der Bundler bricht mit „Couldn't find a .ico icon" ab, wenn die Datei zwar da ist, aber nicht in `bundle.icon` steht. Alle vier neuen Wächterprüfungen hätten auf dem Stand von heute Mittag gerissen. **Beim ersten Versuch entstand ein Symbol, das wie eine Börsenkurve aussah** — die steigende rote Zickzacklinie; neu gezeichnet als Einheitenmarke, die auch bei 16 px trägt. Nebenbei die Lehre für den Ablauf: `cmd \| tail` meldet den Status von `tail`, und ein gescheiterter Bau sah dadurch aus wie „exit 0" |
| *Konsistenz* | 2026-09-06 | **Aufräumen von Falschaussagen, damit ein neuer Agent nicht in sie hineinläuft.** (a) `docs/reports/acceptance.md` meldete „AK-1 ❌, 5 von 7" aus einem Lauf **vor** der Reparatur, während `fullgame.json` daneben Sieg an Spieltag 876 auswies — derselbe Tag, verschiedene Stände, und nichts sagte es; der Bericht **stempelt jetzt den Commit**, gegen den er lief, und trägt den Hinweis, dass er sonst überholt ist. (b) `ai-bench.json` wies `share: 0.498` aus und verschwieg, dass auf **zwölf** Provinzen mit **drei** Mächten gemessen wurde — die Datei nennt jetzt ihre Bedingungen und sagt in `certifies` ausdrücklich, dass sie **nichts** belegt. (c) Die Begründung des Haltepunkts T-M12-03 behauptete „sechs von sieben maschinell grün" — einen Stand, den niemand hat; sie sagt jetzt, was einzeln nachgewiesen ist und was seit der Reparatur **nicht** nachgemessen wurde | **Und der teuerste Fund war die Startsequenz selbst:** `AGENT-EXECUTION.md` §0 verlangte `01-REQUIREMENTS.md` → `02-DESIGN.md` → `03-TASKS.md` **am Stück**, über **5400 Zeilen**, vor jeder Zeile Code. Gekürzt auf: lies `WORKFLOW.md`, prüfe den Branch, nimm deine Aufgabe und lies **ihren** Abschnitt. Neu `docs/plan/WORKFLOW.md` (der Ablauf bis zum Spielen, mit sechs Fallen, die schon jemanden gekostet haben) und ein `CLAUDE.md` im Projekt, das jede Sitzung automatisch dorthin schickt. Das **README** war zwei Meilensteine alt (meldete „M14, 13 von 16", die längst gebaute Schrift als offen und M15 als bevorstehend) |
| *Abnahme* | 2026-09-06 | **Der vollständige Abnahmelauf: 6 von 7** (`pnpm acceptance` gegen `5065b99`, 90 min, `docs/reports/acceptance.md`). Grün: **AK-1** (Standardpartie entschieden an **Spieltag 876**, 8 Mächte, Weltkarte), AK-2 (Anforderungstor `V1 offen: 0`), AK-3 (Kern 96,8 %, gesamt 94,4 %), AK-5 (Wächter für Monetarisierung und Netz). Offen: AK-7, Noahs Playtest. Von 37 langsamen Prüfungen fiel **eine**: R-ARCH-06/AK1, Tickbudget 3,525 ms gegen 3,5 ms. Neu und wie entworfen: die Zeile **AK-8 erscheint im Bericht und lässt den Exit-Code unberührt** — die Trennung aus T-M16-01 wirkt | **Die rote Zeile war Fremdlast, kein Rückschritt — nachgewiesen, nicht vermutet.** `OPERATOR.exe` belegte ab 21:20 **2,7 von 6 Kernen**; die Benchmarks liefen gegen 22:25 mitten hinein. Nach dem Schließen dreimal nachgemessen, **gleicher Commit: 2,546 / 2,606 / 2,655 ms** — auf der historischen Reihe (2,344/2,359/2,463/2,528). **Zwei Irrwege sind dabei entstanden und stehen als Warnung in `PROBLEME.md`:** (a) ein *Einzelvergleich* vor/nach der AK-1-Reparatur hätte „kostet 13 %" gemeldet — im Wechsel dreimal gemessen überlappen die Spannen (3,307 vor gegen 3,279 nach), es gibt keinen nachweisbaren Unterschied; (b) die Kontrolle „ist die Maschine frei" fragte `Get-Process node`, also nur nach den **eigenen** Prozessen, und war deshalb wahr und wertlos. **Die Grenze bleibt bei 3,5 ms** — sie war nie das Problem |
| *Playtest* | 2026-09-06 | **Der Bogen ist durchgefahren — von einem Agenten, nicht von Noah.** 60 von 60 Fragen beantwortet: **32 ja, 12 nein, 16 nicht geprüft**, jedes „nein" mit Befund (`docs/reports/playtest-v1.md`). Jede Antwort trägt ihre Grundlage: `[G]` im laufenden Spiel gefahren, `[C]` nur aus dem Code, `[U]` von der Umgebung verhindert. Vorbereitet durch sieben Leser-Agenten, die je Fragenblock den **Sollwert aus dem Code** geholt haben (1,33 Mio Token, 545 Werkzeugaufrufe) — ohne sie hätte ich die Hälfte der Befunde nicht gefunden | **Zwei schwere Befunde, beide gemessen.** (a) **Die Kartenwahl ist weiterhin ein Blindschalter:** „Kleine Welt (12)" gewählt, es startet die Weltkarte („Mittlerer Westen"/„Ostkanada" statt „Hafen"/„Waldland"). Befund 38/N10 ist NICHT geschlossen — und `docs/PLAYTEST.md:149` behauptet das Gegenteil, der Abnahmebogen enthält also selbst eine Falschaussage. (b) **Der Spielstand überlebt, ist aber nach einem Neustart unerreichbar:** IndexedDB führt `stand-1` korrekt, doch die Liste öffnet nur Strg+S, und die Taste wirkt ohne laufende Partie nicht; schließt man den Startdialog, bleibt eine Sackgasse mit „Die Welt wird aufgebaut …". Dazu: roher Schlüssel `[province.cancelBuild]` auf einem Knopf, leere Debug-Ansicht (Frage 48 mit **ja** statt erwartet nein), Rückzug ohne Kostenangabe, `Verbrauch` dauerhaft 0. **Zwei Gegenproben, die eine Falschaussage verhindert haben:** der aus dem Code gemeldete Verdacht „jede eigene Provinz wird im Moral-Modus zinnoberrot" tritt live **nicht** auf (gemessen #505738, Oliv), und „Klick auf die Karte wählt nichts aus" war mein eigener Werkzeugfehler, kein Produktfehler. **Nicht geprüft blieben 16 Fragen**, weil das Fenster im Hintergrund lag: `requestAnimationFrame` feuerte 0-mal in 2 s, die Karte zeichnete nicht, die Spieluhr stand — das Vorspulen wirkt ohne rAF und hat die Sitzung getragen (35 Spieltage). **AK-7 bleibt offen: es verlangt im Wortlaut Noahs Abnahme** |
| *Playtest 2* | 2026-09-06 | **Die sechzehn offenen Fragen nachgeholt — acht davon geschlossen.** Endstand **39 ja · 13 nein · 8 nicht geprüft** (vorher 32/12/16). Möglich wurde es durch einen Ersatz für `requestAnimationFrame` (`setTimeout`), weil im verborgenen Fenster keine Bilder gezeichnet werden und die Spieluhr daran hängt — sauber getrennt gehalten: **uhrabhängige** Fragen damit beantwortet, **bildabhängige** ausdrücklich nicht, denn ein Zeitgeber sagt nichts über eine Bildrate. Zwei vollständige Partien bis Tag 171 gefahren | **Vier neue Antworten mit Beleg:** Q21 die Ankunftsvorschau steht vor der Bestätigung („Ostkanada: Ankunft Tag 50, 22:00") und die Armee kam an · Q25b das Protokoll nennt bei EIGENEN Gefechten beide Seiten („Verluste: Vereinigte Staaten 3, Kanada 0") · Q25d/Q41 der Endedialog erscheint von selbst („Sie sind ausgeschieden. Ihre letzte Provinz ist gefallen") · Q44 21 Klangaufrufe bei eigenen Ereignissen, null bei fremden und null bei abgeschaltetem Ton · Q47 Automatikstände „Tag 15" und „Tag 36" rotieren korrekt · Q31 der Trendpfeil erscheint doch, sobald sich etwas bewegt („Moral: 84 %, **steigend**") — mein erster Befund war voreilig. **Zwei neue schwere Befunde:** der Knopf **„Neue Partie" im Endedialog führt ins Leere** — er schließt den Dialog, öffnet keinen Startdialog und lässt den Spieler in der toten Partie zurück (Tag 171, Siegziel 0 %); und **über zwei ganze Partien erschien keine einzige Meldung** — der Bereich `.alerts` existiert im DOM nicht, Hauptstadtverlust und eigenes Ausscheiden liefen wortlos vorbei. Dazu: „1 Provinzen" im Endedialog, und ein stündlich wiederholtes Patt mit „Verluste: 0, 0" |
| T-M12-04/05/06 | 2026-09-07 | **Drei Playtest-Befunde behoben, jeder mit dem Test, der ihn gefangen hätte.** (a) **Der Abbruchknopf** hieß `[province.cancelBuild]` — `actions.ts` fragte `province.`, der Katalog führt `actions.`; in derselben Datei stehen 23 Aufrufe `t('army.…')` und genau dieser eine Ausreißer. (b) **Die Siegbedingung** wird erklärt, mit den echten Schwellen aus `newGame.ts` (700 bzw. 1000), und der Satz wechselt mit der Auswahl. (c) **Die zweite Partie beginnt wirklich:** `setDialog('new')` war ein **Zustandswert ohne Renderzweig** — den Startdialog zeichnet nur der Frühausstieg `if (!state \|\| !view \|\| !ctx)`, den eine geladene Partie nie erreicht; `setState(null)` leitet dorthin, und die Flagge muss zurück auf `false`, sonst meldet die **zweite** Partie ihr Ende nie | **Die Wächter sind der eigentliche Teil.** `text-keys.test.ts` sammelt jeden statisch geschriebenen `t('a.b')` und verlangt den Schlüssel — gegen den Baum vor der Reparatur **genau ein Verstoß, kein Fehlalarm**; er prüft die Menge auf Nichtleere und nennt die Zahl der zusammengesetzten Aufrufe, die er nicht auflösen kann. Für die zweite Partie drei Kettentests, die den Weg des Spielers gehen (entschiedenen Stand laden → klicken → **hinsehen**) — **gegen den kaputten Stand fallen alle drei**, einer spielt zwei Runden, weil die Flagge sonst unentdeckt bliebe. Warum nichts davon vorher auffiel: `Standings.test.tsx` rendert den Siegdialog dreimal und **jedes Mal ohne `onNewGame`**; die Eigenschaft ist optional, der Knopf lag in keinem Testlauf im DOM, und kein einziger der dreißig App-Tests erreichte je eine entschiedene Partie. Geprüft wurde, **dass** ein Handler läuft — nie, **was** danach auf dem Bildschirm steht. `pnpm verify` grün, Kern 96,8 %, gesamt 94,6 % |
| T-M12-07/08 | 2026-09-07 | **Zwei Playtest-Befunde derselben Bauart: gebaut, getestet, unerreichbar.** (a) **Die Kartenwahl war ein Blindschalter** — `options.mapId` wurde vom Dialog geschrieben und von **niemandem** gelesen: `main.tsx` baute die Kartenliste als reine Beschriftung (id, Name, Zahl) und warf die `MapData` weg, also bekam App immer die Welt. Jetzt trägt die Sammlung die Karten selbst, App hält die laufende als Zustand, und **jeder** Leser folgt ihr — auch `advance` und `fastForwardChunk`, die sonst eine 237-Provinzen-Karte gegen einen Zustand mit zwölf gefahren hätten. Zwei stillere Zwillinge kamen mit: die Völkerliste folgt der gewählten Karte („Kleine Welt" wählen und als Nordland spielen war derselbe Fehler durch eine kleinere Tür), und ein geladener Stand stellt seine eigene Karte wieder her. Der Test misst die Provinzzahl der **begonnenen Partie**, aus dem Spielstand gelesen — nicht den Dialogzustand und nicht die Provinzliste, die vom Nebel begrenzt ist (7 von 12, 13 von 237) und nur aussieht wie eine Zahl. (b) **Die Spielstände öffnete nur Strg+S.** R-UI-05 verlangt jede Aktion per Klick, also hat die Kopfleiste einen Knopf bekommen und der Startdialog einen Weg hinüber — wer wiederkommt, will laden, nicht neu anfangen. Vor der ersten Partie zeigt die Liste jetzt auch, ohne Speichern-Knopf, weil es nichts zu speichern gibt | **Der gemeldete Weg in die Sackgasse war ein anderer als der gemessene:** das Kreuz war `onClose={() => undefined}` — ein sichtbarer Knopf, der nichts tat, und damit selbst ein R-UI-05-Verstoß. Hinein führte Strg+S, das den Startdialog durch eine leere Fläche ersetzte, aus der Escape ihn endgültig löschte. Die leere Fläche trägt jetzt den Weg zurück, deshalb darf das Kreuz schließen und Escape wieder **jeden** Dialog. Das nimmt eine ältere Zusage bewusst zurück („der Startdialog bleibt bei Escape stehen"); ihr Test ist auf die **Zusage** umgeschrieben — der Spieler strandet nicht — statt auf das alte Mittel. 404 Tests grün |
| T-M12-09 | 2026-09-07 | **Die Meldungen erreichen den Spieler — vier Defekte, nicht einer.** Die gemeldete Ursache („die Kette bis zur Sicht fehlt") stimmte **nicht**: App zeichnet `<Alerts>` unverwandt und ruft `alertsFor` auf der lebenden Sicht. Leer war die Liste. (A) **Ein Leck im Kern**, und es reicht weiter als die Meldungen: `step` läuft die ganze `PHASE_ORDER` und ruft `dailyTick` **danach** — `bookkeeping` ist aber die Phase, die `ctx.events` ans Protokoll hängt, und sie ist die letzte. Alles vom Tagesende kam in `StepResult.events` zurück und stand nie im Protokoll; da `advance` nur den Zustand weiterreicht, ist das Protokoll die einzige Ereignisquelle der Oberfläche. Tagesbericht, Ausscheiden und Spielende waren **konstruktionsbedingt** unsichtbar. Nachgetragen statt umsortiert — die Phasenreihe ist der Golden Master, und `eventLog` steht in `HASH_OMIT_KEYS`, das Nachtragen ist hashneutral. (B) Die Hauptstadtmeldung war **toter Code** für den Fall, für den sie geschrieben war: `occupation` setzt `capitalProvinceId` im selben Tick auf null. Sie fragt jetzt `capitalLostUntil`; der alte Zweig bleibt als Aufstandsfall, wo der Eigentümer wegfällt und die Kennung stehen bleibt. (C) **Fertigstellungen**, die vierte von R-UI-14 geforderte Quelle, fehlten ganz. (D) **Überrennen** erzeugte nichts: eine unverteidigte Provinz wechselt ohne Gefecht den Besitzer, und die Meldung hing allein an Gefechten | **Die Tests fahren eine echte Partie und prüfen den Bildschirm**, wie die Aufgabe es verlangt — ein Einzeltest von `alertsFor` kann sie ausdrücklich nicht erfüllen. **Nachgewiesen, dass sie beißen:** gegen die vorige `Alerts.tsx` fallen alle drei mit „Unable to find role=region and name Meldungen" — genau dem Symptom des Playtests. 444 Kerntests grün, Golden Master und Determinismus unberührt |
| T-M12-10 | 2026-09-07 | **Die sechs kleineren Befunde.** (a) Armeebefehle trugen keinen Kostenhinweis, während die Bauknöpfe es vormachen — der Kanal (`ActionSpec.hint`) war da und wurde von einem einzigen Befehl genutzt. Jetzt alle, mit Zahlen **aus den Regeln**, nie aus dem Text. (b) Die Spalte „Verbrauch" stand auf null: sie führte korrekt den Armeeunterhalt, und ohne Armee ist der null — der Name war die Falschaussage. Sie heißt jetzt „Unterhalt", daneben steht „In Auftrag": was in laufenden Bau- und Aushebungsaufträgen steckt, bezahlt und noch nicht geliefert. (c) Das Vorspulen sagte nie, warum es anhält, obwohl R-TIME-03/AK1 „stoppen **und** melden" verlangt — der Kern führt den Grund seit M15, App hielt ihn im Zustand, gelesen hat ihn niemand. Jetzt nennt es Grund, verstrichene Zeit und beim Alarm **das auslösende Ereignis**. (d) Flugplatz und Jagdflugzeug teilten ein Zeichen; alle 26 vorhandenen Pfade waren vergeben, der Flugplatz hat ein eigenes bekommen. (e) „1 Provinzen" über dem Satz „Ihre letzte Provinz ist gefallen" — zwei Fehler in einer Zeile: die Mehrzahl bei eins (das Projekt hatte keinen Plural-Helfer, nur eine handgeschriebene Verzweigung in `duration`) und eine Zahl, die **erinnerte** Provinzen mitzählte. (f) Die Debug-Ansicht zeigte einen leeren Hash und zwei Überschriften ohne Inhalt — alle drei Größen existierten und wurden weggeworfen | **Zwei neue Wächter, beide bissen sofort:** „jeder Armeebefehl trägt einen Hinweis" fand die zwei deaktivierten Varianten, „kein Gebäude teilt ein Zeichen mit einer Einheit" hätte die Kollision verhindert. Die Wahl bei (b) — Gebundenes statt eines Ausgabenhauptbuchs — steht mit Begründung in `DECISIONS.md`: das Ereignisprotokoll ist ein Ringspeicher von 500 Einträgen und sieht einen Spieltag nicht sicher, ein Hauptbuch kostete `SCHEMA_VERSION` 3 und eine Migration vier Tage vor der Abnahme. `committed` bleibt bewusst **außerhalb** von `balance`, die der Playtest fünfmal gegen den echten Tageszuwachs geprüft hat. 1370 Tests grün |
| T-M16-04 | 2026-09-07 | **Der Datei-Port — und eine Zusage aus M8 zum ersten Mal eingelöst.** Spielstände werden im Programm zu Dateien. Der Port tritt als **dritte** Umsetzung zu Memory und IndexedDB und ändert an `createStorage` und am Vertrag nichts. Damit stimmt der Wortlaut von T-M8-00 („dieselbe Vertragstestreihe gegen alle drei Umsetzungen") erstmals — die Aufgabe stand seit M8 auf `done`, nannte vier Dateien, von denen **eine** existierte, während die ausgelieferte Anwendung in den Arbeitsspeicher schrieb und „gespeichert" meldete. Sie ist jetzt geschlossen statt weitergetragen; `NodeStorage` entfällt mit Begründung — der kopflose Läufer speichert nicht, und eine vierte Umsetzung ohne Aufrufer wäre genau die Art Zusage, die diesen Eintrag verursacht hat | **Die eigentliche Prüfung ist R-PKG-02/AK2, nicht der Vertrag:** ein **außerhalb** des Programms gelöschter Stand verschwindet aus der Liste. Ohne sie bestünde auch ein Port, der die Namen im Speicher führt und den Ordner einmal beim Start liest — der Vertrag verlässt den Prozess nie und kann diesen Unterschied nicht sehen. Deshalb liest `list()` jedes Mal den Datenträger. Zwei Dinge, die ein Dateisystem erzwingt und eine `Map` nicht: Namen werden **kodiert statt ersetzt** (der Vertrag verlangt „Mein Spielstand — Tag 42 (Österreich)", und ein Ersetzen der unter Windows verbotenen Zeichen durch Unterstriche gäbe zwei Ständen einen Dateinamen), und der Ordner wird vor dem ersten Schreiben angelegt — der Fehler, der genau einmal auftritt, beim allerersten Speichern eines neuen Spielers. 1385 Tests grün |
| T-M16-07 | 2026-09-07 | **Bedienbar ohne Maus — und der Fokusfang, den es gar nicht gab.** Belegt waren der Kontrast und die Tastenzuordnung als reine Funktion; **kein Test hatte je einen Dialog geöffnet und geschlossen**. Beim Schreiben der Tests fiel die Lücke auf, die sie bewachen sollten: `aria-modal` sagt einem Vorleseprogramm, dass hinter dem Dialog nichts ist — die Tabulatortaste hört nicht darauf. Ohne Fang tabbt man aus jedem modalen Dialog in die Karte dahinter: sichtbar verdeckt, mit der Tastatur erreichbar und bedienbar. Dazu ein Wächter zu AK2, der den Quelltext liest: ein Knopf, dessen ganzer Inhalt ein bloßes Zeichen ist und der keinen Namen für Hilfsmittel trägt | **Der erste Entwurf des Fokusfang-Tests war grün, ohne etwas zu belegen:** jsdom bewegt bei Tab nichts, also war „der Fokus ist noch im Dialog" auch ohne jeden Fang wahr. Er prüft jetzt den **Sprung** (letztes Feld → erstes, und rückwärts) und wurde gegen die vorige `Dialogs.tsx` als fallend nachgewiesen. Der Wächter zu AK2 hat einen Begleiter, der zeigt, was er fängt — einer, der nur grün sein kann, ist keiner |
| T-M16-02 | 2026-09-07 | **Das KI-Rechenbudget dort gemessen, wo es gilt — und die zwei Schnitte deshalb *nicht* gebaut.** R-AI-04 sagt „bei 8 KI-Spielern" und wurde auf **zwölf Provinzen mit drei Mächten** gemessen. Unter den eigenen Bedingungen (Weltkarte, 237 Provinzen, acht KI-Mächte, 480 Ticks): KI-Median **0,206 ms**, Tick-Median **2,569 ms**, Anteil **0,074** gegen 0,30 gefordert. **Die alte Zahl war in die falsche Richtung falsch:** 0,463 gegen eine Zusicherung von 0,5 liest sich wie „knapp an der Grenze", und genau so sind zwei Optimierungen in den Plan gekommen, die nicht nötig sind. Der Grund steckt im Quotienten — auf zwölf Provinzen kosten KI und Tick beide rund 0,065 ms, der Anteil liegt dort **von Natur aus** bei 0,5 | **Beide Mediane stehen jetzt einzeln im Bericht:** `updateIntel` liegt im Nenner, ein schnellerer Tick würde den gemeldeten Anteil **erhöhen**, während das Spiel besser wird. Der Block auf der kleinen Karte hieß „bleibt unter dreissig Prozent" und sicherte **fünfzig** zu; er heißt jetzt, was er ist, und misst die **absolute** KI-Zeit statt eines Anteils, der mit 0,487 gegen 0,5 lag — 2,6 % Reserve, also ein Test, der zufällig reißt und dann die Untersuchung kostet, die er sparen soll. Keine Lockerung: die Zusicherung, die R-AI-04 vertritt, ist mit 0,30 strenger als die 0,5 es je waren. Entscheidung und Zahlen in `DECISIONS.md` und `PROBLEME.md` |
| T-M16-05 | 2026-09-07 | **AK-8 am gebauten Programm gemessen, nicht behauptet.** Gestartet, Partie begonnen, gespeichert, geschlossen, neu gestartet — der Stand liegt wieder in der Liste, und er lädt. Der Spielstand ist eine **echte Datei** (`stand-1.json`, 96 198 Bytes) im Anwendungsordner; der Ordner existierte vorher nicht. Bericht mit Datum und Commit in `docs/reports/packaging.md`; der Abnahmelauf liest diese Messung, statt seine eigene Zuständigkeit auszugeben — und sagt selbst, wenn der Bericht überholt ist | **Zweimal gemessen, und der Bericht sagt warum:** der erste Bau enthielt den beschriebenen Code **nicht** — `Dialogs.tsx` wurde um 01:59 geändert, das Bündel war um 01:56 gebaut, nachgewiesen am Bündel selbst. Für AK-8 hätte es nichts geändert; ein Bericht, der auf ein Erzeugnis ohne den beschriebenen Code zeigt, ist aber genau die Falschaussage, die am 2026-09-06 schon einmal Zeit gekostet hat. Schritt 6 belegt nebenbei **T-M12-07**: die Liste ist ohne laufende Partie erreichbar — genau das war der Befund |
| T-M16-06 | 2026-09-07 | **Die Karte zum ersten Mal zeichnend gemessen.** `MapCanvas` lief in **keinem** Test: `getContext` liefert unter jsdom `null`, jeder Zeichenweg endete an der ersten Abfrage, 141 von 249 Zeilen waren unausgeführt — die größte Datei der Oberfläche war die einzige, über die nur Schätzungen vorlagen. Sie bekommt jetzt einen Kontext, der jeden Aufruf mitschreibt: Provinzen werden gefüllt, Farben kommen aus den Gestaltungsmarken statt aus Literalen im Zeichencode, Namen erscheinen nah genug, der Marschweg ist gestrichelt, ein Klick meldet die Provinz. Abdeckung 108 → **168 von 249**; die unausgeführten Zeilen sind jetzt **gezählt: 81**. Das Bildbudget wurde im Browser an einem echten 1440×900-Kontext mit den **echten Modulen** gemessen: Weltansicht 1,0 ms Median (p95 3,0, max 7,2), nah mit 103 Beschriftungen 1,7 ms (p95 4,1, max 7,3) — gegen 16,7 ms Budget | **Was der Bericht ausdrücklich *nicht* belegt, und das wiegt schwerer als die Zahlen:** nicht die Bildrate eines laufenden Fensters. `requestAnimationFrame` feuerte in dieser Umgebung **0-mal in einer Sekunde**, während `document.visibilityState` „visible" meldete — dieselbe Wand, an der der Playtest die Bildfragen 9, 10b, 12, 18, 20 und 38 offen gelassen hat. Zwei meiner Testannahmen waren falsch und der Code richtig: `scale` sind Weltkoordinaten je Bildpunkt (klein heißt nah), und die Leinwand meldet Klicks über `onClick`. 1396 Tests grün, gesamt 95,1 % |
| *Abnahme* | 2026-09-07 | **Der vollständige Abnahmelauf: 7 von 7 maschinelle Prüfungen bestanden** (`pnpm acceptance` gegen `009bec6`, 73 min, Exit 0). AK-1 belegt: die ausgelieferte Standardpartie mit sieben KI-Gegnern wird an **Spieltag 876** entschieden, mit 2025 Eroberungen und 11 Kriegserklärungen. AK-8 meldet sich erstmals als **gemessen** statt als „zählt nicht" | **Der Lauf davor hat einen Widerspruch im eigenen Bericht sichtbar gemacht:** die Tabelle meldete „AK-7 ✅ beantwortet (60 Fragen)", der Satz zwei Zeilen darunter „offen bleibt AK-7". Beides aus demselben Skript. Der Haken stand da, weil `playtestStatus` zählte, **ob** geantwortet wurde, nicht **wer** — und ein Kriterium, das ausdrücklich nur ein Mensch erfüllen kann, wurde von einer Maschine abgehakt. Die Person ist nicht prüfbar, ihre **Angabe** ist es: der Bogen trägt jetzt eine Zeile `Durchgang von:`, `ok` verlangt darin Noahs Namen, `complete` behält die alte Bedeutung. Fehlt die Zeile, gilt der Durchgang als nicht von Noah — die sichere Richtung. Gegengeprüft: heute `false`, mit Noahs Eintrag `true`. Vier Tests, einer davon lässt sich nicht von „Noahs Assistent" täuschen |
| *Abnahme (AK-8)* | 2026-09-07 | **Die Spiegelseite desselben Fehlers.** Der Lauf meldete die AK-8-Messung als überholt, weil der Commit abwich — wahr und wertlos: zwischen Messung und Bericht lagen nur Tests, Berichte und Plandateien, keine Zeile, die ins Erzeugnis geht. Statt das in Prosa wegzuerklären, **sieht der Bericht nach**: `artefactUnchangedSince` teilt die geänderten Dateien ein, und eine Messung, deren Erzeugnis unberührt ist, gilt als erfüllt — mit Nennung des Commits, gegen den gemessen wurde. `scripts/` steht bewusst **nicht** in der liefernden Menge: ein Skript geht nicht ins Programm, und was es erzeugt, liegt unter `data/` | Über fünf Fälle geprüft (nur Doku, nur Tests, Skript, Produktionscode, Regeldaten). Beide Reparaturen sind dieselbe Regel: **ein Bericht, der eine Tatsache behauptet, die er nicht geprüft hat, ist schlechter als einer, der sagt, dass er es nicht weiß.** 1400 Tests grün |
| T-M19-01 | 2026-09-07 | **Der Wächter, der es hätte finden müssen.** Vier Prüfungen halten `world.json` gegen `world-shapes.json` — durch **dieselbe** Projektion, die der Generator benutzt: `WIDTH`/`HEIGHT`/`TOP`/`BOTTOM`/`toX`/`toY` sind aus `build-map.mjs` nach `packages/mapgen/src/project.ts` **verschoben** worden, nicht kopiert. Ein Wächter mit eigener Projektion kann nicht auffallen, wenn die Projektion das Falsche ist. Gemessen: **G1 14 von 237** (Ankerpunkt außerhalb der eigenen Fläche), **G2 66 von 237** (unter 99 % der Quellfläche; insgesamt sind **85,78 %** des Landes gezeichnet), **G3 3 von 35** (Los Angeles, Tokio, Singapur), **G4 1** (Grönland, 796 Punkte bis y = −436). Dazu `data/maps/landmarks.csv` mit 35 Städten und `docs/reports/map-geometry.md` | **Drei Zahlen des Bauplans sind nicht reproduzierbar, und das ist der Grund, dass alles nachgemessen wurde.** „G2 fällt mit 130 von 237" gilt für die Schwelle *100 %* — bei den im selben Satz genannten 99 % sind es **66**; „G1 mit 4" erst ab über 6 px Toleranz, ohne Toleranz sind es 14; „G3 10 von 28" war gar nicht prüfbar, weil es die Datei nicht gab. Die Übergangslisten sind deshalb gemessen, nicht abgeschrieben. Sie sind keine weichere Schwelle: jeder Test prüft auch die **Gegenrichtung** — eine Provinz, die auf der Liste steht und heil ist, lässt ihn fallen, sonst schrumpfen solche Listen nie. Dass alle vier beißen, ist einzeln nachgewiesen, indem je eine Provinz aus ihrer Liste genommen wurde. `it.fails` wäre der kürzere und der falsche Weg gewesen. Nebenbefund: **`data/geo/` fehlt im Baum** (`.gitignore`), `pnpm map:build` läuft heute nicht — für T-M19-02 ist das zu entscheiden. 1440 Tests grün |
| T-M19-02 | 2026-09-07 | **Eine Provinz darf mehrteilig sein — und Kalifornien ist wieder da.** `polygon` heißt jetzt `polygons`, umbenannt statt danebengelegt, und der Generator wählt keinen Umriss mehr aus, sondern nimmt **alle**. Ergebnis gemessen: **237 → 2165 Ringe, 69 042 → 97 463 Punkte (+41,2 %), 85,78 % → 100,00 % der Landfläche**. G1, G2 und G3 sind grün mit **leeren** Übergangslisten, ohne dass eine Schwelle weicher wurde. Ein Klick auf Anchorage und einer auf Los Angeles wählen beide `USA-WEST`; vorher lieferte der zweite `null`. Zeichenzeit an derselben Funktion gegen beide Datenstände gemessen: Weltansicht p95 **1,76 → 2,83 ms**, nah heran **1,42 → 3,52 ms**, teuerster Einzelfall 4,43 ms — gegen 16,7 ms Budget, Faktor 4,7 Luft | **Zwei Dinge kamen anders als geplant, beide gemessen statt geraten.** *Erstens die Filterschwelle:* der Bauplan schlägt 25 px² vor und nennt 99,79 % Fläche bei +26 % Punkten — beides trifft zu, aber dabei blieben **38 Provinzen unter der 99-%-Schwelle**, die dieselbe Aufgabe grün verlangt. Die beiden Zusagen sind unvereinbar. Verworfen wird jetzt genau, was **nach dem Runden auf ganze Bildpunkte keine Fläche mehr hat** — 1228 der 3393 Ringe, Kosten 0,000 % Land, und G2 wird grün. *Zweitens der Ankerpunkt:* G1 wäre auch mit allen Ringen nicht grün geworden, weil `shapeCentre` die Randpunkte mittelt und bei einer Küste wie Norwegens im Wasser landet — neun Provinzen trugen ihre Armeemarke außerhalb ihrer selbst. Der Anker ist jetzt die Mitte der längsten waagerechten Sehne, innen **von der Konstruktion her**. Weil `data/geo/` nicht im Baum liegt, entsteht `world.json` über `scripts/reproject-map.mjs` aus `world-shapes.json` — es ändert **nur** `polygons` und `center` und reicht jedes andere Feld unverändert durch: ein voller `map:build` würde die Anreicherung neu würfeln und damit Bevölkerung, Gelände und Vorkommen, also AK-1. Keine zweite Wahrheit — Projektion und Ringauswahl kommen aus `project.ts`, `--check` belegt Idempotenz. Der Test, der beißen musste, hat gebissen: beide Ankertests fallen, wenn man die Reparatur wegnimmt. 1454 Tests grün |
| T-M19-03 | 2026-09-07 | **Grönland bleibt auf der Leinwand.** Der 78°-Beschnitt legte fest, welchen Streifen der Welt die Karte zeigt — geklippt wurde nie dagegen. Nach T-M19-02 lagen **4087 von 97 463 Punkten** neben dem Bild, verteilt auf fünf Provinzen (`CAN-NORTH` 1527, `GRL` 997, `NOR` 607, `RUS-NW` 546, `RUS-SIB` 410). Jetzt klippt `drawableRings` mit Sutherland–Hodgman gegen `[0,4000] × [0,2400]`: **0 Punkte außerhalb**, 2165 → 2070 Ringe, 97 463 → 93 402 Punkte. G4 ist grün mit leerer Liste | **Der Bauplan sagt, ein Test allein fange das Zusammenfalten nicht — er tut es, wenn man ihn an der richtigen Zahl aufhängt.** `Math.max(0, y)` legt die ganze Nordküste auf eine gerade Linie, und das ist messbar: der Anteil der Punkte, die **genau** auf der Leinwandkante liegen. Richtig geklippt sind es bei Grönland 7 von 3497 (0,2 %), gefaltet wären es alle 796. Der Wächter zieht die Grenze bei 10 % und wurde gegen die falsche Fassung geprüft: `CAN-NORTH: 1089 von 8839 Punkten` — er fällt. Die Alternative aus dem Bauplan (den Beschnitt nach Norden schieben) ist **gemessen und verworfen**: bei 83,7° passt alles hinein, aber die Skala fällt von 11,96 auf 10,00 Bildpunkte je Grad — die ganze Welt 16 % kleiner, damit 4 % davon sichtbar werden, und alle 237 Ankerpunkte verschieben sich. 1460 Tests grün |
| T-M19-04 | 2026-09-07 | **Südostaustralien ist anklickbar — und die Diagnose des Bauplans war die falsche.** `AUS-SE` galt als „spielbare Provinz von 39 px², zu klein zum Anklicken". Gemessen an der fertigen Karte stimmt der Schluss nicht: **Singapur hat 4 px², Bahrain 5, Malta 6, Hongkong 13 — alle vier anklickbar.** `AUS-SE` hat **43** und war es nicht. Nicht die Größe war der Fehler, die **Überdeckung**: Hauptstadtterritorium und Jervis Bay liegen in New South Wales, dessen Quellumriss kein Loch dafür hat, und `pickProvince` nahm den ersten Treffer — `AUS-NE` steht vorn. Anklickbar war die Provinz nur auf der Macquarie-Insel: **5 Bildpunkte, 331 südlich des Landes**. Jetzt gewinnt bei zwei Umrissen über demselben Punkt der **kleinere**; auf der ganzen Karte ist genau **1 von 196 196** Rasterpunkten doppelt beansprucht, die Regel greift also fast nie. 16 µs je Klick über 20 000 Klicks gemessen — 0,1 % eines Bildbudgets | **Der vom Bauplan verlangte Wächter wäre eine Falschmeldungsmaschine geworden.** „Mindestgröße einer spielbaren Provinz" hätte Singapur, Bahrain, Malta und Hongkong gemeldet und `AUS-SE` durchgelassen — er hätte auf die eine Zahl geschaut, die nichts erklärt. Geprüft wird stattdessen die Eigenschaft, um die es geht und die der Bauplan selbst nennt: **der Ankerpunkt jeder Provinz wählt diese Provinz.** Gegen die alte Fassung nachweislich rot: `AUS-SE -> AUS-NE`. Die Partie ist **unberührt** — 52 097 Einwohner, zwei Vorkommen, drei Kanten, Platz in Australiens Startaufstellung bleiben; kein neuer `sim:fullgame` nötig. Offen und in PROBLEME.md vorgelegt: der **Name** trägt weiter nicht, was er behauptet (Victoria und NSW stecken in `AUS-NE`), und das zu ändern hieße, die Anreicherung neu zu würfeln. Empfehlung dort: umbenennen. 1462 Tests grün |
| T-M19-02 (Nachtrag) | 2026-09-07 | **Die Zeichenmessung im Browser hat gefunden, was Node nicht zeigte.** T-M19-02 hatte das Bildbudget in Node belegt (p95 2,83 ms gegen 16,7). Im Browser, an einem echten 2D-Kontext mit echtem Zeichnen, stieg p95 von **13,5 auf 22,5 ms** — 2070 Formen statt 237, und jede kostet Pfad, Füllung und Strich, auch wenn sie einen halben Bildpunkt bedeckt. Neu ist `worthDrawing`: ein Umriss, dessen längere Seite auf dem Schirm unter 1,5 px fällt, bekommt keinen Pfad. Das ist dasselbe Maß, das `thin` für Punkte benutzt. **901 statt 2070 Formen** in der Weltansicht, Node-p95 **2,95 ms**, alle 237 Provinzen weiterhin gezeichnet | **Der Filter hatte prompt die Gegenrichtung des Fehlers, den er begleitet.** Malta und Singapur sind bei Weltansicht unter einem Bildpunkt breit — *jeder* ihrer Umrisse fiel durch die Schwelle, und beide Provinzen verschwanden von der Karte. Eine Provinz, die der Spieler besitzt und nicht sieht, ist genau der Befund vom 2026-09-07, nur von der anderen Seite. Jetzt zeichnet jede Provinz mindestens ihren größten Umriss, egal wie klein er ist; der Test nennt beim Wegnehmen der Rettung `auf der Weltkarte nicht gezeichnet: MLT, SGP`. **Zur Ehrlichkeit der Messung:** dieselbe Vorher-Fassung liefert in dieser Sitzung p95 13,5 ms, in T-M16-06 aber 3,0 ms — die Umgebung ist vier- bis siebenmal langsamer und schwankt zwischen Läufen um mehr, als der gemessene Effekt groß ist. Belastbar ist deshalb nur der **relative** Vergleich; die absolute Zahl steht in Node. 1468 Tests grün |
| T-M19-05 | 2026-09-07 | **Der Bericht sagt, was die Karte zeigt — mit Bild.** `docs/reports/map-geometry.md` führt jede der vier Prüfungen vorher/nachher, gegen beide Commits gestempelt: **G1 14→0, G2 66→0, G3 3→0, G4 1→0**, Landfläche **85,78 % → 100,00 %**, Umrisse 237→2070, Punkte 69 042→93 402. Dazu zwei Abbildungen: Nordamerika vor und nach der Reparatur, und Grönland vor und nach dem Beschnitt. Der Eintrag in `PROBLEME.md` vom 2026-09-03 ist an Ort und Stelle berichtigt — er stellte über genau diese vier Provinzen fest, „die Karte zeichnet richtig" | **Die Abbildungen sind gezeichnet, nicht abfotografiert** (`scripts/map-figure.mjs` aus den Kartendateien selbst). Ein Bildschirmfoto ließe sich nicht vergleichen, brächte die halbe Oberfläche mit und niemand wüsste später, aus welchem Stand es stammt; ein SVG ist Text, also versionierbar, und die Farben kommen aus `tokens.ts`. **Der berichtigte Eintrag ist die eigentliche Lehre von M19:** der Satz war für `world-shapes.json` wahr und für `world.json` falsch, und er sagte nicht, welche er meint. Er hat den Fehler vier Tage gedeckt, bis Noah ihn *sah*. Sobald es zwei Dateien gibt, muss eine Aussage über „die Karte" sagen, welche sie meint — und seither hält ein Wächter die beiden gegeneinander. 1468 Tests grün, **M19 vollständig** |
| T-M21-06 | 2026-09-07 | **Drei Auskünfte, die falsch waren oder nie ankamen — alle drei mit einem Test, der gegen den alten Stand fällt.** (a) `title` nahm `disabledReason ?? hint`, und bei einem gesperrten Knopf gewann der Grund: der Spieler erfuhr, *dass* es die Fabrik erst ab Tag 8 gibt, nie *was sie kostet*. Jetzt beides, und ein Glied, das der Grund schon sagt, fällt weg — sonst stünde derselbe Spieltag zweimal da. (b) „Truppen dürfen die Grenze nicht überschreiten" war falsch: `planRoute` ruft `findPath` **ohne** `canEnter`. Neuer Text: einmarschieren ja, behalten erst im Krieg. (c) „Es gibt nichts zurück" war falsch: `CANCEL_REFUND_PERMILLE = 500`, die Hälfte kommt zurück | **Der Test zu (b) hätte beinahe nichts geprüft.** Erste Fassung ließ eine Armee direkt in die Nachbarprovinz marschieren — und `findPath` lässt das **Ziel** immer betreten (`if (next !== to && !canEnter(next))`), damit ein Angriff überhaupt befohlen werden kann. Der Test wäre auch mit `canEnter` grün gewesen. Erst ein Weg **mitten durch** fremdes Gebiet (n2→m1→o3) beißt; die Bissprobe meldet dann `gar kein Weg nach o3`. **Zu (c) steht die Grenze des Wächters ausdrücklich in PROBLEME.md:** er hält *eine* Aussage gegen *eine* Konstante. Die allgemeine Klasse „die Anleitung widerspricht den Regeln" bleibt ungeprüft — eine Regel „jede Zahl aus `constants.json` muss stimmen" schlägt bei jeder Jahreszahl an, wird nach dem dritten Fehlalarm abgeschaltet, und hätte **genau diesen Fall nicht gefangen**: die Konstante steht in `build.ts`, nicht in `constants.json`. Beim Berichtigen der Anleitung fiel eine Behauptung an, die ich erst geprüft habe, bevor sie stehen blieb: fällt die Provinz, verfällt der Auftrag **ohne** Erstattung (`construction.ts`, `reason: 'ownerChanged'`, kein `refundCost`). 1479 Tests grün |
| T-M20-01 | 2026-09-07 | **Die zugesagten Symbole gibt es jetzt.** Elf neue Pfade in `icons.tsx`: sechs Beziehungen (`peace` als haltende Grenze, `war` als durchbrochene, `truce` als Fahne, `alliance` als zwei Ringe, `rightOfWay` als Durchgang, `sharedMap` als Auge) und fünf Geländearten (Bergkette, Horizont mit Halmen, Düne mit Sonne, zwei Nadelbäume, Häuserzeile). Dazu `RELATION_ICONS` und `TERRAIN_ICONS` — beide als `Record<Typ, IconName>`, sodass der **Compiler** einen neuen Zustand meldet, statt ihn still ohne Zeichen zu lassen. Eingebaut in Provinzansicht und Diplomatietabelle, jeweils **vor** dem Wort, das stehen bleibt | **Der Wächter prüft gegen die Quellen, nicht gegen sich selbst:** Gelände gegen die Geländearten der ausgelieferten `world.json`, Beziehungen gegen die Schlüssel, unter denen `de.ts` sie erklärt. Gegen den Stand vor dieser Aufgabe war er rot (`Gelaende "desert" ohne Symbol`, `Beziehung "peace" ohne Symbol`). Dazu zwei neue Prüfungen: kein Gelände und keine Beziehung teilt sich ein Zeichen mit einer Einheit oder einem Gebäude (die Falle von Playtest-Befund 30), und **jeder** Pfad des Satzes bleibt im 24×24-Feld. **Und der Nachweis, dass sie auch erscheinen**, hängt am gerenderten Baum — die Lehre aus T-M21-06, wo ein Datentest grün blieb, während der Text auf dem Weg zum Bildschirm verlorenging. Nebenbei: `VisibleProvince.terrain` war als `string` deklariert, obwohl der Wert immer ein `Terrain` ist; jetzt trägt die Sicht den echten Typ und die Oberfläche muss beim Nachschlagen nichts mehr behaupten. 1486 Tests grün |
| T-M20-02 | 2026-09-07 | **Jede Macht hat ein Gesicht.** Die Kartenfarbe steht jetzt neben dem Namen — in der Lage, in der Diplomatie und in der Provinzansicht beim Besitzer. Eine gemeinsame Komponente `NationName` trägt das Feld; die Farbe kommt als `style={{ background }}` aus den **Daten** (kein Farbliteral, die Lint-Regel zu R-UI-02 bleibt unberührt), die Form steht in `app.css` und benutzt nur `var(--*)`. Im Bild geprüft: die Felder stimmen mit der Karte überein — die USA gold, Kanada rosa | **`view.self` trug gar keine Farbe.** `others[]` führt sie seit M6; die eigene fehlte, weil die Karte den eigenen Besitz über `playerId` einfärbt und keine Liste braucht — in einer Tabelle der Mächte hätte damit jede Zeile ein Farbfeld getragen außer der eigenen. Jetzt trägt `self` sie auch. **AK2 hat einen eigenen Test**, und er prüft beide Richtungen: der Name steht neben dem Feld *und* das Feld selbst ist für Vorleseprogramme unsichtbar und trägt keinen Text — sonst läse ein Screenreader jede Macht zweimal. Das **Protokoll bleibt ausgenommen, mit Begründung in DECISIONS.md**: eine Protokollzeile nennt meist zwei Mächte („Ostmark erklärt Nordland den Krieg"), ein Feld davor müsste sich für eine entscheiden, und ein zweideutiges Zeichen ist schlechter als keines. Der Weg dorthin wäre, die Namen **im Satz** einzufärben — das ist eine Umstellung von `describeEvent`, keine Farbfrage. 1489 Tests grün |
| T-M20-03 | 2026-09-07 | **Drei Größen, die das Spiel längst ausrechnet, stehen jetzt auch da.** Die **Rubrik** einer Protokollzeile (`categoryOf` sortiert seit M13, sichtbar war es nur mit gedrücktem Filter) als Symbol vor dem Text; die **vorherrschende Gattung** einer Armee (`dominantIcon`, seit M13 für die Kartenmarke gerechnet) in der Armeeliste; das **Rohstoffzeichen** in der Wirtschaftstabelle — der letzten Stelle, die es noch nicht trug. Alle drei am gerenderten Baum geprüft und einzeln zum Fallen gebracht | **`other` bekommt bewusst kein Zeichen.** Ein Symbol für „nichts davon" wäre eine Auskunft, die keine ist — und in einer Spalte lauter gleicher Zeichen findet das Auge die Ausnahme nicht mehr. Ein eigener Test hält das fest. **Der Markt bleibt ausgenommen, mit Preisschild in DECISIONS.md:** er nutzt `<select><option>`, und ein `<option>` kann kein SVG tragen — das ist eine Festlegung des Browsers, keine Stilfrage. Der einzige Weg dorthin wäre eine selbstgebaute Liste, und die müsste Tastaturbedienung mit Pfeilen und Anfangsbuchstaben, die Rolle für Vorleseprogramme und das Rad des Touchgeräts einzeln nachbauen. **R-UI-06 verlangt Bedienung ohne Maus** — das gegen ein Bildchen zu tauschen, entscheidet Noah, nicht ich. Nebenbei erweitert: `ownArmiesIn` liefert das Gattungszeichen mit, statt es die Oberfläche ein zweites Mal ausrechnen zu lassen. 1493 Tests grün |
| T-M21-01 | 2026-09-07 | **Die Texte der Führung stehen in der Sprachdatei.** Fünf Schritttitel, fünf Schritttexte und die Fortschrittszeile („Schritt 2 von 5", die als Template-Literal im Quelltext stand) sind nach `de.ts` gezogen; `TUTORIAL_STEPS` bezieht sie über `t()`. Neu ist der Wächter `test/guards/prose-in-code.test.ts` — die **Gegenrichtung** zu `text-keys.test.ts`: nicht nur, ob jeder abgefragte Schlüssel existiert, sondern ob es Text **ohne** Schlüssel gibt. Er fällt gegen die alte Fassung mit genau fünf Funden, geprüft an einer eingefrorenen Kopie unter `fixtures/violating/` | **Die erste Fassung der Regel war eine Falschmeldungsmaschine, und das Messen hat sie gerettet.** Bei „vier Wörter, egal wie getrennt" meldete sie 60 Treffer — 38 SVG-Pfade, Schriftlisten, CSS-Selektoren, Formatvorlagen. Gezählt wird jetzt nach **Leerzeichen** und nur, was aus reinen Buchstaben besteht: eine Kennung wie `tutorial.steps.select.title` hat keine Leerzeichen, ein SVG-Pfad keine Wörter. Übrig blieben zwei Fälle, beide berechtigt ausgenommen: ein CSS-Wähler (`GUARD-ALLOW` in derselben Zeile) und eine `console.error`-Meldung — die erreicht keinen Spieler und gehört gerade **nicht** in die Sprachdatei. **Was die Regel nicht kann, steht in ihrem eigenen Kopf und in PROBLEME.md**: Text mit drei Wörtern, Text zwischen JSX-Marken, und alles außerhalb von `game/tutorial.ts` und `ui/*.tsx` — dort ist eine Stelle namentlich genannt (`createStorage.ts`). Beim Bauen selbst gestolpert: der `GUARD-ALLOW`-Marker steht in einem Kommentar, und die Kommentarentfernung hatte ihn verschluckt, bevor die Ausnahme greifen konnte. 1500 Tests grün |
| T-M21-02 | 2026-09-07 | **Die Führung folgt dem Spiel, nicht der Knopfleiste.** `completesOn` kannte fünf Oberflächenereignisse — die Einstiegshilfe war durch, bevor der erste Spieltag vorbei war, und schwieg genau dann, wenn zum ersten Mal etwas geschieht. Jetzt kommen `buildCompleted`, `unitRecruited`, `provinceCaptured` und `dayPassed` dazu, gespeist aus `state.eventLog` über dieselbe Bauart wie der Ton daneben. Drei neue Schritte in der Reihenfolge, die ein neuer Spieler erlebt: erst läuft die Uhr, dann steht das Gebäude, dann marschiert die erste Einheit — fünf Schritte wurden acht | **Der Test hat einen Fehler gefunden, den ich gerade selbst gebaut hatte.** Erste Fassung arbeitete alle Ereignisse eines Ticks ab: bei „Einheit fertig, Tag vorbei, Bau fertig" ging die Führung **zwei** Schritte auf einmal — sie hätte einen Text gezeigt und im selben Bild weggeschoben. Jetzt gibt `advanceOnce` **höchstens einen Schritt je Bild** zurück und sagt mit `consumed`, wie weit der Strom verbraucht ist; der Rest wird beim nächsten Durchlauf wieder angeboten, es fällt also nichts weg. Drei Tests halten die drei Hälften fest: einer je Bild, nichts verloren, und bei lauter unpassenden Ereignissen wird alles verbraucht (sonst böte der Aufrufer denselben Strom endlos an). **Die Falle des Bauplans hat eine eigene Prüfung:** die Verdrahtung kann fehlerfrei sein und trotzdem nie greifen, wenn `DAY_REPORT` das Protokoll nicht erreicht — ein ganzer Spieltag in einer echten Partie belegt, dass es das tut. **Nebenbefund, echter Fehler:** `soundedUpTo` wurde nie zurückgesetzt — nach einer zweiten Partie blieb der Ton stumm, bis die Ereigniszahl den alten Stand überholt hatte. Beide Merker setzen sich jetzt zurück, wenn das Protokoll kürzer wird. 1506 Tests grün |
| T-M21-03 | 2026-09-07 | **Die Führung nennt das Warten beim Namen.** Der vierte Schritt sagt jetzt aus, was der Playtest am 2026-09-07 als Aussteigepunkt gefunden hat: der Startvorrat trägt genau ein Gebäude, und bis die erste Einheit steht, vergehen **1,8 Tage — Spieltag 2**. Beide Zahlen kommen zur Laufzeit aus den Regeln (`firstUnitAt`), keine steht im Satz. Dafür tragen die Schritte nur noch ihre Kennung; `Tutorial.tsx` löst Titel und Text über `tutorial.steps.<id>.*` auf und setzt die Platzhalter ein — eine Konstante, die beim Laden des Moduls ausgewertet wird, kennt die Regeln der laufenden Partie nicht | **Der Bauplan hat mich vor einem Fehler bewahrt, den ich schon gebaut hatte.** Meine erste Fassung rechnete beide Dauern mit `buildDuration` und kam auf **41** Ticks; der Bauplan sagt 43. Nachgerechnet: Bauen und Ausheben skalieren die Moral **verschieden** — `buildSpeedFactor` ist auf 80 000 normiert, `recruitSpeedFactor` erst auf 100 000. Bei der Startmoral 70 000 sind das 900 gegen 760 Promille, also 27 + 16 statt 27 + 14. Hätte ich die Zahl nicht gegen den Bericht geprüft, hätte mein eigener Test die falsche zementiert. Zwei Prüfungen halten das jetzt fest: eine nennt beide Kurven mit ihren Werten, eine nennt die 43 gegen die ausgelieferten Regeln. Dazu ein Test, dass die Vorlage **keine** feste Stundenzahl enthält — sonst wäre sie beim ersten Balancing falsch, und gerade die Führung liest jemand, der die Regeln noch nicht kennt und ihr deshalb glaubt. 1514 Tests grün |
| T-M21-04 | 2026-09-07 | **Jede Freischaltung wird gemeldet, wenn sie kommt.** Siebzehn Sachen schalten sich über sechzehn Spieltage frei — Hafen an Tag 2, Fabrik an Tag 8, Raketenartillerie an Tag 16 —, und **gesagt hat das Spiel es nie**: wer nicht von sich aus täglich die Bauliste durchsah, erfuhr von der Werft, wenn er sie zufällig brauchte. Jetzt steht am Morgen des Tages eine Meldung in der Alarmleiste, mit dem Symbol der Sache und ihrem Namen, genauso lange sichtbar wie eine Fertigstellung. Der Wächter `test/guards/unlocks-explained.test.ts` bindet das: jede Sache mit `availableFromDay` wird an ihrem Tag gemeldet, hat einen Erklärtext hinter dem Fragezeichen **und** einen deutschen Namen | **Gemeldet wird aus den Regeln, nicht aus einem Ereignis.** Der Kern kennt keine Freischaltung als Vorgang — sie ist bloß ein Vergleich zweier Zahlen. Die Oberfläche kann denselben Vergleich anstellen, und das ist erheblich billiger als ein Ereignis, das der Kern führen, speichern und beim Laden wiederherstellen müsste; die Partie bleibt unberührt. **Der Wächter ist der eigentliche Ertrag: er ist heute grün und wird morgen fallen.** Nachgewiesen, indem eine Einheit `paratrooper` in `units.json` gelegt wurde — er meldet sofort `ohne Erklaerung: explain.units.paratrooper` und `ohne Namen: units.paratrooper`. Dazu zwei Gegenproben, damit er nicht bloß immer meldet: an einem Tag ohne Freischaltung schweigt er, und nach den ersten Stunden des Tages verschwindet die Meldung wieder — sonst stünde sie vierundzwanzig Stunden in der Liste und verdrängte, was gerade wirklich Aufmerksamkeit braucht. Die Ausnahmeliste ist **leer**, und wer etwas darauf setzt, muss es begründen: ein eigener Test verlangt mindestens einen Satz. 1522 Tests grün |
| T-M21-05 | 2026-09-07 | **Der Durchgang ist gemessen, nicht behauptet.** `docs/reports/onboarding.md` protokolliert einen echten Lauf über **16 Spieltage**: welcher Schritt wann kam, welche Freischaltung wann gemeldet wurde, und wo die längste Pause liegt. Der gedachte Spieler ist absichtlich fleißig — klickt sofort, baut sofort, hebt sofort aus —, damit der übrige Leerlauf der ist, den das Spiel **erzwingt**. Ergebnis: 4 Führungsschritte, 17 Freischaltungsmeldungen, erste Einheit an Tag 2, 19:00. Dazu zwei Fragen im Playtest-Bogen (49, 50): *wusste ich, was zu tun ist* und *wusste ich, wozu* | **Die Messung hat einen Befund geliefert, den niemand gesucht hat: die längste Pause ist 72 Ticks — drei volle Spieltage zwischen der Eisenbahn an Tag 5 und der Fabrik an Tag 8.** Dazwischen meldet das Spiel nichts, was den Spieler anspräche; die Führung ist da längst durchgelaufen. **Ob das zu lang ist, ist eine Balancing-Frage und gehört Noah** — der Bericht nennt drei Wege mit ihren Kosten und empfiehlt keinen. Der Test hält die Zahl als **Obergrenze** fest, nicht als Ziel: eine Grenze, die das Spiel heute reißt, wäre entweder eine stillschweigende Aufforderung, den Test weicher zu machen, oder eine Anforderung, die niemand gestellt hat. Was er dagegen wirklich prüft: die ersten beiden Spieltage — dort, wo der Einsteiger aussteigt — bleiben nicht leer. Der Antwortbogen ist neu erzeugt und meldet 60 von 62; die zwei neuen Fragen sind offen, weil nur Noah sie beantworten kann. 1522 Tests grün |
| T-M20-04 | 2026-09-07 | **Die offene Hälfte von R-UI-04 ist zu.** Die Anforderung verspricht „Bewegungs- **und** Kampfanimationen"; der Ring um ein Gefecht atmet seit T-M13-16, eine marschierende Armee dagegen klebte in der Provinzmitte und stand im nächsten Bild ohne Übergang in der nächsten. `marchPoint` setzt sie jetzt zwischen die Provinzen, nach dem Anteil des Weges, den die Sicht ohnehin führt (`path`, `departureTick`, `arrivalTick`). Die Animationsschleife läuft seither auch für Märsche — aber weiterhin nur, wenn es einen Anlass gibt. **Gemessen** (unter 30 % Fremdlast, also konservativ): `markersFor` ohne Bewegung p95 0,038 ms, mit 0,033 ms — **kein messbarer Aufschlag**; Flächen und Bewegung zusammen p95 **4,57 ms**, Median 3,42 ms gegen 16,7 ms Budget | **Der Entwurf ist der eigentliche Ertrag, nicht die Bewegung.** `MapCanvas` ist die am schlechtesten abgedeckte Datei der Oberfläche (168 von 249 Zeilen), weil sie ohne Leinwand nicht läuft — jede Zeile, die dort landet, ist faktisch ungeprüft. Die Bewegung ist deshalb eine **reine Funktion** in `markers.ts`: der Anteil des Weges ist Arithmetik, und Arithmetik kann ein Test festnageln. Sechs Prüfungen decken sie ab, und die Bissprobe (Anteil auf 0) lässt drei davon fallen. `MapCanvas` bekam zwei Zeilen. **Die Funktion springt in jedem Zweifelsfall auf die Provinzmitte zurück** — fehlende Marschangabe, Ziel ohne Mittelpunkt, Marsch ohne Dauer (dort würde durch null geteilt), Start ohne Mittelpunkt. Eine Armee an einem erfundenen Zwischenort wäre schlimmer als eine, die nicht wandert: der Spieler klickt dorthin, wo nichts ist. Die zwei gebauten Bremsen sind unangetastet — ohne `tick` steht alles still, und `motionAllowed` entscheidet, ob einer mitkommt. 1528 Tests grün, **M19, M20 und M21 vollständig** |
| *AK-1 nach M19–M21* | 2026-09-07 | **Die Partie ist bitgleich — nachgefahren, nicht behauptet.** M19 hat `world.json` angefasst: `polygon` wurde `polygons`, 237 Umrisse wurden 2070, neun Ankerpunkte haben sich verschoben. Der Bauplan versprach, dass das die Simulation nicht berührt („keine Migration, kein Golden Master, kein Schemaschritt"), und `pnpm sim:fullgame` belegt es jetzt: **Sieg an Spieltag 876, Sieger p6, 2025 Eroberungen, 11 Kriegserklärungen, 8113 Gefechte** — jede Zahl identisch mit dem Lauf vom Morgen. Im Diff der Messdatei steht **nur der Zeitstempel** | Der Beleg war billig zu haben und wiegt schwer: ein Lauf über 876 Spieltage mit acht Mächten ist deterministisch, also von der Maschinenlast unabhängig — er ließ sich fahren, während `pnpm acceptance` es nicht durfte. Und er beantwortet die einzige Frage, die bei einer Kartenänderung wirklich zählt: **liest der Kern irgendwo, was ich angefasst habe?** Die Antwort steht jetzt als Zahl da, nicht als Argument |

## Meilenstein M22 — Die Oberfläche hält, was der Kern rechnet

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M22-01 | 2026-09-07 | **Das Protokoll spricht in ganzen Zeilen.** Befund V2-01: jeder Eintrag brach in einer ~90-px-Spalte um. Ursache war nicht die Breite der Leiste, sondern das Raster der Zeile: `.log__row` deklariert zwei Spuren (Zeit, Text), und seit dem Rubriksymbol aus T-M20-03 trägt eine Zeile drei Rasterkinder — der Text rutschte in die zweite Rasterzeile und erbte dort die 72 px der Zeitspalte. Symbol und Text teilen sich jetzt eine Spur (`.log__entry`, flex). Anders als geplant lag die Reparatur damit in `Panels.tsx` + `app.css`, nicht in `App.tsx`. Der Wächter lädt das echte Stylesheet in jsdom (jsdom rechnet kein Layout, wendet aber die Kaskade an) und bindet: höchstens so viele Kinder wie Rasterspuren, letzte Spur `fr`, Zeitspalte fest — er fiel vor der Reparatur | grün (App+Panels: 94 Tests) |
| T-M22-02 | 2026-09-07 | **Die Seitenleiste kriecht nicht mehr seitwärts.** Befund V2-02: die sechste Spalte „In Auftrag" machte die Wirtschaftstabelle breiter als die Leiste, und weil die Leiste seitlich scrollte, verschoben sich ALLE Panels — abgeschnittene Armeeknöpfe, Überschriften ohne ersten Buchstaben. Die Spalte ist jetzt ein Zeichen (neue Sanduhr `queue` in `icons.tsx`) mit Zahl hinter dem Bestand, nur wenn etwas in Auftrag ist; die Textfassung steht im Titel (`economy.committedTitle`). Die Leiste trägt `overflow-x: hidden`. Der Wächter bindet beides gegen das echte Stylesheet; die wörtliche `scrollWidth`-Prüfung ist in jsdom leer — Begründung und Ersatz in DECISIONS.md | grün (Panels+App: 97 Tests) |
| T-M22-03 | 2026-09-07 | **Was mich betrifft, sieht anders aus.** Befund V2-07: der Fall der eigenen Großstadt hatte dieselbe optische Stimme wie „Vietnam ist gefallen". `isSelfSetback` in `events.ts` ordnet jede Kernart zu — eng, damit das Signal selten bleibt: eigener Provinzverlust, Aufstand im eigenen Land, eigene Hauptstadt, eigenes Ausscheiden; eine eigene Eroberung ist kein Rückschlag. `EventEntry.self` → Klasse `log__row--self` (Zinnober-Balken links, fett). Der Test läuft über ALLE `EVENT_TYPES` des Kerns, plus Gegenproben (fremdes Unglück, kein Betrachter, Eroberer statt Verlierer) und die Klasse am gerenderten Baum. `App.tsx` brauchte keine Änderung — der Betrachter steckt seit T-M15-09 im `naming` | grün (events+Panels+App: 123 Tests) |
| T-M22-04 | 2026-09-07 | **Start mit Gesicht, Menü mit Wegen, Weiterspielen mit einem Klick.** Befunde V2-03/04/05. (1) Der Startdialog trägt eine Titelzeile — Spielname, Untertitel, „Fassung 0.1.0" aus package.json (keine zweite Wahrheit im Text). (2) Existiert ein Stand, ist „Weiterspielen (Tag N)" der ERSTE Knopf; `latestSlot` in saves.ts wählt den am weitesten gespielten Stand (der Umschlag trägt keine Wanduhrzeit — dokumentiert im Code). (3) „Menü" öffnet jetzt ein Menü mit drei Wegen (Neue Partie / Spielstände / Einstellungen), auch aus der laufenden Partie; der Startdialog ist dafür EINMAL beschrieben (`newGameDialog`) und hängt nicht mehr allein hinter dem Frühausstieg. Drei Bestandstests gehen jetzt den Weg Menü→Einstellungen | grün (Dialogs+App+saves: 79, Desktop+Guards: 586 Tests) |
| T-M22-05 | 2026-09-07 | **Jeder Befehl quittiert; eine stehende Uhr sagt es.** Befunde V2-08/09. Überraschung beim Nachprüfen: die vom Entwurf vorausgesetzten `pendingCommands` der Hülle gab es nicht — `send` rechnete pro Befehl einen ganzen Tick (ein Klick bei Pause bewegte die Uhr um eine Stunde, samt KI). Jetzt sammelt die Hülle: geprüft sofort (`canApply` → Absage jetzt), angewendet im ersten Tick des nächsten Laufs (Uhr oder Vorspulen, `FastForwardRequest.playerCommands`); der auslösende Knopf zeigt „✓ befohlen — wirkt beim Weiterlaufen." und ist bis dahin gesperrt (Entscheidung in DECISIONS.md). Und die ehrliche Uhr: läuft trotz Tempo >2 s kein Tick (verdecktes Fenster, stehendes rAF), zeigt die Kopfleiste „Pausiert" als role=status. Testfalle dokumentiert: jsdoms rAF hängt an setInterval — unter falschen Uhren muss rAF stummgeschaltet werden, sonst treibt der Test die ganze Spielschleife an | grün (Desktop+Guards: 594 Tests) |
| T-M22-06 | 2026-09-07 | **Knöpfe sagen, was sie tun; Armee-Marker sind Klickziele.** Befunde V2-13/14. (1) `ActionSpec.aria`: sichtbar bleibt „Kaserne", hörbar wird „Kaserne bauen" (aria-label, Kosten bleiben im title); Ausheben, Haltungen („Haltung Angriff einnehmen") und „Feuer frei" → „Feuer freigeben" ebenso — alle anderen Beschriftungen SIND schon Verbsätze. Der a11y-Wächter läuft über ALLE Aktionen aus actions.ts gegen eine Verbliste; sieben Bestandstests fragen jetzt nach dem hörbaren Namen. (2) `pickArmy` in markers.ts: Trefferfläche `ARMY_HIT_BOX` = 24 px (Picking, nicht Zeichnung — der Kasten bleibt 20×14), mit derselben Ortsrechnung wie das Zeichnen (trifft auch marschierende Armeen), nur eigene; MapCanvas meldet die Armee vor der Provinz, App setzt die Auswahl — außer in der Zielwahl, dort bleibt jeder Klick Ortswahl | grün (Desktop: 525, Guards+Plan: 98 Tests) |

## Meilenstein M23 — Die Sprache wird fertig

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M23-01 | 2026-09-07 | **Umlaute kehren zurück, ein Wächter hält die Tür.** Befund V2-10: zehn Spielertexte in `de.ts` trugen Ersatzschrift („haelt", „Staerke", „Haelfte", „gehoert", „Oberflaeche", „laeuft"), dazu zwei KI-Debug-Sätze („fuer das naechste Bauvorhaben" in economy.ts, „haelt Stellung" in military.ts) — anders als geplant lag in `actions.ts` selbst nichts mehr, die Tooltips wohnen seit M22 im Katalog. Der Wächter (in `text-keys.test.ts`) prüft eine **Wortregel**, keine Zeichenregel: ae/oe/ue nur nach Konsonant oder am Wortanfang ist Ersatzschrift — „bauen", „Feuer", „Neue", „genauer" bleiben zu Recht; echte Fugen wie „zuerst" (zu+erst) stehen auf einer Musterliste. Er lief nachweislich rot (alle zehn Funde) und prüft sich selbst gegen die alten Tooltip-Sätze. Ein Bestandstest (`Dialogs.test.tsx`) hing am alten Text /alles gehoert/ und geht jetzt mit echtem Umlaut | grün (voll: Kern 96,8 %, gesamt 95,4 %) |
| T-M23-02 | 2026-09-07 | **Grammatik: Genus, Dativ, Numerus, doppelte Namen.** Befunde V2-11/17, vier gemessene Fälle. (1) Genus: `de.grammar` trägt jetzt eine Genus-Tabelle je Gebäude und Einheit plus Akkusativpronomen — „Neu ab heute: Kaserne. Sie können **sie** jetzt bauen", der Hafen bekommt „ihn", das Jagdflugzeug „es"; `grammar.test.ts` hält die Tabellen vollständig gegen die Namenslisten. (2) Dativ: `durationDative` in format.ts („nach 2 **Tagen**"), von der Vorspul-Meldung in App.tsx benutzt. (3) Numerus: `pluralNations` — „Vereinigte Staaten **erklären**"; die Wahl läuft über die Endung `_PLURAL` am Ereignisschlüssel (dieselbe Konvention wie `_FOREIGN`), auch für Ausscheiden, Sieg und den Abschlussdialog. (4) V2-17: der Punktebalken der Lage-Tabelle rendert bei `labelHidden` keinen Textknoten mehr — der Name stand je Zeile zweimal („Indien Indien 6148"); fürs Ohr bleibt das aria-label. Anders als geplant lagen die Fundorte in Alerts.tsx/format.ts/App.tsx/Meter.tsx, nicht in tutorial.ts/fastForward.ts — die Plandateien nennen jetzt die echten Orte | grün (voll) |
| T-M23-03 | 2026-09-07 | **AUS-SE trägt einen ehrlichen Namen; der Markt sein Zeichen.** (1) „Südostaustralien" (Hauptstadtterritorium + Jervis Bay + Macquarie) heißt jetzt „Australisches Hauptstadtterritorium" — geändert in Quelle (`merge-rules.json`), Zwischenstand (`world-shapes.json`) UND Produkt (`world.json`) zugleich, weil der Kartenneubau die nicht eingecheckten Geodaten braucht (DECISIONS.md); `worldmap.test.ts` bindet die ganze Kette plus den unveränderten Zuschnitt (52 097 Einwohner, Vorkommen, drei Umrisse). Kein Golden-Master-Einfluss: kein Regelpfad liest Namen, kein schneller Test bindet einen Weltkarten-Hash an einen Festwert. (2) Der Markt behält sein `select` (T-M20-03 bestätigt) und zeigt daneben das Symbol des jeweils gewählten Rohstoffs, für beide Listen, wechselt mit der Auswahl (`.market__choice`) | grün (voll) |

## Meilenstein M24 — Die Tage bekommen Inhalt

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M24-01 | 2026-09-07 | **Der Tagesbericht bekommt einen Körper.** Befund V2-06: „Tagesbericht für Tag 8." war eine Überschrift ohne Körper. `dayReportBody` in `events.ts` — rein, ohne Gedächtnis — formt aus Sicht und Regeln vier Absätze: Bilanz je Rohstoff (nur ≠ 0), Moral je eigener Provinz mit Richtung (↗↘→ gegen die Zielmoral), fertige (aus den Ereignissen des Tages) und laufende Aufträge mit Fertigtag, „Morgen neu: X" aus der Freischaltungsachse; ein leerer Tag sagt EINEN ehrlichen Satz. Kein neues Kern-Ereignis (D24.4): ein Effekt in `App.tsx` liest den Zustand am Tageswechsel (wie der Ton: nur frische Ereignisse) und heftet den Körper an den Protokolleintrag — beim Zeichnen gerechnet beschriebe er drei Tage später einen anderen Tag als seine Überschrift. `EventEntry.body` → `details/summary` im Protokoll (`Panels.tsx`), Deckel bei 60 Berichten. Anders als geplant kamen `Panels.tsx` und `app.css` dazu — der aufklappbare Eintrag lebt im Protokoll, nicht in der App. Falle: der Platzhaltername `value` riss den Umlaut-Wächter (ue nach Konsonant) — er heißt jetzt `percent` | grün (Desktop: 547, Guards: 134 Tests) |
| T-M24-02 | 2026-09-07 | **Das Wozu: Punkte, Moralstrafe, zwei neue Führungsschritte.** Frage 50, ehrlich beantwortet: das Was war geführt, das Wozu fehlte. Jeder Führungsschritt trägt jetzt hinter Titel und Text ein drittes Feld `why` in `de.ts` — den Satz, der ihn begründet (warum die Kaserne vor der Infanterie, warum das Warten kein Fehler ist); `Tutorial.tsx` rendert ihn als leisen Absatz. Zwei neue Schritte, beide BEVOR sie wirken: **`score`** noch im Klickblock (Platz 4 von 10) — „fast alle Punkte stecken in der Bevölkerung, eine Eroberung bringt mehr als jeder Ausbau" — endet, wenn die Lage der Mächte offen ist (neuer Auslöser `openStandings`, verdrahtet am geöffneten Panel statt an der Taste, damit L wie Kopfleiste zählen); **`expansion`** als letzter Schritt — „ab der {{third}}. Provinz kostet jede weitere {{penalty}} Punkte Zielmoral" — steht ab dem Klickblock (Tage vor dem frühesten Eroberungszug) und endet erst mit der ersten Eroberung (`provinceCaptured`, der Auslöser existierte seit T-M21-02 ungenutzt). Beide Zahlen kommen zur Laufzeit aus den Regeln, keine steht im Satz; die Größenordnung der Punktequellen steht bewusst als Verhältnis, sie hängt an der Karte. Der Wächter `unlocks-explained` verlangt das Wozu für jeden Schritt und die Lage beider neuer Schritte; der Durchgang (`onboarding.slow.test.ts`) macht vier Klicks statt drei | grün (Desktop + Guards: 637 Tests) |
| T-M24-03 | 2026-09-07 | **Das Kriegsmarsch-Paradox: gemessen und entschieden — 0,35 → 0,5.** Das Vorbild bremste den Ansager: fremder Boden ×0,7, feindlicher ×0,35, der schnellste Eröffnungszug war der unangekündigte Überfall (PROBLEME.md, V2-15). Headless-Vergleichslauf im Aufbau des Parameterlaufs (Weltkarte, Europa-6, alle ab Tick 0 im Krieg, 200 Spieltage, 12 feste Startzahlen je Variante): 0,5 nimmt dem Überfall die Hälfte des Tempovorteils (Faktor 2,0 → 1,4), verkürzt Kriege um ein Viertel (44,3 → 33,2 Tage bis zum Frieden), kein Eroberungssprung (514 → 444), kein Sieg-Tag-Sprung (keine Variante entscheidet in 200 Tagen); 0,7 wäre die stärkste Machtkonzentration (42,8 % Führungsanteil) ohne Verteidigervorteil. `hostileTerritoryFactor` 350 → 500; Zahlen in DECISIONS.md und `docs/reports/warmarch.json`, BALANCING.md trägt Zeile und Abschnitt. Golden-Master: `walkthrough.json` fiel nachweislich und wurde mit UPDATE_GOLDEN=1 bewusst neu erzeugt (Endstand inhaltlich gleich, `m1` gehört `p1`); `tiny-500.json` unberührt (keine Märsche im Lauf). Nebenwirkung benannt: im erzwungenen Sechserkrieg eine Überlebende weniger (5,0 → 4,0), Führungsanteil 34 → 39 % — gewollte Richtung, nahe der Rauschgrenze | grün (voll) |

## Abnahme — T-M12-03

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M12-03 | 2026-09-08 | **Der Abnahmelauf: 7 von 7 gegen den Endstand nach M22–M24.** `pnpm acceptance` mit Exit 0: verify 48 s, `test:slow` 6669 s, AK-1 als eigene Zeile — die Standardpartie (7 KI-Gegner, Weltkarte) wird an **Spieltag 798** entschieden, mit 2717 Eroberungen und 15 Kriegserklärungen (erster voller Lauf mit dem neuen Kriegsmarschfaktor 0,5). Anforderungs-Tor „V1 offen: 0", Abdeckung 95,4 % gesamt. **AK-7 gilt als abgenommen:** der Playtest lief auf Noahs ausdrückliche Delegation (/goal-Auftrag vom 2026-09-07, Entscheid in DECISIONS.md), 62 von 62 Fragen beantwortet, zwei Berichte (`playtest-v1.md`, `playtest-2026-09-07-v2.md`). Ehrlicher Vorbehalt: die 111 min für `test:slow` und die neu geschriebenen Benchmark-Zahlen entstanden unter **Fremdlast** — zwei verwaiste vitest-Worker eines am Sitzungslimit abgestorbenen Laufs banden je einen Kern und ließen sich aus der Sitzung heraus nicht beenden (Berechtigungs-Classifier). Das ist die sichere Richtung des Fehlers: die Budgets bestanden **trotz** Last; die Berichtszahlen sind eher zu schlecht als zu gut. AK-8 bleibt „gemessen gegen 1c33ec7, zählt nicht gegen V1" — das Erzeugnis ist seit M22–M24 um 37 Dateien weiter und wäre für eine neue AK-8-Messung neu zu bündeln | grün (7/7) |

## M25 — Die Zahlen werden Bilder

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M25-01 | 2026-09-08 | **Die Partie bekommt ein Gedächtnis: Zeitreihe je Spieltag.** Die Sicht kennt nur das Jetzt — jeder Verlauf braucht eine Aufzeichnung, und die ist Sache der Hülle (Kern unberührt, D25.1). `saves.ts` führt `TimelineEntry` (Tag, Punkte je lebender Macht, eigene Bestände und Bilanzen je Rohstoff, Festkomma) mit `recordTimelineDay` (höchstens ein Eintrag je Spieltag; derselbe Tag gibt dieselbe Referenz zurück, damit React nichts umsonst zeichnet) und Deckel `TIMELINE_CAP = 400`. Aufgezeichnet wird in `App.tsx` am Tageswechsel, im selben Effekt wie der Tagesbericht (T-M24-01). Je Spielstand-Slot wandert die Reihe als Nachbarschlüssel `zeitreihe.<slot>` mit — beim manuellen Speichern, beim Autosave (Slotname vor `writeAutosave` gemerkt) und beim Laden; alles best effort: ein alter oder kaputter Schlüssel heißt leere Reihe, die Kurve beginnt ehrlich am Ladetag. Falle: der Schlüssel stand zuerst als Suffix (`autosave-0.json.zeitreihe`) und zählte im Rotations-Test als vierter Autosave — jetzt Präfix | grün (saves 13, App 70 Tests) |
| T-M25-02 | 2026-09-08 | **Der Machtverlauf wird eine Kurve.** Vorschlag A: „gewinne ich?" hatte nur das Jetzt. Neuer Baustein `ui/charts/LineChart.tsx` — eigene SVG-Komponente, keine Fremdbibliothek: Pfadraum 0…100 in beiden Achsen (viewBox, per CSS gestreckt, non-scaling-stroke), `chartDomain`/`linePath` als reine, exakt gebundene Funktionen („M0,100 L50,50 L100,0" gegen bekannte Reihen). Das Lage-Panel zeigt die Kurve über der Punktetabelle: je bekannter Macht eine Reihe in ihrer Spielerfarbe (als Daten aus der Sicht, kein Literal — `tokens.ts` blieb deshalb unberührt), Legende mit Farbfeldern (`NationName`), aria-Beschreibung nennt die Endwerte. Unter zwei aufgezeichneten Tagen ehrlicher Leerzustand („Noch keine Aufzeichnung …") statt leerer Fläche — auch nach dem Laden eines alten Stands. Verdrahtung in App.test.tsx: nach zwei vorgespulten Spieltagen trägt das Panel die eigene Kurve. Querscroll-Wächter bleibt grün (Diagramm skaliert auf Panelbreite) | grün (Standings 16, LineChart 7, App 72 Tests) |
| T-M25-03 | 2026-09-08 | **Die Wirtschaft zeigt Trend und Bilanz als Bild.** Vorschlag B: sieben Rohstoffe × fünf Zahlenspalten, Trends musste man sich merken. Zwei neue Bausteine unter `ui/charts/`: **`Sparkline`** (Sieben-Tage-Fenster des Bestands aus der Zeitreihe, Punktraum 0…100 per CSS auf 40 px gestaucht, normalisiert auf die eigene Spanne — Richtung, nicht Maßstab; flache Reihe liegt in der Mitte) und **`DeltaBar`** (±-Balken: größter Betrag bekommt die halbe Spur, positiv grün nach rechts, negativ zinnober nach links, null als Strich; winzige Werte bleiben mit 2 % sichtbar). Beide wohnen IN den bestehenden Zellen — keine sechste Spalte, der Querscroll-Wächter (T-M22-02) bleibt grün, ein neuer Test bindet die fünf Spaltenköpfe. Beide Bilder sind stumm (`aria-hidden`), die Zahl daneben bleibt der zugängliche Wert. Eine Zeile ohne Verlauf (Rohstoff fehlt in der Zeitreihe, alter Stand) trägt keine Sparkline — ein erfundener Trend wäre schlimmer als keiner | grün (Panels 39, Charts 17, App 72 Tests) |
| T-M25-04 | 2026-09-08 | **Der Tagesbericht bekommt Balken.** Der Körper aus T-M24-01 nannte die Bilanzen als Satz („Bilanz je Tag: Nahrung +120, Eisen −40"). Jetzt liefert `dayReportDeltas` in `events.ts` sie als Daten (Beschriftung + Festkomma-Bilanz, nur ≠ 0), die App heftet sie neben den Textzeilen an den Protokolleintrag (`EventEntry.deltas`), und das Protokoll zeichnet je Rohstoff **denselben `DeltaBar`** wie die Wirtschaftstabelle — eine Komponente, zweimal verwendet, nicht zwei Kopien (ein Test bindet am gerenderten Eintrag die delta-Klassen, Balkenlängen 50 %/16,7 % und die Zahl daneben). Die alte Textzeile ist verstummt; ihr Schlüssel `dayReport.balance` wurde die Überschrift der Balkenliste (aria-label). Der „ruhige Tag" bleibt ehrlich: er kommt nur, wenn auch die Balken nichts zu zeigen hätten — sonst bleibt die Textfassung schlicht leer. Falle: der erste Wurf des Stille-Tests übersah „Morgen neu" aus der Freischaltungsachse | grün (Desktop 590 Tests) |

## M26 — Die Karte lebt

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M26-01 | 2026-09-08 | **Märsche werden Pfeile mit Fortschritt.** Vorschlag C: eine Bewegung war eine gestrichelte Linie ohne Richtung, und die nur für die gewählte Armee. Jetzt zeichnet die Karte JEDEN sichtbaren Marsch als Pfeil: `marchProgress`/`marchArrow`/`marchStroke` als reine Funktionen in `render.ts` (0 beim Abmarsch, ½ in der Mitte, voll bei Ankunft; geklemmt statt erfunden), der zurückgelegte Anteil voll, der Rest blass (`globalAlpha` 0,35), Pfeilspitze am Ziel, eigene Märsche in Tinte, fremde in Spielerfarbe. **Dokumentierte Wahl:** die Sicht führt `departureTick` (Beginn des ganzen Marsches) und `arrivalTick` (Ankunft der NÄCHSTEN Etappe, je Etappe neu gesetzt) — eine Gesamtankunft gibt es in der Sicht nicht; der Fortschritt ist deshalb der Anteil der laufenden Etappe, exakt die Rechnung von `marchPoint` (T-M20-04), damit Füllung und Marker sich immer im selben Punkt treffen. Fremde Armeen tragen in der Sicht weder Route noch Ticks (R-DIP-04) — ihr Pfeil entsteht erst, wenn die Sicht es je erlaubt; die Farbregel steht trotzdem bereit. Die gestrichelte Vorschau samt `path`-Prop entfiel (D25.3 „ersetzt die gestrichelte Linie"); der Fortschritt hängt am Spieltick, nicht an der Bildschirmuhr, und bleibt unter `prefers-reduced-motion` ablesbar — nur der gleitende Marker respektiert die Einstellung weiter. **Zeichenbudget nachgemessen** (Node, Maschine ruhig): Flächen der ganzen Welt plus 60 Pfeile p95 **5,77 ms** (Median 3,88, Max 10,2) gegen 16,7 ms Budget — der Bench schreibt die Zahl selbst nach `render-bench.json` (`nodeRemeasurements`), die Browser-Messung vom 2026-09-07 bleibt unangetastet | grün (render 18, MapCanvas 5, App 72 Tests) |
| T-M26-02 | 2026-09-08 | **Kampf und Eroberung sind auf der Karte sichtbar.** Ein Besitzwechsel war ein harter Farbsprung der teuren Ebene, ein Kampf ein Ring fester Stärke. Jetzt läuft der Wechsel als Farbwelle: `fadeProgress` in `motion.ts` (Smoothstep über `OWNERSHIP_FADE_MS = 600`; an feste Zeitpunkte gebunden — 0/½/1 bei 0/300/600 ms, monoton; bei `prefers-reduced-motion` oder Tempo über `CUE_SPEED_LIMIT` sofort 1, der alte harte Wechsel IST der reduced-Pfad), `ownershipChanges` erkennt die Wechsel als reine Differenz zweier Besitzstände (ein erstes Bild ist keine Eroberung — sonst waberte beim Partiestart die halbe Welt), und `MapCanvas` blendet die Fläche auf dem Überzug mit der **exportierten** Mischformel `mixColors` der Kartenmodi (eine Formel, keine zweite Farbtabelle). Die Welle bekommt ihren Start vom ersten rAF-Bild (eine fremde Uhr ergäbe Wellen, die vorbei sind, bevor jemand sie sah) und endet als Anlass der Schleife mit sich selbst. Der Gefechtsring skaliert nach Größe: `battleIntensity` (Wurzel aus sichtbarer Gesamtstärke gegen `STRENGTH_FULL` — ein Viertel der vollen Stärke ist der halbe Ring, ein Scharmützel bleibt sichtbar), Radius 9…16 px plus Atmen, Strich 1,4…3 px. Budget erneut gemessen: p95 **5,39 ms** (Median 3,67, Max 11,2) gegen 16,7 ms, `nodeRemeasurements` in `render-bench.json` aktualisiert | grün (motion 11, render 21, Desktop 604 Tests) |
| T-M26-03 | 2026-09-08 | **Der fünfte Kartenmodus: Beziehungen.** Vorschlag E: die Diplomatie wohnte nur in einer Tabelle, obwohl `PublicView.relations` je Macht Zustand und Dauer führt. `MAP_MODES` kennt jetzt `relations` (am Ende des M-Zyklus, `keyboard.test.ts` bindet strength→relations→political), `RELATION_COLORS` in `tokens.ts` als eigene kleine Palette — eigen Gold `#D9B84A`, verbündet gedecktes Grün `#9BBB88`, Frieden Leinen `#E4E0D2`, Krieg heller Zinnober-Ton `#D2896F` (als Fläche ganzer Weltgegenden wäre das Alarmrot selbst Tapete statt Signal), unbekannt Nebelgrau `#BFBFB9`; alle Paare ΔE > 10 (knappstes: Frieden/unbekannt 12,7), Kartenschrift auf jeder ≥ 4,5:1 (schwächste Krieg 6,3:1) — beides im erweiterten Farbtest gebunden. `relationKindFor` als reine Ableitung: eigen/verbündet/Krieg; Waffenstillstand zählt als Frieden (es wird nicht geschossen), herrenloses Land als Frieden, Provinz hinter dem Nebel und Macht ohne bekannte Beziehung bleiben unbekannt — Frieden zu behaupten, den man nicht kennt, wäre gelogen. Die App reicht `relation` je Provinz an die Schattierung (auch ein veralteter gemerkter Eigentümer trägt die heutige Beziehung — die kennt man von sich selbst), die Legende nennt alle fünf über das bestehende `legendFor`, `de.ts` erklärt den Modus; die Diplomatie-Tabelle bleibt | grün (modes 15, contrast 13, Desktop 609 Tests) |

## M27 — Das Gefecht zeigt sich

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M27-01 | 2026-09-08 | **Das Gefecht sammelt seine Zahlen für die Anzeige.** Vorschlag D, erste Hälfte: der Kern kannte Stärken, Gelände und Festung — das Ereignis nannte nur die Verluste. Fünf Felder ADDITIV am `BATTLE_RESOLVED` (optional im Typ, immer gesetzt beim Erzeugen; Ereignisse gehen nicht in den Zustands-Hash ein, Golden-Master nachweislich unberührt): `strengths` je Seite vorher/nachher (nachher **nachgemessen**, nicht gerechnet — dieselbe Quelle wie die Verluste), `terrain`, `fortressLevel` (0 = keine), `entrenched` (exakt die Bedingung, unter der `defenceMultiplier` den Eingrabungsbonus vergibt: Eigentümer, alle Armeen stehend) und `attackBlocked` (keine Armee der Seite durfte in diesem Tick angreifen, D6.8). Daraus formt `battleReport` in `game/events.ts` den Anzeigedatensatz für die Stärkebalken (T-M27-02): Namen statt Kennungen, Festkomma bleibt bis zum Zeichnen. Drei Grenzen mit Absicht: ein altes Ereignis ohne die Felder ergibt KEINEN Datensatz (lieber kein Bild als ein erfundenes), ein Unbeteiligter bekommt keine Mengen (R-DIP-04, dieselbe Linie wie `_FOREIGN`), der Betrachter steht zuerst — es ist sein Bericht | grün (core combat 27, events 42, Determinismus 4 Tests) |
| T-M27-02 | 2026-09-08 | **Das Gefecht zeigt sich: Stärkebalken und Zeichen.** Vorschlag D, zweite Hälfte: der Kampfbericht war ein Satz — jetzt trägt der Gefechtseintrag einen aufklappbaren Körper nach dem Muster des Tagesberichts (T-M24-01), aber strukturiert: Balken statt Absätze. `EventEntry.battle` hält den Datensatz aus T-M27-01 (`describeEvent` heftet ihn an; ohne die additiven Felder oder aus fremder Sicht bleibt die Zeile schlicht), `BattleBody` in `Panels.tsx` zeichnet je Seite einen Stärkebalken vorher → nachher — Maßstab ist die stärkste Seite VORHER, damit das Kräfteverhältnis ablesbar ist, nicht nur der eigene Schwund; der Verlust ist der zinnoberrote Abschnitt (`--accent`, der Signalton zu Recht), `battleBarWidths` rein und exakt gebunden (87,5/12,5 und 20/30 % bei bekannten Zahlen), die Zahlen stehen sichtbar daneben. Die Umstände als Zeichen aus dem Symbolsatz: Gelände und Festung (Stufe) unter den Balken, Eingrabung und Rückzugssperre an der Seite, die sie betreffen — zwei neue Zeichen (`entrenched` als Stellungsbogen, `noRetreat` als gesperrter Pfeil), der Rest war da. Fürs Ohr spricht das Bild als EIN Satz (`role="img"` + aria-label aus `battleSentence`, dieselben Zahlen wie neben den Balken, dann Gelände und Festung). Querscroll-Wächter bleibt grün (Körper max. 280 px in der Spur des Protokolls) | grün (Desktop 705 Tests inkl. Guards) |

## Pflege — der Abnahmelauf wird kurz und ehrlich (2026-09-08, Noahs Auftrag)

| Was | Ergebnis |
|---|---|
| Abnahmelauf verkürzt | **6 min 21 s statt 75–130 min** (gemessen, `docs/reports/acceptance-timing.json`). Die Abnahme fährt nur noch ihre Kriterien: verify (AK-2/3/4) → AK-1-Vollpartie **parallel** zum AK-6-Langlauf → Zeitbudgets seriell. Parameterlauf/Turnier/KI-Integration/Onboarding bleiben in `pnpm test:slow` und als Einzelbefehle; ein **Frische-Wächter** (`gaugeStatus`, TDD in `test/requirements.test.ts`) färbt die Abnahme rot, sobald die Regeln jünger sind als die Messgerät-Berichte. Das Skript nennt vorab die **erwartete Dauer aus der Messung des letzten Laufs** statt einer Schätzung. Entscheid in DECISIONS.md |
| Dabei eine Attrappe gefunden | Der „AK-6-Langlauf" lief seit jeher auf der **12-Provinzen-Testkarte mit 3 Spielern** — AK-6 verlangt wörtlich „1000 Spieltage, 8 Spieler". Sichtbar wurde es erst, als der Lauf einzeln aufgerufen wurde und seine 7 Sekunden auffielen (dieselbe Klasse wie R-AI-04 auf der falschen Karte). Jetzt spielt er die **ausgelieferte Voreinstellung** (Weltkarte, 8 KI, 1000 Tage, 4,6 min) und nagelt die Bedingungen mit Zusicherungen fest, damit sie nie wieder still schrumpfen |

## M28 — Der Feinschliff nach dem Spielen

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M28-01 | 2026-09-08 | **Die Kurve wird bei jeder Historienlänge lesbar.** Sichtprüfung 2026-09-08: acht Tage Historie ergaben flache, ans obere Fünftel gedrängte Linien, weil die Y-Skala bei 0 begann. `chartDomain` spannt jetzt von min−Rand bis max+Rand (Rand ein Zehntel der Wertspanne, nie unter eins — eine flache Reihe liegt in der Mitte statt durch null zu teilen), `linePath` rechnet gegen die untere Skalengrenze; die Tests binden die neuen Grenzen an bekannte Reihen und fielen nachweislich gegen die 0-Basis. Jede Linie trägt ihren **Endwert am rechten Rand** (`chart__endvalue`, absolut auf der Höhe des letzten Punkts, Reihenfarbe, aria-hidden — die Bildbeschreibung nennt die Endwerte mit Namen). Unter **drei** Aufzeichnungspunkten steht statt der Pseudokurve der ehrliche Wartesatz `standings.historyWaiting` („Erst {{days}} von 3 Tagen aufgezeichnet …"); ohne Aufzeichnung bleibt der alte Leerzustand. Verdrahtungstest in App.test.tsx fährt jetzt drei Tageswechsel | grün (LineChart 9, Standings 20, App 73 Tests) |
| T-M28-02 | 2026-09-08 | **Auch die Zielwahl quittiert sichtbar.** Debugging 2026-09-08: die Quittung aus T-M22-05 hing per `actionId` am „Marsch befehlen"-Knopf — der verschwindet mit `setTargeting(null)` im selben Klick, der Spieler sah nach dem Bestätigen nichts. Die Armee-Statuszeile zeigt die Quittung jetzt zusätzlich (`ArmyPanel.pendingNotice`, `role="status"`, dieselben Sätze `actions.ordered`/`orderedPaused` — kein neuer Text nötig), gespeist aus derselben `pendingCommands`-Sammlung der App; bewusst nur für `MOVE_ARMY` und `BOMBARD`, denn die übrigen Armee-Befehle behalten ihren sichtbaren Knopf samt Knopf-Quittung. Der Test fährt den VOLLEN Zielwahl-Weg am gerenderten Baum in **StrictMode wie main.tsx** (bauen, ausheben, Armee wählen, Ziel per Dropdown, Marsch befehlen) und sieht die Quittung nach dem Bestätigen — er fiel gegen den alten Stand; nach dem nächsten Tick ist sie nachweislich wieder weg | grün (App 74, Panels 57 Tests) |
| T-M28-04 | 2026-09-08 | **Die Debug-Ansicht spricht Namen.** Befund V2-12: das opt-in-Debug sagte „p2" und „money". Die KI bleibt englisch und kernnah — `packages/ai` blieb unangetastet; die Übersetzung passiert beim Rendern in `Dialogs.tsx`: `localizeDebugText` ersetzt in der freien Prosa der Zieltexte Spieler-Kennungen per Wortgrenzen-Muster (`\bp\d+\b` — `p22` als Provinzkennung bleibt stehen) durch Machtnamen (`nameOf` aus der App, dieselbe Quelle wie die übrige Oberfläche) und die sieben Rohstoffschlüssel aus `RESOURCE_KEYS` durch die deutschen Namen aus `de.ts`; der Spielername vor dem Doppelpunkt läuft direkt durch `nameOf`. Dazu D26.4, zweite Hälfte: der Umlaut-Wächter (`text-keys.test.ts`) prüft jetzt auch die Prosa-Literale aller KI-Quelldateien auf Ersatzschrift — mit Selbstprüfung, dass er überhaupt Dateien findet. Test bindet einen Zieltext mit Namen am gerenderten Baum und fiel gegen den alten Stand | grün (Dialogs 9, Guards text-keys 10 Tests) |
| T-M28-05 | 2026-09-08 | **Die Wirtschaft sagt, wohin die Rohstoffe gehen.** v1-Befund 15, nie adressiert: die Spalte Unterhalt führte nur den Armeeunterhalt — Bau-, Aushebungs- und Marktkosten erschienen nirgends, und seit den Sparklines fiel der Bestand sichtbar, ohne dass eine Spalte sagt warum. Neue reine Funktion `dayExpenses` in `game/events.ts` (denselben Quellen wie der Tagesbericht): Bau aus den `BUILD_STARTED`-Ereignissen des Tages (kennen den Auftrag auch, wenn er am selben Tag fertig wurde), Aushebung aus den heute begonnenen Aufträgen der eigenen Warteschlangen (das Erteilen hat kein Ereignis — die ehrliche Grenze steht als Kommentar dabei), Markt aus `TRADE_EXECUTED` (Abgabe ja, Ertrag nein); Fenster ist der Spieltag bis `view.tick`. **Bewusste Abweichung vom Wort „Spalte":** die Auskunft steht als Zahl mit Titel („Ausgaben des Tages: … — Bau, Aushebung und Markt") NEBEN dem Unterhalt, D24.2-Stil — eine sechste Spalte schob die Tabelle schon einmal aus der Leiste, und der Querscroll-Wächter aus T-M22-02 (fünf Spaltenköpfe, `overflow-x: hidden`) bleibt unangetastet bindend; genau dieser Rückfallweg stand in der Aufgabenplanung. Tests binden `dayExpenses` an einen Tag mit bekannten Zahlen (fremde Provinzen und gestrige Aufträge zählen nicht) und die Auskunft an die vierte Zelle der Tabelle | grün (events 45, Panels 59, App 74 Tests) |
| T-M28-03 | 2026-09-08 | **Frisches Bündel — und die Messung wurde zur Reparatur.** Der erste Lauf am neuen Bündel **brach AK-8**: Speichern schrieb die Datei, jede Anzeige blieb „leer". Falsifikationskette über CDP am laufenden Programm (fünf Hypothesen, vier widerlegt — PROBLEME.md): die Scope-Prüfung von `tauri-plugin-fs` kanonisiert **existierende** Pfade zur `\?\C:\…`-Form, auf die kein Scope-Muster passt; deshalb ging Schreiben neuer Stände immer und Wiederlesen nie. Beweis-Messung: Geisterdatei → sauberes `false`, existierende Datei → „forbidden". Reparatur: sechs **eigene, engere Hüllen-Kommandos** (`saves_list`…`saves_exists`, Dateiname statt Pfad, fest auf `$APPDATA/saves`); fs-Plugin samt Berechtigungen entfernt; Vertragsreihe gegen die Kommando-Nachbildung. AK-8 danach komplett neu gemessen (7/7, erstmals inkl. „Weiterspielen (Tag 1)" am Programm) — `docs/reports/packaging.md`. Ehrlicher Rand: die Messung vom 2026-09-07 ist mit identischem Code/Rechten/Abhängigkeiten nicht reproduzierbar und bleibt als nicht nachvollziehbar markiert | grün (Storage 36, verify folgt) |

## M29 — Kriegsrat: das Aussehen

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M29-01 | 2026-09-11 | **Die Oberfläche ist dunkel, und die zwei Farbquellen können nicht mehr driften.** Zuerst der Wächter `test/guards/css-mirrors-tokens.test.ts` (`mirrorDiff`: jeder Token muss als `--kebab: #hex` in `app.css :root` stehen und jede Variable dort einen Token haben; bewiesen an der verstimmten Fixture `css-mirror.txt` in beide Richtungen) — er **fiel gegen den alten Stand**, weil `--on-player` in der CSS fehlte. Dann D27.1 an beiden Orten: `ground #0D1117`, `paper #161C25`, `paperSunk #1E2632`, `ink #E6E1D3`, `inkSoft #9AA0A8`, `water #0A0E14`, `good #7EC57E`, `warn #E0A220`; neue Tokens `onWarn #1A1200`, `building #C9B98A`, `ally #6FA8DC`. **Zwei Werte weichen begründet vom Entwurf ab, der Kontrasttest hatte das letzte Wort:** `line #5C6A78` statt `#2F3944` (1,46 : 1 auf `paper` — eine leere Balkenspur wäre unsichtbar; 3,09 : 1 jetzt) und `accent #E8583F` statt `#E2503A` (4,44 : 1 auf `paper`, jetzt 4,81 : 1). Spielerfarben: fünf der elf Kandidaten aus D27.1 hielten ΔE > 10, die übrigen sechs fand eine Greedy-Suche über gedeckte dunkle Töne (min ΔE 14,7, min Kontrast 4,82 : 1 gegen `onPlayer`, alle > 13 ΔE von Grenze und Neutralfüllung). `RELATION_COLORS` sind dunkle Flächenverwandte (`self #3C6E44`, `ally #2A4A66`, `war #7A2E22`, `peace = paperSunk`, `unknown #3A3D40`) statt `good`/`accent` selbst — Entscheid in DECISIONS.md. `MAP_COLORS`: Auswahl Bernstein, Schrift-Halo `ground`; eigene Armeen und Marschpfeile `good`. Bernstein-Knöpfe (`.button--primary`, `.speed--active`, Explain-Toggle) mit `onWarn`. 17 Kontrastpaare grün. Sichtprüfung im Browser (Weltkarte, USA, Tag 1): dunkler Tisch, helle Schrift, Bernstein-Tempo, Rohstoffe mit Vorzeichen | gezielt grün (Kontrast 11, Spiegel 3, modes 20, markers 14, render 21, MapCanvas; `pnpm verify` folgt nach M29) |
| T-M29-02 | 2026-09-11 | **Die Kopfleiste spricht Kriegsrat.** Zwei Zeilen: oben Titel, Uhr in Bernstein mit Uhr-Symbol (`.clock__time`, `TYPE.num`), Tempo als Knopfgruppe (Pause mit Symbol und `aria-label`, die Rasten, Vorspulen/Abbrechen als letzter Knopf mit `aria-pressed` nur während des Laufs), Kartenmodus als Knopfgruppe (`role="group"`, genau ein `aria-pressed`, ersetzt das `<select>`), Panelknöpfe rechts, dann der **leere, verborgene Alarmchip-Slot** `.header__alarm` für T-M28-06. Unten die Rohstoffleiste auf `paperSunk` mit Trennlinien: Symbol in `building` (Geld in `warn`), Bestand, Bilanz mit Vorzeichen im Text **und** Richtung als Klasse (`resource__balance--plus/minus/zero`, grün/zinnober), **Reichweite im `title`** statt als dritte Zahl (Zeile bleibt eine Zeile). Drei neue Icon-Pfade (`clock`, `pause`, `fastForward`), Text `header.speedStop`. Tasten unverändert (Leertaste, M — App-Tests auf die Knopfgruppen umgestellt). Tests zuerst: fünf neue in `Header.test.tsx` fielen gegen den alten Stand (Tempo-Gruppe genau ein gedrückt + Klick ruft `onSpeed(10)`, laufende Stufe gedrückt, Vorzeichen + Klasse, Modus-Gruppe + `onMode('morale')`, Alarm-Slot leer und `hidden`). Sichtprüfung im Browser: Kopfzeile wie im Entwurf, Bernstein-Pause gedrückt, BESITZ gedrückt, Bilanzen grün | gezielt grün (Header 15, icons, App 74, a11y) |
| T-M29-03 | 2026-09-11 | **Das Provinzpanel zeigt Bauplätze statt einer Gebäudeliste.** Raster `.slots` in vier Spalten, je `BuildingKey` genau ein Feld in der Reihenfolge von `BUILDING_ICONS` (`BUILDING_ORDER`): **gebaut** (Glyphe in `building`, Name, Stufe ≥ 2 hochgestellt in Bernstein, Ausbau als kompakter „+“-Knopf oben rechts — dieselbe Bau-Aktion), **im Bau** (Bernstein-Rahmen, `Meter` mit `aria-valuenow` aus `startedTick/completesAtTick`, 3-px-Spur, Restzeit), **frei** (gestrichelt, die bestehende Bau-Aktion als Knopf in Bernstein; in fremden Provinzen nur der Name). Die Bau-Gruppe verschwindet aus den Aktionsgruppen; Aktionen, die keinem Bauplatz gehören, bleiben als Gruppe (Test T-M21-06 „title ganz weg“ fand das). `Meter` kann jetzt **Segmente** (`segments={10}` für die Moral: Prozent, Tendenz und zehn Stufen; Rolle und Werte unverändert). Gelände mit **Verteidigungsbonus** („Gebirge · Verteidigung +30 %“) aus `TERRAIN_DEFENCE_PERMILLE` — der Kern schreibt die Boni als Literale in `combat.ts`, darum die Tabelle in der Oberfläche **mit Wächter gegen `defenceMultiplier`** (Kern unberührt). Hauptstadt als Stern vor dem Namen. Tests zuerst: fünf neue fielen gegen den alten Stand. Sichtprüfung im Browser (Mittlerer Westen, Tag 1): Kaserne gebaut, sechs freie Plätze in Bernstein, Moral 70 % als sieben von zehn Segmenten, Auswahl auf der Karte in Bernstein | gezielt grün (Panels 73, Meter, App 74, a11y); `pnpm verify` für M29 folgt |

## M30 — Kriegsrat: die Karte zeigt den Zustand

> **verify-Stand M29 (2026-09-11):** drei Läufe. Der erste fand drei Tests, die den alten hellen Stand banden (Design-Tor „helle Richtung“, Asset-Register ohne `kriegsrat-icons.svg`, LineChart-Endwert als Hex-Literal) — alle drei auf den Kriegsrat-Stand gebracht (Commit `03fa886`). Der zweite fiel allein am Lint-Wächter `no-color-literals` (ESLint-Kaltstart 5,3 s unter 136 parallelen Dateien, allein 1,5 s — Ladezeit, kein Spielbudget; ESLint-Instanz je Arbeiter geteilt, Vorgabe 30 s nur für die Lint-Wächter, Commit `81b…`). Der dritte scheiterte in der Typprüfung ausschließlich an den **bereits eingespielten, noch unerfüllten T-M30-01-Tests** im Arbeitsbaum (mein Fehler in der Reihenfolge); Tests + Guards + Typen des M29-Stands sind damit über die drei Läufe zusammen belegt (1701/1702 → 1702/1702 nach dem Wächter-Fix). Der nächste volle `pnpm verify` läuft am M30-Tor.

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M30-01 | 2026-09-11 | **Armeen sind Stapel mit Zahl und Zustand.** `stackSummary(units, rules)` in `markers.ts` (rein): Stückzahl `Σ ceil(hpTotal/hpPerUnit)` wie `unitCount` im Kern, Zustand `Σ hp / Σ (n · hpPerUnit)`; `toneFor` (eigen `good`, Krieg `accent`, Bündnis `ally`, sonst `inkSoft`) aus `own` und der Beziehung des Besitzers (`view.relations`); `ArmyMarker` trägt `count/condition/relation`, `Marker` trägt `count/condition/tone`. **Fremde Stapel ohne Zahl** — die Sicht kennt ihre Einheiten nicht (R-DIP-04), also steht auch keine da. `ARMY_BOX` 30×18: Rahmen in Besitzerfarbe auf `ground`, Glyphe der dominanten Gattung links, Zahl rechts in `TYPE.num` 9 px, 3-px-Zustandsbalken am unteren Rand (Spur `line`, Füllung Besitzerfarbe). **Stempel:** je (Ton, Glyphe) ein Offscreen-Canvas, je Bild `drawImage` statt `Path2D`-Zug (KRIEGSRAT §6.1); Zahl und Balken werden darüber gezeichnet. Trefferfläche quer 15 px (halbe Stapelbreite), hochkant 12 px — nie kleiner als der Stapel. **Bench vorher/nachher unter Fremdlast** (ein fremder Prozess „GR“ band ~6 Kerne, Last 53–68 %; nicht abschaltbar): Pfeile-Fall p95 8,37 → 8,14 ms; neuer Fall „300 Stapel + Gebäude in jeder Provinz“ p95 **8,65 ms** (Median 5,04, max 14,9) — Budget 16,7 hält auch unter Last; bei freier Maschine lag derselbe Pfeile-Fall am 2026-09-08 bei 3,01. Tests zuerst: sechs neue fielen (Summe über zwei Gattungen, Randfälle, Marker-Felder, Ton, Trefferfläche, `drawImage`+`fillText` am aufzeichnenden Kontext). **Sichtprüfung im Pane nicht möglich:** die Bildschleife läuft dort mit ~1 Tick/s (rAF gedrosselt), Armeen entstehen erst nach Tagen — wird am M30-Tor nachgeholt | gezielt grün (markers 32, MapCanvas 9, App 74); Typen grün; Bench 8/8 |
| T-M30-02 | 2026-09-11 | **Gebäude stehen verteilt in der Provinz.** Neues `map/anchors.ts` (rein): `anchorsFor(polygons, center)` — größter Ring, Gitter mit Schrittweite `max(16, √Fläche/5)`, Punkt-in-Polygon, Randabstand ≥ 12, nah am Mittelpunkt zuerst, Mindestabstand 14, höchstens 12 Anker; zu kleine Provinzen bekommen den Mittelpunkt. `placeBuildings`: **fester Platz je Gebäudeart** (Hafen und Werft die zwei randnächsten Anker, die übrigen Arten in `BUILDING_ORDER` je einen der inneren) — so verschiebt ein neuer Bau die anderen nicht (der erste Entwurf „nächster freier Anker“ hätte die Fabrik beim Bau einer Festung versetzt; der Test fand es). `BUILDING_ORDER` wanderte nach `icons.tsx` (Panel und Karte teilen es). Marker 14×14 als Stempel je Glyphe (Rahmen `building`, Glyphe aus `BUILDING_ICONS`), Stufe ≥ 2 als Ziffer rechts oben; **die Pips entfallen**; `MapCanvas.buildings` ist jetzt Art → Stufe je Provinz, `anchors` einmal je Karte in App gerechnet. Sichtbar erst ab `scale ≤ 1` (`BUILDING_MAX_SCALE`, D27.4 — T-M30-03 macht daraus `zoomTier`). Anker an allen 237 Provinzen der Weltkarte geprüft (jeder Anker im Land, > 3 je Provinz im Mittel). **Bench nachher** (Fremdlast wie zuvor ~65 %): mittlere Stufe mit 300 Stapeln + **945 Gebäuden** an Ankern p95 **10,17 ms** (Median 5,84, max 15,73); Pfeile-Fall 7,92. Vorher-Wert derselben Aufgabe: der Stapel-Fall aus T-M30-01 (8,65 bei Weltansicht — nicht derselbe Ausschnitt, da Gebäude auf der Weltansicht gar nicht gezeichnet werden). Tests zuerst: 10 in `anchors.test.ts`, 3 neue/umgebaute in `markers.test.ts`, 1 in `MapCanvas.test.tsx` (zwei Stempel je Bild). **Sichtprüfung im Browser** (Spielstand Tag 105, Kaserne gebaut, Tag 107): das Gebäudequadrat steht in der Fläche von Mittlerer Westen unterhalb des Hauptstadtsterns, nicht in der Mitte; Stapelmarker mit Zahl in Ostkanada und Mexiko sichtbar (Nachtrag zur Sichtprüfung von T-M30-01) | gezielt grün (anchors 10, markers 34, MapCanvas 10, App 74, Panels 73); Typen grün; Bench 8/8 |
| T-M30-03 | 2026-09-11 | **Drei Zoomstufen, drei Knöpfe, eine Übersichtskarte.** `zoomTier(scale)` in `picking.ts` (rein): nah ≤ 1, mittel ≤ 2, weit darüber — `scale` sind Kartenraum-Einheiten je Bildpunkt, die Zahlen des Entwurfs (0,5/1,0 als Zoomfaktor) sind der Kehrwert davon. Gebäude ab mittel (`markersFor` fragt `zoomTier`), Stapel und Hauptstadt immer; **Namen bleiben ab mittel** (bestehendes `LABEL_MAX_SCALE = 2`, der Startausschnitt liegt bei 1,6 — Namen erst ab nah hätte T-M13-08 zurückgedreht; bewusste Abweichung von D27.4). **Moralringe gibt es nicht** — der Entwurf nennt ein Element, das der Code nie hatte; nicht gebaut, nicht erfunden. Knöpfe `+ − ◎` oben rechts auf der Karte (`.map-controls`, 26×24, `aria-label` Hineinzoomen/Herauszoomen/Hauptstadt zentrieren; zoomen um die Mitte des Ausschnitts, `ZOOM_STEP` 1,2 wie ein Mausrad-Rasten). **Tasten: Bild↑/Bild↓ zoomen, Pos1 zentriert die Hauptstadt** — nicht `+`/`−` wie im Entwurf, denn die gehören seit T-M10-06 dem Tempo (Tastaturhilfe ergänzt). Übersichtskarte 132×74 unten rechts über der Legende: dieselbe `prepareFrame` mit Weltmaßstab (fast alles fällt der Ausdünnung zum Opfer, darum billig), Ausschnitt als Bernstein-Rahmen, Klick zentriert dort, `role="button"` mit Namen. Tests zuerst: `zoomTier` an den Schwellen, drei Tasten, drei Knöpfe ändern den Ausschnitt (Zentrieren trifft die Kartenmitte, die die Karte kennt), Übersicht 132×74 und Klick-Zentrierung mit dem echten Übersichtsmaßstab. Bench bei freier Maschine (Fremdlast weg): Pfeile p95 2,59, Stapel+945 Gebäude 2,81 ms; Weltansicht zeichnet keine Gebäude — nicht teurer als vor T-M30-01. Sichtprüfung im Browser (Spielstand Tag 105): Knöpfe oben rechts, ◎ holt Mittlerer Westen in die Mitte, `+` zoomt, der Bernstein-Rahmen der Übersicht folgt. Nebenbefund: nach dem Anhängen eines Exports hielt Vite ein veraltetes `keyboard.ts` im Modulcache (weiße Seite, „does not provide an export named ZOOM_STEP“) — Neustart des Dev-Servers behob es, Tests und Typen waren durchgehend grün | gezielt grün (picking, keyboard, MapCanvas 11, markers, App 74, Dialogs); Typen grün; Bench 8/8 |
| T-M30-04 | 2026-09-11 | **Der Marschweg zeigt Stand und Rest.** `render.ts`: `MarchArrow.standpoint` (die Segmentgrenze des Fortschritts, `done` endet und `ahead` beginnt dort), `marchDays(timing, tick, ticksPerDay)` (aufgerundet, geklemmt; Marsch ohne Dauer = 1/1), `marchLabel` → „n/m T“; Konstanten `MARCH_DONE_WIDTH 3`, `MARCH_AHEAD_WIDTH 1,6`, `MARCH_DASH 3/3`, `MARCH_STANDPOINT_RADIUS 3,5`, `MARCH_LABEL_PX 7`. `MARCH_AHEAD_ALPHA` entfällt (der Rest ist gestrichelt statt blass, D27.5). MapCanvas: Gelaufenes 3 px rund in Besitzerfarbe, Rest 1,6 px gestrichelt mit Spitze, Standpunkt Kreis r 3,5 mit `ground`-Rand, Tagesangabe in `TYPE.num` 7 px mit `ground`-Halo neben dem Standpunkt **ab Stufe mittel** (`zoomTier`); neue Prop `ticksPerDay` aus App. Nachschublinie unberührt. Der Fortschritt hängt an der Spieluhr — reduced-motion ändert nichts (gebunden). Tests zuerst: vier in `render.test.ts` (Aufrunden/Klemmen, Beschriftung, Standpunkt = Segmentgrenze, gleicher Tick = gleicher Pfeil) und einer in `MapCanvas.test.tsx` (`setLineDash`, `arc`, `fillText`); der alte T-M26-01-Test verlangte **keine** Strichelung und keine Deckung 0,35 — auf D27.5 umgestellt (Strichelung ist jetzt Absicht, Spitze als Fläche). Bench bei freier Maschine: Pfeile p95 2,53 ms, Stapel+Gebäude 3,05. Sichtprüfung im Browser (Spielstand Tag 105 → Kaserne, Infanterie, Marschbefehl nach Westen der USA, Tag 111): grüner Stapel „1“ am Standpunkt, gelaufener Teil voll, Rest gestrichelt nach Westen; die Tagesangabe ist im verkleinerten Pane-Screenshot zu klein, um sie zu lesen — der Test bindet sie | gezielt grün (render 25, MapCanvas 12, App 74); Typen grün; Bench 8/8 |

## M31 — Kriegsrat: Panels, Protokoll, Fuß

> **verify-Stand M30 (2026-09-11):** `pnpm verify` grün gegen `3813c38` — Lint, Typen, **1729 Tests**, Abdeckung Kern 96,8 %, gesamt 95,1 %, Exit 0.

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M31-01 | 2026-09-11 | **Die Provinz erklärt sich im Tooltip.** Neues `ui/Tooltip.tsx`: `tooltipFor(provinceId, view, quellen)` (rein) baut die Auskunft **aus der Sicht** — Name, Besitzer-Macht, Moral (nur eigen), Gelände mit Verteidigungsbonus, sichtbare Armeen (Anzahl + Stärke; „keine“ / „1 Armee“ / „n Armeen“), Gefechtsrunde aus `view.battles` (Spieltage seit Beginn, ab 1), Stand veralteter Sicht; unbekannte Provinz → null. `<Tooltip>` mit `role="tooltip"`, Bernstein-Kante, 14 px neben dem Anker, `pointer-events: none`, Bedienhinweis. MapCanvas: `onHover(provinceId, at)` **entprellt 120 ms** (`HOVER_DELAY_MS`, trailing `setTimeout`, beim Verlassen sofort null; kein Zeigen während des Ziehens). App: Anker ist der Zeiger, sonst die Mitte der per Tastatur/Auswahlliste gewählten Provinz (`toScreen`) — dieselbe Auskunft für Maus und Tastatur; **Escape** blendet ihn aus, bis Auswahl oder Zeiger wechseln (die Escape-Kaskade darunter bleibt). Tests zuerst: fünf in `Tooltip.test.tsx` (Inhalt aus der Sicht, fremde Moral verschwiegen, veralteter Stand, Rolle und Zeilen, Position neben dem Anker), einer in `MapCanvas.test.tsx` (zwei Bewegungen → eine Meldung nach der Entprellzeit, Verlassen → null; `vi.useFakeTimers`), einer in `App.test.tsx` (Auswahlliste → Tooltip mit Name und Moral, Escape schließt). Erster Wurf band `nameOf` vor seiner Deklaration (TS2448) — Block hinter `nameOf` verschoben. Sichtprüfung im Browser (Tag 105, Zeiger auf Nordkanada): Kasten „Nordkanada · Kanada“, Ebene, Armeen keine, Hinweis | gezielt grün (Tooltip 5, MapCanvas 13, App 75, a11y); Typen grün |
| T-M31-02 | 2026-09-11 | **Das Armeepanel spricht die Sprache der Karte.** Neues `ui/UnitMarker.tsx`: der NATO-Stapel als SVG 30×18 (Rahmen in Besitzerfarbe, Glyphe links aus demselben 24er-Pfad wie auf der Karte, Zahl rechts in `TYPE.num`; `role="img"` mit Namen „8 Infanterie“) — ein Strichbild, zwei Ausgaben. `ArmyPanel`: Kopfzeile „Kampfkraft n · Zustand p %“ plus Zustands-`Meter` (neue Prop `condition` 0…1 aus `stackSummary`, nur eigene Armeen), Einheitenzeile `.units` aus Markern statt `IconRow`, **Haltung als Dreiergruppe** (`role="group"` „Haltung“, `aria-pressed` = aktuelle Haltung aus `army.stance`, gedrückt in Bernstein; die bereits geltende Haltung bleibt wie bisher gesperrt), **Befehle zweispaltig** (`.actions--grid`, `role="group"` „Befehle“) mit Zeichen aus dem vorhandenen Satz (`ARMY_ACTION_ICONS`: Marsch → Pfeil, Anhalten → Eingraben, Zusammenlegen → Bündnisringe, Teilen → Schlange, Beschießen → Artillerie, Feuer → Kampf), **Marsch als Hauptaktion** (`button--primary`, Bernstein). `ActionButton` kann jetzt `primary` und `pressed`. Während der Zielwahl bleibt das bisherige Zielwahl-Formular. Texte `army.power/condition/commands`. Tests zuerst: fünf in `Panels.test.tsx` (genau ein gedrückter Haltungsknopf, Marker je Gattung hörbar, Kampfkraft mit Zustand und Balken, Raster mit Zeichen und Hauptaktion, **Querscroll-Wächter** mit echter Kaskade am Armeepanel). Nebenbefund: ein Patch-Skript spielte `de.ts` zweimal ein (TS1117, doppelte Schlüssel) — bereinigt, bevor etwas lief. Sichtprüfung im Browser (Tag 110, Infanterie ausgehoben): Marker „1“, Haltungsgruppe mit gedrückter Verteidigung, zweispaltige Befehle mit Zeichen, Marschieren in Bernstein | gezielt grün (Panels 69, App 75, a11y); Typen grün |
| T-M31-03 | 2026-09-11 | **Der Fuß trägt Protokoll, Rangliste und drei Knöpfe.** Neues `ui/Foot.tsx`: dreispaltig — links das bestehende `EventLog` (Zeitspalte, Rubrik-Icon, Text, Filter unverändert), Mitte die **Rangliste dauerhaft** (`footRows`: vier Zeilen um die eigene Macht, Fenster rutscht am Rand, eigene Zeile immer dabei und in Bernstein; aus `standingsRows`, denselben Daten wie das Lage-Panel), rechts **Depesche** (öffnet den jüngsten Tagesbericht als Dialog — `latestReport`/`isDayReport`: Bilanzen oder Absätze, kein Kampf; gesperrt, solange es keinen gibt), **Diplomatie · Markt** als Paar und **Rangliste / Sieg mit Neu-Marke** (`unreadCount`: Protokollzeilen mit Tick über dem zuletzt gesehenen; Öffnen der Lage setzt `seenTick` auf den aktuellen Tick → Marke null). Die Kopfzeile verliert Diplomatie/Markt/Lage (sie wohnen jetzt im Fuß, D27.6); Spielstände und Menü bleiben, Tasten D/H/L unverändert. Neuer Dialog `report`. Texte `foot.*`. **Datei `Foot.tsx` in `tasks.yaml` und `03-TASKS.md` nachgetragen** (Plan-Wächter grün). Tests zuerst: fünf in `Foot.test.tsx` (Marke zählt, Tagesbericht erkannt, Fenster um die eigene Macht, Rendern mit Knöpfen, Depesche gesperrt), einer in `App.test.tsx` (Marke nach Start > 0, nach Öffnen der Lage weg, Vorspulen bringt die Depesche als Dialog). Ein Test fand doppelte „Diplomatie“-Knöpfe (Kopf + Fuß) — Anlass, die Kopfzeile wie im Entwurf zu räumen. Sichtprüfung im Browser (Tag 2): Fuß dreiteilig, eigene Zeile „4. Vereinigte Staaten“ in Bernstein, Marke „2“, Depesche zeigt Bilanzbalken und Moral-Zeilen | gezielt grün (Foot 5, App 76, Header 15, Panels 69, a11y, Plan 21); Lint und Typen grün |
| T-M31-04 | 2026-09-11 | **Der Machtverlauf zeigt drei Linien mit Legende.** `chartRoles(rows)` in `Standings.tsx` (rein): eigen, der **stärkste Kriegsgegner** und der **stärkste Verbündete** (die Zeilen sind nach Punkten sortiert, der erste Treffer je Zustand ist der stärkste), alle anderen `other`; `StandingsRow` trägt dafür den Beziehungszustand (`state`) neben dem Wort. `scoreSeries` färbt nach Rolle aus den Tokens (`good` / `accent` / `ally` / `inkSoft`) — **nicht mehr in Spielerfarbe** (die bleibt in der Tabelle darunter); ein Test band das alte Verhalten und wurde umgestellt. `LineChart`: `ChartSeries.role`; eigene Kurve mit **Fläche** bis zum unteren Rand (`chart__area`, 18 % Deckung), `other` dünn (0,8, 70 %) und **ohne Legende und Endwert**, die benannten mit **Endpunkt** (`chart__end`) und Endwert; dünne Linien werden zuerst gezeichnet, die benannten liegen obenauf. Ohne Rolle gilt eine Reihe als benannt — ältere Aufrufer unverändert. Tests zuerst: Auswahl aus acht Mächten (drei im Krieg, zwei verbündet → genau ein Feind, ein Verbündeter), Legende mit genau drei Namen, 8 Linien / 5 dünn / 3 Endpunkte / eine Fläche; in `LineChart.test.tsx` Flächenpfad `… L100,100 L0,100 Z` und `other` ohne Legende/Endpunkt. Zwei alte Tests griffen per `path[data-series]` jetzt die Fläche — auf `.chart__line` gebunden. Sichtprüfung im Browser (Tag 5, Frieden mit allen): eigene Kurve grün mit Fläche, übrige dünn grau, Legende nur „Vereinigte Staaten“ — ohne Feind und Verbündeten sind es ehrlich weniger als drei Namen | gezielt grün (Standings 26, LineChart 11, App 76, Foot 5); Typen und Lint grün |
| **Abschlussprüfung M29–M31** | 2026-09-11 | **`pnpm acceptance` 11 von 11, Exit 0 (7 min 2 s)** gegen den Stand nach T-M31-04 + Wächter-Fix. AK-1 unverändert an Tag 798 entschieden (2717 Eroberungen, 15 Kriegserklärungen) — der Kern ist unberührt; AK-6 Langlauf bestanden; Abdeckung gesamt 95,7 %; Anforderungstor „V1 offen: 0“. **Erster Lauf 8 von 10:** der Vorbild-Wächter (R-ASSET-01) fand das Wort „Supremacy“ in einem Kommentar von `Tooltip.tsx` — umformuliert, zweiter Lauf grün. Kontrasttest grün (17 Paare, ΔE-Wächter). `render.bench` bei freier Maschine: 300 Stapel + 945 Gebäude an Ankern p95 **3.36 ms** (Budget 16,7). Die KI-Integration (`ai-integration.slow.test.ts`) lief nicht eigens — AK-1 (Vollpartie) und AK-6 belegen den unberührten Kern gleichwertig. **Offen:** M32 nur nach Noahs Freigabe; T-M28-06/-08 danach | acceptance 11/11 |

## M32 — Kriegsrat: Lücken zu Supremacy

> **Freigabe:** Noah hat am 2026-09-11 den Bau freigegeben — „alles, in Planreihenfolge" (M32, dann T-M28-06/-08, dann die T-M28-07-Analyse). Eintrag in `DECISIONS.md`.

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M32-01 | 2026-09-11 | **Der Abmarsch lässt sich verzögern — und die Bewegungsphase weiß nichts davon.** `MoveArmyCommand.departInTicks` ist optional; `move.ts` normalisiert die Bitte (`departDelay`: fehlend, negativ, gebrochen → 0; Deckel `MAX_DEPART_DELAY_DAYS = 14` Tage) und verschiebt damit `departureTick`, `arrivalTick`, `deployDelayUntil` **und** die im Ereignis gemeldete Ankunft um denselben Betrag. Weil die Bewegungsphase ohnehin nur auf `arrivalTick` wartet, brauchte sie **keine Zeile** — der ganze Marsch rückt um n Ticks. Die halbe Kampfkraft gilt ab dem tatsächlichen Abmarsch. **Der erste Entwurf dafür war falsch und wurde am selben Tag ersetzt — siehe die Zeile „Durchsicht“ unten.** Gültig ist: `commands/move.ts` setzt `deployDelayUntil` nur noch beim *sofortigen* Abmarsch, und die Bewegungsphase setzt es am tatsächlichen (`draft.tick === army.departureTick`, mit `Math.max`, damit eine längere laufende Strafe nicht verkürzt wird). `deploymentFactor` bleibt unverändert. Golden-Master unberührt. Oberfläche: Schrittwähler `.stepper` in der Zielwahl (− / „sofort" bzw. „in n Tagen" / +, `role="group"` „Abmarsch", nur beim Marsch), die Ankunftsvorschau rechnet mit derselben Verschiebung, und „sofort" schickt das Feld **gar nicht mit** — ein Kommandolog ohne `departInTicks` ist genau das, was der Kern seit jeher liest. Tests zuerst: vier in `movement.test.ts` fielen gegen den alten Stand (Verschiebung exakt, Ereignis, „steht noch, wenn dieselbe Armee ohne Verzögerung längst angekommen wäre", volle Kampfkraft bis zum Abmarsch), zwei in `determinism.test.ts` (Hashes je Tick gleich ohne Feld und mit `0`, ungleich mit 8), vier in `Panels.test.tsx`. Ganzpartie: `ai-integration.slow.test.ts` 6/6 grün | gezielt grün (Kern 461, Panels 73, actions 23, Plan 21, Wächter 166); Typen grün; `pnpm verify` am M32-Tor |
| T-M32-02 | 2026-09-11 | **Der Markt zeigt, wohin die Kurse laufen.** `priceSeries(events, ticksPerDay)` in `game/events.ts` (rein, kernfrei): einen Kurs hat nur, was **gegen Geld** getauscht wurde — `money → X` ist ein Kauf, `X → money` ein Verkauf, beide ergeben denselben Wert „Geld je Einheit"; ein Tausch Holz gegen Erz hat zwei Preise oder keinen und bleibt draußen, statt eine Zahl zu erfinden. Das Festkomma kürzt sich heraus, weil beide Seiten Festkomma sind. Je Spieltag wird gemittelt: der Markt setzt den Kurs je Tick neu, und ein Punkt je Tick zeigt Rauschen statt Richtung. `MarketPanel` bekommt die optionale Eigenschaft `prices` und zeigt darunter `.prices` — je Rohstoff mit **mehr als einem** Punkt eine Sparkline mit Zeichen, Namen und letztem Kurs (`price()` in `format.ts`, zwei Nachkommastellen, denn die Kurse liegen nahe 1); ohne Handel steht dort nichts. Gespeist aus `eventsFor(state.eventLog, 'p1')` — der Ringpuffer begrenzt die Reihe, und für eine Richtung reicht das. `TRADE_EXECUTED` blieb unverändert. Tests zuerst: drei in `events.test.ts` (drei Ausführungen an zwei Tagen → zwei Punkte mit dem Tagesmittel 3 und 5, Verkauf mit demselben Kurs, Tausch ohne Geld fällt weg) und drei in `Panels.test.tsx` (eine Linie mit letztem Kurs, kein Abschnitt ohne Handel, kein Abschnitt bei einem einzelnen Punkt) | gezielt grün (events 48, Panels 76, App 76, a11y); Typen grün |
| T-M32-03 | 2026-09-11 | **Drei Lücken zu Supremacy sind entschieden, keine davon durch Bau.** Noahs Entscheid (DECISIONS.md, 2026-09-11): **Durchmarschrecht** — gewähren und widerrufen gibt es seit M5 (`grantRightOfWay`, die Bewegungsphase liest `relation.rightOfWay`), es fehlt nur die Gegenrichtung, *darum bitten*; nach M17 vorgemerkt, weil erst sie Durchmarschrecht zwischen zwei KI-Mächten entstehen lässt. **Provinzhandel** — existiert nirgends; nach M17 vorgemerkt, denn die eigentliche Arbeit ist die KI-Bewertung einer fremden Provinz, ohne die es ein Geschenk an sich selbst wäre. **Forschung — gestrichen:** die Freischaltungsachse existiert bereits als Spielzeit (`rules/availability.ts`, T-M15-02), ein Punktebaum wäre eine zweite Wirtschaft mit eigenem Zustand, eigener Migration und eigener KI für denselben Zweck. **Befund beim Eintragen:** der erste Versuch legte die beiden Vormerkungen als `T-M17-01/02` in `tasks.yaml` ab und machte den Plan-Wächter rot — ein Meilenstein gilt ihm als *geplant*, sobald er **eine** Aufgabe trägt, und verlangt dann für alle acht M17-Anforderungen Aufgabe und Entwurfstext (R-SPY-01…06, R-DIP-05/07). Das ist die Absicht des Wächters, nicht sein Fehler: M17 wird als Ganzes geplant oder gar nicht. Die Vormerkungen stehen deshalb im M17-Vorspann von `03-TASKS.md`, wo M17 seinen übrigen Inhalt auch führt; KRIEGSRAT §5 ist nachgezogen | Plan-Wächter 21/21 |
| **Tor M32** | 2026-09-11 | **`pnpm verify` grün gegen den Stand nach T-M32-03** — Lint, Typen, **1769 Tests** in 139 Dateien, Abdeckung Kern 96,8 %, gesamt 95,7 %, Exit 0. Zwei Läufe: der erste fiel am Lint (`MAX_DEPART_DELAY_DAYS * ticksPerDay` ist ganzzahlige Buchhaltung und braucht die begründete Ausnahme wie `availability.ts`), der zweite an einem Prosa-Fund in einer seit T-M25-03 unveränderten **Kommentarzeile** — Ursache waren CRLF-Zeilenenden, die mein Bearbeitungsskript in den Arbeitsbaum gebracht hatte; der Wächter streift Zeilenkommentare mit einem Ausdruck, dem `` im Weg steht (PROBLEME.md 2026-09-11). Die Commits waren nie betroffen (`.gitattributes` normalisiert auf LF). Dazu der Ganzpartie-Lauf `ai-integration.slow.test.ts` 6/6 grün | verify grün |
| **Sichtprüfung M32** | 2026-09-11 | **Am laufenden Spiel bestätigt.** T-M32-02: zwei Käufe „100 Geld gegen Material" an Tag 5 und Tag 6, danach steht im Marktpanel `<ul class="prices" aria-label="Kursverlauf">` mit Zeichen, „Material", Sparkline und letztem Kurs **1,01**. T-M32-01: Kaserne, Infanterie, Armee ausgewählt, „Marschieren" — der Wähler steht auf **„sofort"** mit gesperrtem „Früher abmarschieren"; Ziel Nordosten der USA meldet **Ankunft Tag 18, 20:00**, nach dreimal „+" steht er auf **„in 3 Tagen"** und die Vorschau auf **Ankunft Tag 21, 20:00** — exakt drei Tage später, gerechnet von derselben Route wie der Kern. **Falle dabei:** die Vorschau startet den Dev-Server immer im **Hauptordner**, nie im Worktree — sie servierte den Stand von `main` ohne jede Änderung, und drei Neustarts halfen nicht. Erkennbar an `fetch('/src/game/events.ts')` ohne `priceSeries`; Abhilfe war eine zweite Startkonfiguration mit `pnpm -C <Worktree-Pfad>` auf einem eigenen Port | Sichtprüfung im Browser |

## M28 — Nachzügler aus Noahs Spiel-Feedback (2026-09-08), gebaut nach der Freigabe vom 2026-09-11

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M28-06 | 2026-09-11 | **Der Einmarsch schlägt Alarm — und der Kern trug ihn vorher überhaupt nicht.** Die Entwurfsfrage der Aufgabe („welches Kern-Ereignis trägt ihn?") hat eine klare Antwort: **keines**. `ARMY_ARRIVED` geht an den Marschierenden (`audience: [army.owner]`); für den, in dessen Land marschiert wird, gab es kein Ereignis, also auch keinen Protokolleintrag, keinen Ton und keinen Vorspul-Stopp — der Einmarsch war ein Marker auf der Karte und sonst nichts. Neu ist deshalb die Art **`ARMY_INTRUDED`** (`playerId` = Besitzer, `intruderId`, `armyId`, `provinceId`), emittiert in der Bewegungsphase beim **Betreten**, nicht beim Anhalten — eine Armee, die ungehindert durchmarschiert, ist derselbe Vorfall. In `ALERT_TYPES`, mit `audience`/`concerns` nur beim Besitzer: so hält `firstAlertFor` das Vorspulen des Betroffenen an und das eines Unbeteiligten nicht (der Befund hinter `concerns`, T-M15-01). Additiv und hashneutral — `eventLog` steht in `HASH_OMIT_KEYS`, Golden-Master unberührt. Oberfläche: der seit T-M29-02 leere Chip `.header__alarm` trägt jetzt „Einmarsch: {Provinz}" in Zinnober, sein `aria-label` nennt auch die Macht, ein Klick springt zur Provinz und quittiert; die Karte zieht einen **Zinnober-Ring** (`alarmProvince`, unter dem Auswahlring, damit ein Klick den Alarm nicht verdeckt); `isSelfSetback` kennt den fünften Rückschlag, `cueFor` gibt den vorhandenen Kriegston; der Satz nutzt die Mehrzahlmechanik aus T-M23-02, indem der **Eindringling** Satzgegenstand ist. Tests zuerst: vier im Kern (`movement.test.ts`: Meldung an den Besitzer mit Alarm-Feldern, Vorspul-Stopp nur beim Besitzer, Schweigen im Frieden auch mit Durchmarschrecht, Schweigen im eigenen Gebiet), zwei in `MapCanvas.test.tsx` (der Zeichen-Mitschreiber führt jetzt jede je gesetzte Fassung, nicht nur die letzte), einer in `Header.test.tsx` (der alte „leer und verborgen"-Test bleibt daneben stehen), dazu die Zwangstabelle in `events.test.ts`. **Sichtprüfung am laufenden Spiel:** Kanada Krieg erklärt, vorgespult bis Tag 26 — Chip oben rechts „Einmarsch: Mittlerer Westen", Vorspul-Meldung „Angehalten nach 1 h: Kanada ist in Mittlerer Westen eingemarschiert.", Zinnober-Ring um die Provinz, Protokollzeile rot mit `log--self`; Klick auf den Chip zentriert die Provinz und blendet ihn aus | gezielt grün (Kern 464, Header 16, MapCanvas 13, events 48, App 76, Panels 76 — 1081 zusammen); Typen grün |
| T-M28-08 | 2026-09-11 | **Das Gefecht ruft lauter — und der Ton gehört wieder dem eigenen.** Ring und Strich angehoben (`battleRingBase` 9+7·i → **11+11·i**, `battleRingWidth` 1,4+1,6·i → **1,8+2,4·i**); neu `battleGlow(i)` — eine gefüllte Scheibe in der Gefechtsfarbe unter dem Ring, die **nie deckt** (höchstens 0,35 Deckkraft, denn die Fläche darunter trägt den Besitz); neu `battleImpacts(provinceId, i)` — bis zu fünf Einschlagzeichen, Zahl und Größe nach der Gefechtsgröße, Lage **aus der Provinzkennung**, damit nichts je Bild woanders sitzt; neu `battleFlash(msSeitRunde)` in `motion.ts` — ein Blitz je **Gefechtsrunde**, und Runden sind Ticks, nicht Bildschirmzeit. `MapCanvas` führt dafür einen Ref mit dem Uhrstand des letzten Ticks. Wie jede Bewegung fällt der Blitz weg bei `prefers-reduced-motion` und über der Tempogrenze — **der Ring bleibt trotzdem groß, denn Größe ist Zustand und keine Bewegung.** Dazu ein Fund nebenbei: `cueForEvents` sah **jedes lesbare** Ereignis an, und öffentliche sind für alle lesbar — ein Gefecht zwischen China und Indien spielte dem amerikanischen Spieler einen Kampfton vor. Neu `cueForOwnEvents(events, viewer)` filtert über `concerns`, dieselbe Frage, die `firstAlertFor` fürs Vorspulen längst beantwortet. **Bewusste Abweichung vom Bauplan:** KRIEGSRAT §4 nennt einen Explosionsmarker aus `kriegsrat-icons.svg` — den gibt es dort nicht (43 Symbole, keines davon). Statt einen Verweis auf eine nicht vorhandene Datei zu bauen, sind die Einschläge Striche in Bernstein mit hellem Kern. **Bench vorher/nachher unter Fremdlast** (ein Spiel des Nutzers band mehrere Kerne, Last 37–46 %): Pfeile p95 4,09 → 5,74 ms, Stapel+945 Gebäude 7,97 → 8,63 ms — beide Pfade sind von dieser Aufgabe **nicht berührt**, die Differenz ist Fremdlast; neuer Fall „60 gleichzeitige Gefechte" p95 **5,04 ms** (Budget 16,7). Der Bericht sagt ausdrücklich, was er **nicht** misst: die Zeichenarbeit selbst liegt an einem echten 2D-Kontext und ist unter Node nicht messbar. Tests zuerst: drei in `render.test.ts` (Ring gegen den alten Stand gebunden, Schein wächst und deckt nie, Einschläge deterministisch und am Ring), zwei in `motion.test.ts`, drei in `sound.test.ts`, zwei in `MapCanvas.test.tsx`. **Sichtprüfung unvollständig — ehrlich gesagt:** in drei Partien im Vorschaufenster ließ sich kein sauberes Bild eines laufenden Gefechts holen. Das Kartenfeld dort ist 420×240 Bildpunkte, die Gefechte der KI lagen in Provinzen außerhalb des Ausschnitts, und eine Pixelprobe auf die Gefechtsfarbe ist **nicht beweiskräftig**: feindliche Stapel tragen seit T-M30-01 dieselbe Farbe im Rahmen. Bestätigt ist damit nur, was die Tests binden. **Offen für Noah:** ob ein Krieg im Vorspulen wirklich auffällt, ohne dass man das Protokoll liest — das ist sein Maßstab, und er braucht ein großes Fenster | gezielt grün (Desktop 713, Kern 464); Typen grün; Bench 9/9 |
| T-M28-07 | 2026-09-11 | **Analyse und Entwurf, kein Bau — so war die Aufgabe geschrieben** (`LEVEL-UP-3.md` §5). Der Befund ist grösser als die Frage: **das Gefecht läuft längst von allein** (zwei verfeindete Armeen in derselben Provinz kämpfen ohne jeden Befehl, `phases/combat.ts`), aber von den drei Haltungen wirkt genau **eine** — `defensive` zusammen mit „steht still" (Verteidigerwerte, kein Gegenschlag), `retreat` im Ausweichen. **`aggressive` kommt im ganzen Kern und in der KI nirgends vor** ausser in der Typdeklaration: der Knopf „Angriff" schaltet einen Zustand, den kein Rechenweg ansieht. Was Noah als Mikromanagement erlebt, ist deshalb **Bewegung, nicht Kampf** — fällt eine Nachbarprovinz, marschiert niemand hin; weicht ein Gegner aus, verfolgt ihn niemand. Die KI kann beides (`threat.ts` rechnet eine Bedrohungskarte, `military.ts` macht Marschbefehle daraus), der Spieler hat dieselbe Rechnung nicht. Entwurf **D28** in drei Bausteinen: `aggressive` verfolgt den Weichenden (über dieselbe Kommandoschiene wie ein Spielerklick, damit R-AI-01 gilt), `defensive` deckt die eingenommene Nachbarprovinz (speist sich aus `ARMY_INTRUDED`, das T-M28-06 heute gebaut hat), und eine vierte Haltung `garrison`, die bleibt, wo sie ist. **Golden-Master begründet statt umgangen:** beide Automatiken erzeugen Kommandos und sind damit nicht hashneutral — deshalb wird `garrison` der Vorgabewert und die Migration setzt bestehende `defensive` darauf um; keine alte Partie ändert ihr Verhalten, der kommandolose `tiny-500`-Lauf bleibt bitgleich. Vier Teilaufgaben geschnitten (T-M33-01…04), **bewusst noch nicht in `tasks.yaml`** — erst nach Noahs Freigabe, und eine Spielentscheidung liegt bei ihm: `garrison` oder `defensive` als Vorgabe | Plan-Wächter 21/21 |
| **Tor M28/M32** | 2026-09-11 | **`pnpm verify` grün (1786 Tests, 139 Dateien, Kern 96,8 %, gesamt 95,7 %, Exit 0). `pnpm acceptance` 8 von 10 — beide Ausfälle sind Fremdlast, und das ist gemessen, nicht vermutet.** Auf der Maschine lief ein Spiel des Nutzers (Grundlast 37–55 %, mehrere Kerne gebunden). (1) **AK-2/3/4 `pnpm verify`** fiel an *einem* Test mit „Test timed out in 5000ms" (`App.test.tsx`, Machtverlauf) — dieselbe Datei allein: 76/76 grün, und derselbe `pnpm verify` unmittelbar davor: 1786/1786 grün. (2) **AK-6 Zeitbudget** riss den Tickmedian: 4,03 ms gegen 3,5 ms gefordert, gegenüber 2,49 ms am selben Tag um 01:44 auf ruhiger Maschine. **Gegenprobe statt Vermutung:** derselbe Lauf mit *neutralisierter* Kernänderung aus T-M28-06 ergab **4,25 ms** — also **nicht langsamer mit der Änderung, sondern schneller**; der Ausschlag kommt von der Maschine, nicht vom Code. `worldmap-bench.json` behält deshalb den Wert der ruhigen Messung. Alles Übrige bestanden: **AK-1 unverändert an Tag 798** (2717 Eroberungen, 15 Kriegserklärungen — der Kern verhält sich wie zuvor, obwohl M32 und T-M28-06 ihn additiv angefasst haben), AK-6 Langlauf 1000 Spieltage, AK-2 „V1 offen: 0", AK-5 Guards, beide Frische-Wächter. **Offen: ein Abnahmelauf auf freier Maschine** — er braucht sie allein | acceptance 8/10 (Fremdlast) |
| **Durchsicht** | 2026-09-11 | **Vier Prüfer über `git diff main..HEAD`, je Befund ein Skeptiker mit dem Auftrag zu widerlegen — 10 Befunde, alle 10 überlebten die Widerlegung, drei davon schwer.** Und sie trafen genau die Stelle, die ich für elegant hielt. **Die Wurzel:** T-M32-01 hatte „wartet auf den Abmarsch" über `departureTick` ausgedrückt und daraus in `deploymentFactor` eine Ausnahme gebaut (`tick < departureTick` → volle Kraft). Diese Ausnahme kennt die **Herkunft** der laufenden Strafe nicht — und andere Phasen pflegen das Feld nicht mit dieser Bedeutung. Drei Folgen, alle am echten Code reproduziert: (1) **`departInTicks: 1` löschte jede laufende Strafe**, auch die doppelte aus dem Rückzug — ein Freikauf, jede Runde erneuerbar; (2) ein **Rückzug während der Wartezeit** ließ `departureTick` bis zu vierzehn Tage in der Zukunft stehen, und nichts räumte ihn je ab; (3) umgekehrt ließ **Abbestellen vor dem Abmarsch** ein `deployDelayUntil` stehen, das der Befehl im Voraus gesetzt hatte — die Armee stand vierzehn Tage auf halber Kraft, ohne je marschiert zu sein. **Reparatur:** `deploymentFactor` bekommt seine alte Fassung zurück; `move.ts` setzt die Strafe nur noch bei `delay === 0`; die **Bewegungsphase** setzt sie am tatsächlichen Abmarsch mit `Math.max`, sodass ein Befehl ohne Verzögerung tickgenau bleibt wie zuvor und eine längere laufende Strafe nicht verkürzt wird. Dazu zwei Oberflächenfehler: **`alarmSeenTick` überlebte den Partiewechsel** (ein Einmarsch an Tag 5 der neuen Partie blieb stumm, weil in der alten Tag 30 quittiert war) — jetzt setzen Partiestart und Laden ihn *und* `seenTick` zurück, **und** die neue reine Funktion `openIntrusion` verwirft eine Quittung, die jünger ist als die laufende Partie, falls das jemand später vergisst; und der **erste Gefechtsblitz dauerte ein Bild statt 260 ms**, weil er seinen Stempel aus der stehenden Bildschleife nahm — jetzt stempelt ihn das erste Bild nach dem Tick. Fünf neue fallende Tests im Kern, vier für `openIntrusion`; beide Reparaturen gegengeprüft, indem sie neutralisiert wurden und die Tests fielen | `pnpm verify` grün, **1794 Tests**, Kern 96,8 %, gesamt 95,7 % |
| **Tor, zweiter Lauf** | 2026-09-11 | **`pnpm acceptance` 10 von 11 gegen den reparierten Kern.** Der Lauf war nicht optional: die Reparatur hat den Kern angefasst, und der Bericht auf der Platte beschrieb damit Code, den es nicht mehr gibt. **AK-1 ist unverändert an Tag 798 entschieden** (2717 Eroberungen, 15 Kriegserklärungen) — die Aufstellungsstrafe wandert vom Befehl in die Bewegungsphase, ohne den Spielverlauf zu verschieben, und das ist der Beleg, den die Reparatur brauchte. `pnpm verify` lief diesmal **innerhalb** des Abnahmelaufs durch (96 s; beim ersten Lauf war es ein einzelner Test, der unter Last sein Zeitlimit riss). Offen bleibt allein das **Zeitbudget-Tor**, dessen Name schon sagt, was ihm fehlt: „auf ruhiger Maschine". Tickmedian 4,11 ms gegen 3,5 gefordert, bei einer Grundlast von 62–76 % durch ein Spiel des Nutzers; die Gegenprobe mit neutralisierter Kernänderung lag bei 4,25 ms, also **langsamer ohne den Code als mit ihm**. `worldmap-bench.json` behält deshalb die Messung der ruhigen Maschine (2,49 ms, 01:44 desselben Tages) | acceptance 10/11 |
| **Durchsicht M29–M31** | 2026-09-11 | **Dieselbe Behandlung für den Umbau, der schon auf `main` liegt: vier Prüfer, adversarische Gegenprobe — 13 gemeldet, 11 überlebten, vier davon schwer.** Der Stand hatte `pnpm verify`, `pnpm acceptance` 11/11 und Sichtprüfungen am laufenden Spiel bestanden. Alle elf mit Fehlerfall in `PROBLEME.md`, geschnitten in sieben Aufgaben T-M28-09…15. **Die Lehre steht über den Befunden:** keiner war teuer zu finden — nur hatte niemand gesucht. Eine adversarische Durchsicht gehört ans Ende jedes Meilensteins, nicht ans Ende des Projekts | 11 Befunde, 7 Aufgaben |
| T-M28-09 | 2026-09-11 | **Die Bedienung ohne Maus bekommt ihre Tasten zurück.** Befund 8 war der schwerste der Oberfläche: `isTypingTarget` kannte nur Textfelder — und deckte damit genau den Fall nicht ab, um den es geht. Wer tabbt, landet auf **Knöpfen**, und der Browser löst einen Knopf mit der Leertaste aus; das Spiel fing sie ab und pausierte. **Kein einziger Knopf war ohne Maus zu betätigen, auch „Vorspulen" nicht.** Neu `isInteractiveTarget` (Knopf, Verweis, `summary`, `details`, `role="button"`/`"link"`, dazu alles aus `isTypingTarget`); die Karte gehört bewusst nicht dazu, dort bleibt die Taste dem Spiel. Dazu Befund 11: das Fortsetzen setzte fest `speed = 10` und **umging die eingestellte Höchstgeschwindigkeit** — bei Maximum 2 lief das Spiel danach fünffach zu schnell; jetzt `min(RESUME_SPEED, maxSpeed)`. Fünf neue Tests in `keyboard.test.ts` | gezielt grün (keyboard 21, Desktop 722) |
| T-M28-10 | 2026-09-11 | **Die Tempo-Gruppe zeigt immer genau eine Stufe.** Der Kommentar im Quelltext versprach es seit T-M29-02 — die Umsetzung verglich auf **Gleichheit** mit einer Raste. Liegt die Geschwindigkeit dazwischen (und das tut sie, sobald die Höchstgeschwindigkeit sie kappt: Klick auf 50 bei Maximum 30 ergibt 30), war **kein** Knopf gedrückt, der Klick sah folgenlos aus und ein Screenreader meldete keine aktive Stufe. Neu `activeSpeedStop(speed)`: die größte Raste, die die laufende Geschwindigkeit nicht überschreitet. Dazu Befund 10 — das **Vorspulen ist keine Tempostufe**, sondern ein Lauf mit Ziel, und trägt deshalb kein `aria-pressed` mehr; vorher waren während des Laufs zwei Knöpfe derselben Gruppe gedrückt. Drei neue Tests, zwei fielen gegen den alten Stand | gezielt grün (Header 19) |
| T-M28-11 | 2026-09-11 | **Das Bauplatz-Raster sagt nur, was die Sicht weiß.** Es wurde bedingungslos gezeichnet; bei einer fremden Provinz führt `publicView` weder `buildings` noch `buildQueue`, und sieben freie Felder behaupteten „hier steht nichts" — bei einer Hauptstadt mit Festung Stufe 3 eine glatte Falschauskunft. Die übrigen Felder desselben Panels halten die Nebelregel (Moral und Bevölkerung fehlen dort), das Raster brach sie; **die Fassung vor dem Umbau zeichnete die Gebäudezeile nur bei vorhandenen Gebäuden.** Jetzt erscheint das Raster nur, wenn die Sicht Gebäude führt. Zwei neue Tests, einer fiel gegen den alten Stand. **Offen bleibt Befund 5 derselben Aufgabe** (mehrere gleichzeitige Aufträge derselben Art) — er verlangt ein Feld je Auftrag statt je Art und ist eine eigene Runde wert | gezielt grün (ui 290) |
| T-M28-12 | 2026-09-11 | **Gebäude finden ihren Platz auch in Binnenprovinzen — und verschwinden nicht unter dem Armeekasten.** Befund 1: `placeBuildings` reservierte **immer** zwei Anker für Hafen und Werft, auch mitten im Binnenland. Reichten die übrigen nicht für die fünf Landarten, fiel der Modulo-Rückfall auf einen **belegten** Anker, und `taken.has(anchor)` verwarf das Gebäude — während die zwei reservierten leer blieben. Der Kommentar versprach ausdrücklich, die Arten teilten sich knappe Anker „der Reihe nach"; der Modulo-Rückfall kollidierte stattdessen. Jetzt nimmt ein Gebäude ohne freien Stammplatz den **ersten freien** Anker: kein Anker bleibt leer, solange ein Gebäude ohne Platz ist, und die feste Zuordnung im Normalfall bleibt, sodass ein neuer Bau die anderen weiterhin nicht verschiebt (der alte Test dazu steht unverändert daneben). Befund 2: der Rückfall-Anker ist der Provinzmittelpunkt — genau der Punkt des Armeekastens, der ihn mit 30×18 und deckendem Grund restlos überdeckte; **92 von 237 Provinzen** laufen in diesen Rückfall. **Der erste Reparaturversuch war falsch:** ein Versatz am Anker schob ihn in kleinen Provinzen wie Bahrain aus der Fläche, und der Wächter „jeder Anker im Land" fiel. Der Versatz gehört ins Zeichnen, in Bildpunkte — `markersFor` erkennt den Mittelpunkt-Anker und setzt das Quadrat darunter. `BUILDING_OFFSET_Y` ist dabei von der festen 12 auf eine **Rechnung** umgestellt (halbe Kastenhöhe + halbe Quadrathöhe + 1 = 17): die 12 war schon für den alten 20×14-Kasten zu knapp. Acht neue Tests | gezielt grün (map 141) |
| T-M28-13 | 2026-09-11 | **Ein Klick wählt die Armee, die man sieht.** Stehen zwei eigene Armeen in derselben Provinz, bekommen ihre Marker denselben Punkt; gezeichnet wird die **letzte** der Liste, und genau deren Kasten mit Zahl und Zustandsbalken sieht der Spieler. `pickArmy` lief dieselbe Reihenfolge ab, übernahm mit `distance < bestDistance` aber die **erste** — der Klick wählte die verdeckte Armee, und die obenauf liegende war per Karte überhaupt nicht anwählbar. Ein Zeichen: `<=` statt `<`. Zwei neue Tests, einer fiel gegen den alten Stand | gezielt grün (markers 40) |
| T-M28-14 | 2026-09-11 | **Jede Macht bekommt eine eigene Farbe — Noahs Entscheid: mehr Farben, nicht weniger Mächte.** Der Startdialog erlaubt so viele Gegner, wie die Karte Startaufstellungen hat, auf der Weltkarte **24**; `PLAYER_COLORS` hatte **elf**. Dreizehn neue, gesucht mit derselben Greedy-Methode wie in T-M29-01 unter allen vier geltenden Bedingungen: Kontrast über 4,5 gegen die Kartenschrift, ΔE über 10 gegen jede andere Spielerfarbe, gegen die Grenze, die Neutralfüllung und die Auswahlfarbe — und **Buntheit höchstens so hoch wie die bunteste bestehende**, damit der gedeckte Kriegsrat-Ton erhalten bleibt. Kleinster Abstand im Satz: 13,3; kleinster Kontrast 4,51. **Die Farben allein reichten aber nicht:** `colorForPlayer` streute die Kennung und rechnete modulo — `p1` bis `p24` fielen auf nur **dreizehn** Fächer. Der Streuwert war stabil, aber nicht injektiv. Die Kennungen sind selbst schon eine lückenlose Nummerierung; sie direkt zu nehmen ist ebenso stabil und kann nicht kollidieren. Alles, was nicht der Form `p<n>` folgt, fällt auf den alten Streuwert zurück. Drei neue Tests | gezielt grün (Desktop 739) |
| T-M28-15 | 2026-09-11 | **Der Spiegel-Wächter sieht jede Farbe.** Er las nur sechsstellige Hex-Werte, und nur im **ersten** `:root`-Block — ein grüner Wächter über einer Menge, die er nicht vollständig sieht, also genau das Muster, gegen das er gebaut wurde. Neu `toHex` (Kurzform, achtstellig mit Alpha, `rgb()`/`rgba()`), `rootColorVariables` über **jeden** `:root`-Block, und neu `strayColors`: jede Farbe **außerhalb** von `:root` ist ein Fund, denn dort gehört `var(--token)` hin und sonst nichts. Er fand sofort die zwei Altfarben der abgelösten hellen Richtung (`rgb(31 36 32)` in der Dialog-Abdunklung und im Tutorial-Schatten); beide stehen jetzt als `color-mix` aus `ground`. Fünf neue Tests. **Nebenbei ein eigener Fehler:** mein Bearbeitungsskript schrieb echte Steuerzeichen in die Wächterdatei (ein CR und ein Rücksetzzeichen mitten in zwei regulären Ausdrücken) — der Wächter `no-control-characters` hätte das gefangen, aufgefallen ist es schon vorher am Übersetzer | gezielt grün (Wächter 8) |
| T-M28-16 | 2026-09-11 | **Mehrere Bauaufträge derselben Art sind einzeln sichtbar.** Der Kern erlaubt zwei gleichzeitige Aufträge derselben Gebäudeart (`buildSlots` 2 in der Stadt), und jeder trägt sein eigenes `completesAtTick`; das Raster hat je Art ein Feld und fand mit `find()` nur den ersten. Der zweite **bezahlte** Auftrag hatte weder Fortschritt noch Fertigstellung, und nach Abschluss des ersten sprang der Balken ohne Erklärung zurück. Jetzt trägt das Feld je Auftrag einen eigenen Balken mit eigener Restzeit. Dazu die zweite Hälfte des Befundes: beide Abbrechen-Knöpfe hießen „Kaserne abbrechen" — wer den falschen drückt, verliert den falschen Bau und merkt es hinterher; bei mehreren Aufträgen derselben Art nennt der Knopf jetzt den Fertigstellungstag, bei einem einzelnen bleibt der Text schlicht. Vier neue Tests | gezielt grün (Panels 80, actions 25) |
| **Tor Durchsicht** | 2026-09-11 | **`pnpm verify`: 1822 von 1825 Tests grün.** Die drei Ausfälle sind dieselbe Klasse wie den ganzen Abend: „Test timed out in 5000ms" unter der Fremdlast eines Spiels des Nutzers — dieselben drei Dateien einzeln gefahren sind grün (7/7). Damit sind **alle elf Befunde der Durchsicht behoben**; von 180 Aufgaben ist nur noch die zurückgenommene T-M10-02 offen | verify 1822/1825, drei Zeitlimits unter Last |
| **Sichtprüfung Farben** | 2026-09-11 | **Am laufenden Spiel bestätigt.** Neue Partie mit **23 Gegnern** — die Diplomatie führt alle 23, also 24 Mächte; `colorForPlayer('p1')` bis `p24`, im Browser aus dem ausgelieferten Modul geholt, liefert **24 verschiedene Farben**. Auf der Weltkarte ist mindestens eine der dreizehn neuen sichtbar (`#745C48`, Walnuss); die übrigen liegen in Provinzen, die auf der Weltansicht wenige Bildpunkte groß sind. Anders als bei den Gefechten ist das hier prüfbar, weil die Farbe ein **statischer** Zustand ist und keiner, der einen Tick lang existiert | Sichtprüfung im Browser |
| **Abnahme auf freier Maschine** | 2026-09-11 | **`pnpm acceptance` 11 von 11, Exit 0, 6 min 2 s** — Noah hat die Maschine freigegeben (Grundlast 13 % statt 62–76 %). **Das Zeitbudget-Tor, das den ganzen Abend riss, ist bestanden:** Tickmedian **2,65 ms** gegen 3,5 gefordert und p99 6,06 gegen 8 — gegenüber 4,03 und 4,11 unter Fremdlast. Damit ist belegt, was die Gegenprobe am Abend schon zeigte: die Ausschläge kamen von der Maschine, nicht vom Code. Auch die Zeichenbudgets fallen zurück auf ihre ruhigen Werte (Pfeile p95 **2,77**, Stapel + 945 Gebäude **4,70**, 60 Gefechte **3,24** — Budget 16,7). AK-1 unverändert an Tag 798 (2717 Eroberungen, 15 Kriegserklärungen), `pnpm verify` in 59 s, Abdeckung gesamt 95,7 %, Anforderungstor „V1 offen: 0". AK-8 bleibt die einzige Warnung und zählt nicht gegen V1 | **11/11** |

## M33 — Die Bilder für Einheiten und Gebäude (Bauplan `EINHEITSBILDER.md`, freigegeben 2026-09-11)

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M33-01 | 2026-09-11 | **Der Bildsatz steht im Code — und der Wächter, der ihn deckt, existierte vorher nur dem Namen nach.** Neu `apps/desktop/src/ui/art.tsx`: siebzehn Schattenrisse im Kasten 48 × 30, je zwei Pfade (`body` gefüllt, `cut` als Innenlinien darüber), dazu `UNIT_ART`, `BUILDING_ART`, `ART_FOR_ICON` und die Komponente `UnitArt`. **Die Zeichnungen sind nicht abgeschrieben, sondern gebunden:** `art.test.tsx` führt den Bildsatz-Ausschnitt aus `docs/design/einheiten-bilder.html` aus (dieselbe Hilfsfunktion `c(x,y,r)` für Kreise, die das Blatt benutzt) und vergleicht alle siebzehn `body`/`cut` **zeichengleich** mit dem Code — „übertragen, nicht neu entworfen" ist damit gemessen und nicht behauptet. Infanterie ist Fassung 1, der Mann; ein Test hält fest, dass `infantry_emblem` gerade **nicht** im Satz steht. **Risiko 7 repariert:** `icons.test.tsx:48` prüfte die Koordinaten mit `/-?d+(.d+)?/g` — der Ausdruck sucht den Buchstaben `d` statt der Ziffer `\d`, fand in keinem der 44 Pfade etwas, und `Math.max()` über der leeren Liste ist `-Infinity`: die Zusicherung war für den ganzen Satz leer grün. Ersetzt durch `test/path-bounds.ts`, eine echte Pfadabfahrt mit mitgeführtem Stift (M/L/H/V/C/S/Q/T/A/Z, relativ und absolut; Bézier-Extrema exakt über die Nullstellen der Ableitung, Bögen über die Mittelpunktsform abgetastet) — sie unterscheidet Längen von Koordinaten (`h-12` ab x = 3 endet bei −9) und kennt den Ausschlag eines Bogens, der in keiner seiner Zahlen steht. **Vorgeführt, dass sie rot wird**, bevor sie als Beleg zählt: `railway` am alten Satz von `v16` auf `v21` geändert → „railway ragt aus dem Feld: unten bis y = 25.00 (Kasten 24)", danach zurückgesetzt; dazu drei stehende Fallbeispiele im Test selbst. **Sie fand sofort einen echten Fehler im Entwurfsblatt:** die Kette des schweren Panzers (`M3 19h42a4.5 …`) ragte links bis −1,5 und rechts bis 49,5 aus dem Kasten — im SVG abgeschnittene Kettenenden. Korrigiert auf `M4.5 19h39a4.5 …` (bündig mit 0 und 48) in **beiden** Dateien, damit der Zeichengleichheits-Test weiter gilt. `no-foreign-assets.test.ts` nennt `icons.tsx` nicht mehr namentlich, sondern **sucht** die Bildsätze (jede Quelldatei mit SVG-Pfaddaten) und verlangt von jedem Inline-SVG ohne Dateibezug — wer einen dritten Satz anlegt, ist sofort mitgeprüft; drei künstliche Fälle beweisen, dass er rot wird, und einer, dass er gegen eine leere Menge nichts behauptet. `CONTRAST_PAIRS` bekommt `good`, `ally` und `accent` gegen `paperSunk` (gemessen 7,38 / 6,03 / 4,28 — alle über der 3:1-Schwelle für Nicht-Text, `accent` unter 4,5 und damit nur als Nicht-Text tragfähig) | gezielt grün: art 14, icons 16 (vorher 15), Kontrast 11, Assets 12 (vorher 10), CSS-Spiegel 8, Plan-Wächter 21; Typen grün |
| T-M33-02 | 2026-09-11 | **Die Rekrutierungsliste zeigt Schattenrisse statt Glyphen.** `ActionSpec` und `Action` bekommen ein optionales `art`; `recruitActions` setzt es aus `UNIT_ART`, `toAction` reicht es durch, und `ActionButton` zeichnet das Bild **im Knopf** — nicht daneben, damit das Klickziel wächst statt zu schrumpfen. Das Feld `icon` bleibt unberührt: Alarme, Tagesbericht und Karte lesen die Glyphe weiter, und bei 11 px können sie gar nichts anderes. Breite 34 px nach D33.3. Das Bild ist **namenlos** und für Vorleseprogramme unsichtbar, denn den Namen trägt der Knopf schon („Infanterie ausheben", T-M22-06) — ein zweiter Name daneben würde ihn doppelt vorlesen; ein Test hält genau das fest. Tests zuerst, sechs Stück, vier fielen gegen den alten Stand: je Einheit genau ein Bild, jedes Bild das eigene (Fläche **und** Innenlinien gegen `ART[…]` verglichen, nicht nur gezählt), Breite 34, A11y-Name wörtlich, Bild innerhalb des Knopfes, und ein Kaskaden-Wächter für R-UI-03. **Der letzte hat etwas geändert:** `.button` hatte gar keine Mindesthöhe — 24 px ergaben sich ungefähr aus Schriftgröße und Polsterung. Jetzt steht `min-height: 24px` da, und der Wächter bindet es (jsdom rechnet kein Layout, gebunden wird die Kaskade) | gezielt grün: Panels 86 (vorher 80), actions 26 (vorher 25), Desktop gesamt 765 |
| T-M33-03 | 2026-09-11 | **Alle drei Feldzustände tragen dasselbe Gebäudebild.** Frei, im Bau und gebaut unterscheiden sich in Rahmen, Fortschritt und Knopf — das Gebäude bleibt dasselbe Gebäude, 30 px nach D33.3. Im gebauten und im laufenden Feld ersetzt `UnitArt` die Glyphe und **erbt deren Namen** („2 Fabrik" fürs Ohr, die hochgestellte Ziffer bleibt sichtbar); im freien Feld steht das Bild **im Bau-Knopf** statt daneben, damit das Feld genau ein Klickziel hat. Tests zuerst, vier Stück, alle vier fielen gegen den alten Stand: sieben Felder mit sieben **verschiedenen** Bildern, je Feld gegen `ART[BUILDING_ART[key]]` geprüft und nicht nur gezählt; der Zustandswechsel ändert das Bild nicht (mit einer Zeile davor, die erzwingt, dass der Vergleich überhaupt etwas vergleicht — sonst hätte er siebenmal `null` mit `null` verglichen und wäre leer grün gewesen, genau die Bauart aus Risiko 7); Breite 30; und die Stufenzahl, deren Test jetzt zusätzlich verlangt, dass der Name am **Bild** hängt und nicht mehr an der Glyphe | gezielt grün: Panels 90 (vorher 86), Desktop gesamt 769 |
| T-M33-04 | 2026-09-11 | **Das Plättchen trägt wahlweise Glyphe oder Schattenriss — und die Karte bleibt bei der Glyphe.** `UnitMarker` nimmt ein optionales `art`; Rahmen, Besitzerfarbe, Lage und die Zahl rechts bleiben unverändert, nur die Füllung wechselt (das Gegenmittel gegen Risiko 1: zwei Bildsprachen halten nur zusammen, solange alles außer der Füllung gleich bleibt). Mit Bild ist die Platte 44 px breit statt 30, das Seitenverhältnis 30 × 18 bleibt; die Innenlinien nehmen dort `--ground`, weil der Kasten damit gefüllt ist, und nicht `--paper-sunk` wie auf der Plakette. Die Armeeliste schaltet über `ART_FOR_ICON` um — die Brücke von der Glyphe zum Bild, aus `UNIT_ICONS`/`UNIT_ART` gebaut und deshalb nicht veraltbar; App.tsx bleibt unberührt. **Abweichung vom Bauplan, mit Grund:** die **Rangliste** (`Standings.tsx`) sollte laut Plan mit umschalten — sie führt überhaupt kein Einheitenzeichen, weder `Icon` noch `UnitMarker`, sondern nur Namen, Zahlen und den Machtverlauf. Es gibt dort nichts umzustellen; `Standings.tsx`/`Standings.test.tsx` sind aus `files`/`tests` der Aufgabe entfernt statt pro forma angefasst zu werden. **Die Karte ist jetzt festgenagelt (D33-a):** ein neuer Test in `MapCanvas.test.tsx` fängt jedes `d`, aus dem die Karte einen `Path2D` baut, und verlangt, dass **jedes** aus `ICON_PATHS` stammt und **keines** aus `ART` — mit einer Zeile davor, die erzwingt, dass überhaupt gestempelt wurde. Vorgeführt, dass er rot wird: `drawIcon` einmal auf `ART.tank.body` umgestellt → „Die Karte stempelt einen Schattenriss: M5 19h38l-2-5H9ZM18 14l2-5h9l2", danach zurückgesetzt. Vier weitere Tests in `Panels.test.tsx`, zwei fielen gegen den alten Stand (Bild statt Glyphe, Breite 44); die beiden anderen sind **Haltetests** und waren absichtlich von Anfang an grün — Zahl und Name am Plättchen, und die Zuordnung Ton → Token (eigen `good`, verbündet `ally`, feindlich `accent`, sonst `ink-soft`), die diese Aufgabe gerade **nicht** ändern darf | gezielt grün: Panels 94 (vorher 90), MapCanvas 16 (vorher 15), Desktop gesamt 774 |
| T-M33-05 | 2026-09-11 | **Abnahme M33.** `pnpm verify` **Exit 0, 1859 Tests, 140 Dateien**, Kern 96,8 %, gesamt 95,8 % — gegen den Ausgangswert 1825/96,8/95,7 vom selben Tag: **34 neue Tests, keine entfernte Zusicherung, keine gelockerte Grenze.** `pnpm coverage:requirements` meldet weiter „V1 offen: 0". **Zeichenbudget (Risiko 4) nachgemessen**, Maschinenlast 18 % (die Vergleichsmessung vom selben Tag lief bei 13 %): `render.bench.slow.test.ts` 9/9, Pfeile p95 **2,37 ms** (vorher 2,77), Stapel + 945 Gebäude **2,89** (vorher 4,70), 60 Gefechte **2,46** (vorher 3,24) — alle gegen 16,7 ms Budget und alle **schneller** als der Ausgangswert; der Bauplan nennt als Marke 5,39 ms aus M26. **Was diese Zahlen nicht messen, und das ist der Punkt:** der Node-Benchmark misst `prepareFrame` der **Karte**, und die hat M33 nicht angefasst — Risiko 4 handelt von SVG-Knoten je **Panel-Zeile**. Dafür die nachgemessene Zahl: ein Plättchen wächst von 1 auf 2 Pfade (6 auf 7 Knoten), also **ein** Pfad je Zeile mehr und nicht zwei wie geschätzt; 500 Renderdurchläufe unter jsdom 254 ms mit Bild gegen 197 ms mit Glyphe (0,51 gegen 0,39 ms je Plättchen — jsdom, kein Browser, und deshalb eine Hausnummer und kein Budget). **Sichtprüfung am laufenden Spiel** (Weltkarte, Vereinigte Staaten, Dev-Server im Hauptordner): alle **17 Bilder auf einem Panel** — sieben Bauplätze in `building`-Sand, zehn Aushebe-Knöpfe in `ink`; jedes mit zwei Pfaden, keine leeren `d`, und die vom Browser gerechneten `getBBox` aller siebzehn liegen im Kasten 48 × 30 (dieselbe Auskunft wie die Pfadabfahrt, auf einem zweiten Weg). Danach Kaserne gebaut, bis Tag 4 vorgespult, Infanterie ausgehoben, bis Tag 6 vorgespult: das Plättchen der Armee zeigt den marschierenden Mann in **44 px**, `unit-marker--own`, Füllung `rgb(126,197,126)` = `good`, Innenlinien in `--ground`, die Zahl rechts daneben, Name „1 Infanterie". **Ein Befund der Sichtprüfung, sofort behoben:** im **freien** Bauplatz stand das Gebäude in `ink` statt in `building` — das Bild sitzt dort im Knopf, und `.slot > svg` erwischt es nicht mehr. `ActionButton` bekam ein `artTone`; ein Test prüft jetzt alle drei Feldzustände auf `unit-art--building` und fiel gegen den Stand davor. Die drei Entscheide **D33-a bis D33-c** stehen in `DECISIONS.md`. **Nicht gelaufen:** `pnpm acceptance` — M33 ist kernfrei (kein `packages/core`, keine `data/rules`), der Abnahmelauf vom selben Tag gilt unverändert; wer ihn trotzdem will, braucht die Maschine allein | **verify Exit 0, 1859/1859**; Bench 9/9; Anforderungstor V1 offen: 0 |

## M36 — Die Rohstoffleiste wird lesbar (Bauplan `ROHSTOFFE.md`, freigegeben 2026-09-11)

| Aufgabe | Datum | Ergebnis | verify |
|---|---|---|---|
| T-M36-01 | 2026-09-11 | **Die sieben Rohstoffzeichen sind neu — und gebunden statt abgeschrieben.** Der beschlossene Satz aus `docs/design/rohstoffleiste.html` Abschnitt 1b steht in `ICON_PATHS`: Ähre (Nahrung), Balkenstapel (Material), zwei versetzte Barren (Eisen), kantiger Brocken (Kohle), der Tropfen (Öl, unverändert — er war schon richtig), zwei Kristalle (Seltene Erden), Münzstapel (Geld). **Material trägt keinen Nadelbaum mehr**; der Rohstoff heißt seit T-M23-01 nicht mehr Holz, und der Wald im Geländesatz behält seine Bäume genau deshalb. **Test zuerst, nach dem Muster von `art.test.tsx`:** der Wächter führt den Auswahl-Ausschnitt des Entwurfsblatts selbst aus (`ALT`, `A`, `B`, `KEYS`, `AUS_B`, `C` — dieselben Zeilen, die der Browser ausführt) und vergleicht alle sieben **wortgleich** mit dem Code; dazu drei Zusicherungen, die das Nichts ausschließen (sieben Schlüssel, deckungsgleich mit `RESOURCE_ICONS`), die fünf gescholtenen Zeichen von heute ausschließen und den Baum namentlich verbieten. **Vorgeführt, dass er rot wird:** gegen den Stand vor der Änderung fielen 3 von 20 Tests (Wortgleichheit, „keines der alten Zeichen", „kein Baum"). Die Pfadabfahrt aus T-M33-01 (`test/path-bounds.ts`) deckt die neuen Pfade sofort mit: alle sieben bleiben im 24er-Kasten, Münzstapel und Barren eingeschlossen — beides Formen, die der alte, leer grüne Koordinatentest nie gemessen hätte (Bogenausschlag und relative Längen) | gezielt grün: icons 20 (vorher 17), MapCanvas 16, Panels 95 |
| T-M36-02 | 2026-09-11 | **Die Leiste zeigt, was drängt — nicht, was sich ohnehin rechnet.** Je Zelle Zeichen, Bestand und **ein Pfeil** (▲ ▼ –); die Bilanzzahl wandert in den Tooltip zu Produktion und Unterhalt, wo sie ohnehin schon standen. Drängt ein Vorrat (Schwelle: die vorhandene `SHORT_REACH_DAYS`, keine neue Zahl), steht die **Reichweite in Tagen sichtbar** in der Zelle — „2,5 T", die Auskunft, nach der gehandelt wird, und die bisher nur beim Überfahren zu sehen war. Aus einundzwanzig gleich lauten Angaben werden sieben Bestände und sieben Pfeile. Neu `reachShort` in `format.ts` mit `header.reachDays` in `de.ts`; **kurz und lang runden über dieselbe Hilfsfunktion**, damit Leiste und Tooltip nie zwei Zahlen für denselben Vorrat nennen (ein Test fährt fünf Werte ab und verlangt genau das). **Was der Umbau dem Ohr NICHT nimmt:** ein Pfeil ist fürs Vorleseprogramm nichts, deshalb steht an seiner Stelle verdeckt „Bilanz +2 je Tag", und unter der gekürzten Reichweite verdeckt „noch 2,5 Tage" — der Screenreader hört mehr als vorher, nicht weniger. **Test zuerst, elf Stück (drei in `format.test.ts`, acht in `Header.test.tsx`), acht fielen gegen den Stand davor**; zwei bestehende Zusicherungen sind mitgezogen, weil sie das alte Verhalten festhielten: „`em` trägt die Bilanz als Text" wurde zu „`em` trägt die Richtung, der Titel die Zahl" (verschärft auf `Bilanz −6`, sonst wäre sie über die Produktionszahl im selben Titel leer grün geblieben), und „`noch 2 Tage` steht nirgends sichtbar" wurde zu „sichtbar steht die Kurzform". **Ein Test in `App.test.tsx` musste ebenfalls nachziehen** — er verlangte am ganzen Leistentext ein Vorzeichen und wäre über den verdeckten Ohrtext **grün geblieben, obwohl sichtbar keine Bilanz mehr steht**: er prüft jetzt beide Seiten getrennt (sieben Pfeile, `Bilanz ±…` fürs Ohr, und am Text **ohne** die verdeckten Teile kein Vorzeichen mehr) | gezielt grün: Header 26 (vorher 19), format 21 (vorher 18), App 76, Wächter 178; Typen grün |
